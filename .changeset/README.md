# Changesets

This directory contains changesets for `ui-design-system`.

## Usage

```bash
pnpm changeset          # create a new changeset
pnpm changeset:version  # bump versions based on changesets
pnpm changeset:publish  # publish to GitHub Packages
```
