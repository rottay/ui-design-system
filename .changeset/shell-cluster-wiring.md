---
"@rottay/design-system": patch
---

WO-FAM-11 residue / cascade-wiring lot. Sixteen shell-cluster channels wire to
the governed vocabulary (search-command-bar corners/borders/weights/tracking/
line-height/touch-target, command-palette label weights, the tabs gap floor),
byte-equal at rest for rottay and evnto. One measured visual change, declared:
the four search-command-bar corner wires give bithire's `shape.radius-scale:
0.8` dial the reach it never had — bithire resting corners move 15px → 12px
and 18px → 14.4px. That is the dial doing its documented job; the previous
paint was a frozen corner the tenant decision could not reach.
