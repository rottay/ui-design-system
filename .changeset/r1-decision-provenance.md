---
"@rottay/design-system": minor
---

Judge tenant plan tiers on the selections the gate recorded, not on the leaves
the merge left behind, and bind the authored value grammar at every public
tenant-authored producer.

The document admission now returns the decision-provenance ledger it captured,
so a Standard `typography.pairing` is no longer read back as Pro font-family
authorship; the seven expressive axes refuse an out-of-domain value by name at
the v1 document, v2 document, preview and draft doors; the authored chrome caps
run at those same doors instead of only at V1 publication; and the personality
channels a first-party tenant decided survive the code-owned runtime projection
as names, so the surface override policy adjudicates against them.

```contract-diff
signature ./server#DocumentAdmission — carries the `ledger` the gate captured
```
