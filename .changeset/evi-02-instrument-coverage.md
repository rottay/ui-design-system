---
"@rottay/design-system": patch
---

WO-EVI-02 instrument lot: three gates stop lying about their own corpus. The
engine-token-audit zero-lock policy separates structural refusals (still fatal)
from roster errors, so one unbaselined counter no longer masks the run -- four
pre-existing reds surface and are left named, not re-baselined. The family-cut
readWithoutProducer arm measures the whole Modern-scope skin corpus that
resolveFamily already admits, presentation tier included; 18 rows re-pinned
from the gate's own output as newly visible debt. The adaptive/factory census
stamps a logical `<repository>/<path>` source id read from the checkout's own
package.json, so a regeneration from any worktree name is byte-identical.
