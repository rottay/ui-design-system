---
"@rottay/design-system": patch
---

R3 honest verification: the gate manifest declares the phase every entry can
actually run in, the family-cut gate catches paint behind a cast and refuses a
suppressed suite as accessibility evidence, the claim-exactness proof stops
depending on the directory the repository is checked out into, and the
contract-changeset check derives the signature surface from the package's
exported declarations instead of from a path prefix.

No published signature moved.
