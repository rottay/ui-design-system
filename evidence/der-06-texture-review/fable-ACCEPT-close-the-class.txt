Permission allow rule (.claude/settings.local.json): Write(/Users/daniel/Developer/Rottay/ui-design-system/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(/Users/daniel/Developer/Rottay/ui-design-system/**) instead (Edit rules cover all file-editing tools).
Permission allow rule (.claude/settings.local.json): Bash(cp -r /Users/daniel/Developer/Rottay/dm-recruiter/dist/* /Users/daniel/Developer/Rottay/app-bithire/node_modules/@rottay/recruiter/dist/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(cp -r /Users/daniel/Developer/Rottay/dm-scoring/dist/* /Users/daniel/Developer/Rottay/app-bithire/node_modules/@rottay/scoring/dist/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg create[A-Z][a-zA-Z]*SurfaceConfig /Users/daniel/Developer/Rottay/app-evnto/src -g *.tsx -g *.ts --no-filename) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(grep -r "Outlined.*from ''''@rottay/design-system''''\\|from ''''@rottay/design-system''''.*Outlined" /Users/daniel/Developer/Rottay/app-bithire/src/ --include=*.tsx --include=*.ts -l) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(grep -r "Outlined.*from ''''@rottay/design-system''''\\|from ''''@rottay/design-system''''.*Outlined" /Users/daniel/Developer/Rottay/app-evnto/src/ --include=*.tsx --include=*.ts -l) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(cp /Users/daniel/.claude/plans/home-ai-audit-2026-04-07/*.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-04-07-home-ai-agent-audit-davila/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg -nl import.*bDetailHeaderb src/ 2) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg -n import.*bHeaderb src/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(mv /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-*-davila.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-full-audit/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(mv /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-dm-recruiter-*-davila.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-full-audit/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
**Verdict: ACCEPT** the two-name allowlist extension, with three guardrails the lot must carry and one citation correction before the debrief is recorded as evidence.

## What I verified in r4-recon-opus at 95045d1

Mechanical checks, read-only:

| Check | Result |
| --- | --- |
| Admission table minus kernel table | repeating-linear-gradient, repeating-radial-gradient |
| Kernel table minus admission table | none |
| Tables after the proposed diff | set-equal, 34 names |
| Function names in the real micro-grid value | repeating-linear-gradient, color-mix, var, calc |
| Real value length against the cap | 329 of 512 |
| Existing value-safety suite at HEAD | 57 passed |

- **The asymmetry is exactly as stated.** Admission at `packages/core/src/infrastructure/compilers/runtime/theme/facade/foundation/admission/runtime/limits/index.ts:62-63` admits both repeating variants. The kernel table at `packages/core/src/infrastructure/compilers/kernel/foundation/css/value-safety/index.ts:35-69` admits none. The diff makes the two tables equal. That is a stronger argument than "same class as linear-gradient": the change does not widen the emission grammar, it stops emission from being narrower than admission.
- **The drop mechanism is deterministic.** Only one function name in the value is outside the kernel set. Everything else passes: no structural breakout, no forbidden token, balanced, no bare `@`, under the length cap. The predicate at `value-safety/index.ts:156-158` requires every name to be in the set, so the name alone fails it. The same value passes admission's emitted-side check because its four var references all start with `--ds-`. The assembler at `runtime/theme/runtime/emission/index.ts:37` maps over the admitted subset with no log or evidence channel. Silent, as claimed.
- **The emitter citation is wrong.** Line 1588 of chrome-variables is a comment about the classic page-shell grid, not an emitter. The value comes from the `micro-grid` motif row at `packages/core/src/foundation/tokens/ts/presentation/expressive-profiles/expansion/index.ts:361-364`. The expressive family deriver declares the channel in its produces list, and the bithire vertical document selects the motif at `src/foundation/presets/verticals/bithire/document/index.json:55`. The catalog row `profiles.expressive` declares the fan-out. "Admitted decision" holds, with the corrected pointer.
- **There is a second emitter in the class.** The `pinstripe` row at lines 365-368 of the same file also emits repeating-linear-gradient and is dropped by the same rule. No first-party document selects it today, which is why the census counted one channel. The class has two emitters, not one.
- **The channel has real readers.** The semantic-surface skin reads it with fallback `none` and the patterns sheet reads it with fallback `transparent`. The drop is a paint loss, not unused vocabulary.
- **The parity instrument is sound.** It compiles under the neutral-preset baseline and pins the compiled variable map, which sits upstream of admission. That is why it can see a drop the artifact itself cannot.
- **The safety argument holds.** A `url(` token is refused by the forbidden-token regex at any nesting depth. The names attr, image, image-set, src and element stay outside the set. The breakout regex, the length cap and the quote-and-paren scan are untouched.
- **The torture-fixture claim is vacuous.** No torture fixture carries any repeating gradient, so "zero additional admissions" is true but proves nothing about the new names. The negative test in the diff is the load-bearing safety evidence.
- **The census numbers were not reproduced here.** The probe script does not exist in this worktree, and this tree's bithire artifact still carries the two old radial-gradient literals at HEAD and in the working tree. The 1747-to-1746 count and the zero-occurrence grep are the implementer's measurement. I verified the mechanism independently, not the numbers.
- **No cross-table guard exists today.** The only test that names both predicates is the single-door architecture test, and it asserts nothing about the tables. The admission file's own comment at lines 70-74 records a previous drift in the opposite direction, when saturate and linear were present in the kernel and absent from admission. This lot is the second recorded drift of the same two copies.

## Guardrails the lot must carry

1. **Close the class, not the instance.** The admission owner already imports from the kernel at line 45 of its file, so it is direction-legal for it to import the kernel's set and delete its own copy. That is the root fix and matches the admission file's stated doctrine that one grammar split in two is what let them disagree. If the DT keeps this lot inside the kernel file, the minimum is to export the kernel set and land a test asserting the admission table equals it, in this lot and not as a follow-up. Without one of these, the next name added to admission recreates the silent drop.
2. **Positive tests with both real values.** Use the micro-grid two-layer value and the pinstripe value byte-for-byte from the expansion file, plus one `admitCssVariables` case showing the texture channel survives with the authored string unchanged.
3. **Negative tests that pin the boundary.** Assert that a repeating-linear-gradient carrying `url(x)` is refused, and that `repeating-conic-gradient(...)` is refused. The second turns the deliberate exclusion into a claim that a future symmetric edit must overturn on purpose rather than by accident.
4. **Correct the debrief before it is recorded.** Replace the chrome-variables citation with the expansion file rows, and name the pinstripe row as the second emitter in scope.

The emission-side trace proposed as alternative (iv) is a useful instrument and belongs in the follow-up, as the debrief says. It is not a condition of this diff.
FABLE_EXIT=0
