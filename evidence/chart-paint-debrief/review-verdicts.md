# Review verdicts — chart-paint debrief (WO-FAM-09)

## Kimi (DT, reviewer of record; Codex seat retired by owner order 2026-09-17) — ACCEPT

Read the full debrief (1268 lines), its census corrections, its executed divergences and its
instrument plans. The contract is sound: one resolver per family root with a read-only context
downstream makes the two-authority divergence unrepresentable; the four paint models refuse the
flattening the WO text could be misread as; the parity-locked precedence chain is preserved; the
registry turns six hand-kept lists into projections. Acceptance carries planted negatives per
instrument and an honest browser leg owed before lot 1.

### Rulings on §8 (recorded here; the owner questions are separated)
- Q3 (chart RTL posture): ACCEPT the recommendation — a separate work order; lot 9 as scoped adds
  direction awareness only, not a mirroring policy.
- Q4 (step-3 kernels for charts): ACCEPT — scoped to `useOptionalDirection` plus the
  `useChartInteraction`/`useInteractionState` reconciliation; the other kernels have no meaning
  for an SVG mark.
- Q5 (`chart-c/index.css` split): ACCEPT — one mechanical lot 8; the census instruments watch it.
- Q7 (sankey/network-graph imperative renderers): ACCEPT — a separate work order behind FAM-09;
  not folded into the paint cut.
- Appendix A decisions 1-8: ACCEPTED as recorded.

### Owner questions (escalated, not answered here)
- Q1 (`default` de-aliasing — the largest visual change in the programme): OWNER.
- Q2 (`colors` prop retirement — public API break): OWNER, with the retirement lot 7 and the
  `useSortableList` retirement (F-69 lot 9) in the same authorization package.
- FAM-14 must land before this cut may close (the skeleton arm), per the debrief's R5.

### Condition before lot 1's acceptance
The browser leg of §6.4 (computed fill of a bar mark and a pie slice across the five schemes,
light and dark, on a built dist) is executed in the DT's serialized window.
