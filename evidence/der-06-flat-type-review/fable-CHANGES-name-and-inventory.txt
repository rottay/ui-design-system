Permission allow rule (.claude/settings.local.json): Write(/Users/daniel/Developer/Rottay/ui-design-system/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(/Users/daniel/Developer/Rottay/ui-design-system/**) instead (Edit rules cover all file-editing tools).
Permission allow rule (.claude/settings.local.json): Bash(cp -r /Users/daniel/Developer/Rottay/dm-recruiter/dist/* /Users/daniel/Developer/Rottay/app-bithire/node_modules/@rottay/recruiter/dist/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(cp -r /Users/daniel/Developer/Rottay/dm-scoring/dist/* /Users/daniel/Developer/Rottay/app-bithire/node_modules/@rottay/scoring/dist/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg create[A-Z][a-zA-Z]*SurfaceConfig /Users/daniel/Developer/Rottay/app-evnto/src -g *.tsx -g *.ts --no-filename) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(grep -r "Outlined.*from ''''@rottay/design-system''''\\|from ''''@rottay/design-system''''.*Outlined" /Users/daniel/Developer/Rottay/app-bithire/src/ --include=*.tsx --include=*.ts -l) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(grep -r "Outlined.*from ''''@rottay/design-system''''\\|from ''''@rottay/design-system''''.*Outlined" /Users/daniel/Developer/Rottay/app-evnto/src/ --include=*.tsx --include=*.ts -l) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(cp /Users/daniel/.claude/plans/home-ai-audit-2026-04-07/*.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-04-07-home-ai-agent-audit-davila/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg -nl import.*bDetailHeaderb src/ 2) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg -n import.*bHeaderb src/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(mv /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-*-davila.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-full-audit/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(mv /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-dm-recruiter-*-davila.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-full-audit/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
**Verdict: CHANGES to (a).** The mechanism is right and (b) stays deferred, but the proposed name is false at HEAD, one target name collides with an existing type, and the write set is sized by line rather than by identifier, which leaves the acceptance gate certifying a half-rename. I measured on the recon tree at `4448cd9af`, where the 2a barrel hunk has already landed.

## What I confirmed

The §1.0 facts hold. `BrandTheme` is a flat interface with every family optional at `packages/core/src/foundation/contracts/composition/tenants/themes/index.ts:240`. `Theme` is total and nested with governed slots at `themes/iso/index.ts:84`. They are not aliases. The intake at `lowering/foundation/intake/index.ts:33` unwraps `Theme` into the flat shape, and `liftAuthoredTheme` at line 75 is its exact inverse. The type reaches the public root through the star export at `contracts/index.ts:33`. Renaming is the right move: after 2c-ii nobody authors a first-party BrandTheme, and (c) would leave a name that says "Brand" over a shape whose brand authority is gone.

## Why the proposal needs changes

- **`ThemeReadView` is a false claim.** The proposed doc line says "Not an authoring surface", but the flat shape is the authored draft of the studio, the preview and every fixture, not only the lowering's read view. Brand studio mutates it as a draft at `brand-studio/index.tsx:1089`. The preview intent takes it as `draft` at `ingress/presentation/preview/index.ts:35`, whose own doc states that the authoring surfaces edit a BrandTheme, not a document. Ingress projects it through `authoredThemePatch` at `ingress/foundation/draft-patch/index.ts:67`. The showroom holds it in React state at `playground/theme-builder/page.tsx:67`. Under (a) those drafts stay flat, because moving them to `Theme` or a v2 document is the deferred (b) lot. A name that only becomes true after an undated lot is the pattern the repo's own test-name law forbids. The name must describe the shape, not one of its two roles. `FlatTheme` or `UngovernedTheme` pass that test. `ThemeReadView`, `LoweredThemeInput` and `ThemeInputShape` each name the lowering role only and fail it. The DT picks the name; the criterion is what I am fixing.

- **`ThemeFloors` already exists.** The table row "BrandThemeFloors/Authorship" is not a type. Those four lines are the function `mergeBrandThemeFloors` at `lowering/foundation/floors/index.ts:18`. No identifier named `BrandThemeFloors` or `BrandThemeAuthorship` exists in the tree. The proposed target collides with the exported interface `ThemeFloors` at `contracts/composition/tenants/themes/resolved/index.ts:35`, republished by the compiler barrel and used by the test harness. Rename the function to `mergeThemeFloors` and never mint a type called `ThemeFloors`. The other proposed names are currently free.

- **The inventory is by line, not by identifier, and it is short.** Twenty-four distinct identifiers carry the substring in non-test source. The table names nine. The rest sit inside the "tipo plano" count:

  | Identifier | Lines | Owner |
  |---|---|---|
  | `serializeBrandTheme`, `deserializeBrandTheme` | 6, 4 | brand-studio file-export, PUBLIC at `.` |
  | `liftTenantConfigToBrandTheme`, `createTenantBrandTheme` | 6, 4 | tenant-preview, tenant authoring |
  | `cloneBrandTheme`, `mergeBrandThemeFloors` | 5, 4 | file-export, lowering floors |
  | `evaluateBrandThemeContrast`, `draftBrandTheme`, `normalizeBrandTheme`, `BrandThemeEditor`, `applyHostileBrandTheme` | 3, 3, 2, 2, 2 | brand-studio |

  The two serializers are runtime exports listed in `contracts/runtime/suppliers/index.json`, so the MAJOR changeset must carry them, not only the type rows and the three values.

- **The gate is case-sensitive and goes blind to the lowercase half.** One hundred seventeen non-test source lines carry only `brandTheme*` and read as zero under the gate as written. Among them are `brandThemeToPersonality`, `brandThemeToTokenOverrides` and `brandThemeToChromeVariables` inside the lowering, plus `brandThemeToTenantAppearance` and its `Advanced` sibling, which are PUBLIC at `.`. Under (a) with the current gate, a lowering function named for a brand theme would take the renamed type and the gate would certify the lot complete. Also in that set is `brandThemePath`, a serialized contract field at `contracts/composition/tenants/capabilities/index.ts:72` that the admission adapter parses. Renaming a field is a data change, not a type rename.

## Guardrails the rename must carry

1. **Acceptance is case-insensitive with an enumerated allowlist.** Replace the gate with `grep -rniE "brandtheme"` over src, scripts and showroom, excluding tests, equal to zero, with `brandThemePath` either allowlisted under a written exception or moved to its own lot. Nothing may be swept silently.
2. **Write set by identifier, not by line.** Enumerate all twenty-four camel-case and ten lower-camel identifiers before substitution, and grep every new name for collisions across src, tests, showroom and scripts first.
3. **Zero semantic edits.** The diff in the thirteen files holding the sixty-seven governed reads must be identifier substitution only. Keep the `\bas\b` count invariant per file, and add one more invariant: no new `isGovernedActive` call under `lowering/` during this lot.
4. **Re-point the gates that name the retired identifiers, in the same commit.** `tests/architecture/theme-lowering-single-door` matches `lowerBrandThemeFixture` at lines 470, 687 and 870, and its planted-positive test plants that exact name. `scripts/check/verticals/retired-identity/index.mjs:54` builds `platformBrandTheme` from the substring. Both must learn the new names, and each planted positive must be re-run red.
5. **Docs in the same session.** The repo's own contract-type rule requires `docs-engineering/engineering/design-system/foundations/contracts/README.md` and `runtime/tenancy/README.md`. Twenty docs files name the type. The clause at `CLAUDE.md:380` is false today and must be rewritten in this lot, not left for later.
6. **Record (b) as its own work order now.** Its outcome is the studio draft moving to `Theme` or a v2 document and the flat shape losing its authoring role. Without an owner the flat shape survives by default, and the name you choose today has to stay honest for as long as that takes. One fact for that WO: `liftAuthoredTheme` has zero production callers in src or showroom, only tests and the single-door gate import it.

One thing I checked that needs no change: the changeset parser accepts free text after the em dash, so a row of the form `export .#BrandTheme — renamed to <Name>` is valid as written.
