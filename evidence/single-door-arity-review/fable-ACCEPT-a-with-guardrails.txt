Permission allow rule (.claude/settings.local.json): Write(/Users/daniel/Developer/Rottay/ui-design-system/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(/Users/daniel/Developer/Rottay/ui-design-system/**) instead (Edit rules cover all file-editing tools).
Permission allow rule (.claude/settings.local.json): Bash(cp -r /Users/daniel/Developer/Rottay/dm-recruiter/dist/* /Users/daniel/Developer/Rottay/app-bithire/node_modules/@rottay/recruiter/dist/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(cp -r /Users/daniel/Developer/Rottay/dm-scoring/dist/* /Users/daniel/Developer/Rottay/app-bithire/node_modules/@rottay/scoring/dist/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg create[A-Z][a-zA-Z]*SurfaceConfig /Users/daniel/Developer/Rottay/app-evnto/src -g *.tsx -g *.ts --no-filename) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(grep -r "Outlined.*from ''''@rottay/design-system''''\\|from ''''@rottay/design-system''''.*Outlined" /Users/daniel/Developer/Rottay/app-bithire/src/ --include=*.tsx --include=*.ts -l) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(grep -r "Outlined.*from ''''@rottay/design-system''''\\|from ''''@rottay/design-system''''.*Outlined" /Users/daniel/Developer/Rottay/app-evnto/src/ --include=*.tsx --include=*.ts -l) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(cp /Users/daniel/.claude/plans/home-ai-audit-2026-04-07/*.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-04-07-home-ai-agent-audit-davila/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg -nl import.*bDetailHeaderb src/ 2) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg -n import.*bHeaderb src/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(mv /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-*-davila.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-full-audit/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(mv /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-dm-recruiter-*-davila.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-full-audit/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
**Verdict: ACCEPT (a), conditioned on the guardrails below. (b) and (c) rejected.** One adjacent defect surfaced during verification that none of the three options fixes; it is registered at the end and does not block (a).

## What I verified read-only at HEAD 449e86e55

- **The law and its reason are as quoted**, born in commit bc1d206df. The shape it retired was `resolveTheme(baseline: Theme, intent?: ThemeIntent)` with an early return when the intent was absent, before any intent check. The intent was the optional argument. Today the intent is the first argument and the option carries a baseline. The roles are inverted, so an arity rule no longer expresses the property it was written for.
- **The resolver's order holds.** The first statement of `resolveTheme` at `resolution/index.ts:286` is the unconditional `assertThemeIntent(intent)`. The supplied baseline is cloned, relabelled with the intent's slug and then validated by `assertThemeBaseline` at line 294.
- **The door is the only two-argument caller.** The gate's AST census, which includes the showroom tree, finds exactly one. The brief misquotes it, though. Lines 135 to 140 of `facade/runtime/compile/index.ts` pass a conditional: `{}` when the source is `brand-theme`, `{ baseline: baselineFor(target.vertical, target.slug, baselineSource) }` otherwise. The amended assertion must admit that shape, or the call site must be normalized. Normalizing costs a second clone of the authored theme per default-path request, because the resolver clones a supplied baseline again at line 292. I recommend admitting the conditional.
- **The override cannot be an arbitrary Theme.** The door's `baselineFor` is the facade baseline owner's three-argument version, gated by the closed set `THEME_BASELINE_SOURCES` and throwing on anything else. Callers of the door can only name a source through `CompileThemeIntentOptions.baselineSource`. This closed derivation, more than the validation, is what makes the second argument not an ingress, and the amended law must pin it.
- **Gate state confirmed by running it**: 2 failed, 52 passed, the single finding is the door.
- **The arity law lives only in the test.** Section 11 of `docs/architecture/index.md` states one lowering and the chain, not the argument count. The amendment is therefore a test amendment plus its record in WO-DER-06; no prose law is owed elsewhere.

## Why not (b) or (c)

- **(b)** adds a second owned export of the resolution door with identical validation. That is exactly the shape the file's own compileTheme tests call the defect at line 399. The arity law would stay mechanically green while the property it protects, one validated ingress, split across two names, and the gate would need a fresh census for the new name anyway. Letter over substance.
- **(c)** puts resolved visual content into the serializable intent document and turns the validated door into a Theme-shaped ingress by data. Section 11 already forbids the same move for the catalog: never an intent field, never a request option.

## Guardrails the amended law must carry

1. **Declaration site.** A named constant beside `COMPILE_DOOR` at line 252 of the test, for example `RESOLVE_THEME_OPTIONS_CALLER = COMPILE_DOOR`, plus a key allowlist `["baseline"]`. Key the exception on `COMPILE_DOOR`, not on `INGRESS_OWNER`. The latter is the `runtime/ingress/` directory prefix and would admit two-argument calls from the three intent producers, which is wider than intended.
2. **Assertion shape**, in one test replacing line 479. For every `resolveTheme` call: one argument passes; two arguments are allowed only at the declared caller; more than two is a finding anywhere. At the declared caller the second argument is unwrapped through a ConditionalExpression and every branch must be an ObjectLiteralExpression whose property names are a subset of the allowlist, so `{}` passes. A `baseline` property's initializer must be a CallExpression to the identifier `baselineFor`. Any other node kind as the second argument, including an identifier, a spread or a call, is a finding. Finish with exact equality: the set of two-argument callers equals `[RESOLVE_THEME_OPTIONS_CALLER]`. Exact equality means a door that stops passing options turns the gate red so the exception gets deleted, which is the precedent this file set when it emptied `APPEARANCE_DOOR_KNOWN_CALLERS`.
3. **Pin the option surface at its declaration.** In the resolution owner, the interface `ResolveThemeOptions` must declare exactly the member `baseline`. The fourth origin would be born as a new key such as `intent` or `skipAdmission`, and a call-site subset check catches usage, not declaration.
4. **Executable proof of the order.** The brief's "cannot recur" claim has no test today. The three tests in `resolution/tests/explicit-baseline.test.ts` cover relabel, default and an invalid baseline, none passes an invalid intent together with a valid baseline. Add one: an intent with an unknown vertical plus a valid neutral baseline throws the intent error, not the baseline error. Optionally also assert structurally that the first statement of the resolver's body calls `assertThemeIntent`, since an early return placed before it is the exact historical regression.
5. **Mutant coverage**, each on a planted private copy, each producing exactly one finding while the unmutated tree produces zero: a two-argument call at the existing victim file outside the door; at a door copy, a second key beside `baseline`; at a door copy, an identifier and a spread as the second argument; at a door copy, a `baseline` initializer that is not a `baselineFor` call; three arguments at the door; a resolution-owner copy whose options interface declares a second member; and a tree where the door's calls are all one-argument, which must fail the exact-equality half.
6. **Rename the test.** "Takes exactly ONE argument at every productive call site" becomes a false claim once amended. The name must state the real contract.
7. **Rewrite the comment at lines 480 to 482** to record three things: the retired shape and that it returned before the intent was validated; the admitted shape and that the intent is validated first and the baseline by `assertThemeBaseline`; and that the door derives the baseline from the closed source set, so no caller of the door can hand a Theme in.

One pre-existing blind spot the amendment should not widen: `callsTo` matches a bare identifier, so an aliased import or a property-access call evades the whole file's census. Adding import specifiers whose original name is `resolveTheme` to the census closes the cheap half.

## Finding to register in WO-DER-06, outside this gate's scope

**The intent admission measures against a floor that is not the one the resolution was made over.** At line 41 the admission owner imports the resolution owner's two-argument `baselineFor`, the authored theme, and calls it at line 135 for `movedLeaves` and `envelopeIssues`. `ThemeResolution` carries no baseline field, so the admission cannot read the real floor from the resolution. Meanwhile the door hands `admitThemeCompilation` the correct `baselineResolution.theme`. Under `neutral-preset`, one tenant intent is admitted against two floors, and D6-2a's own commit message records that the floors differ on evnto dark. No productive tenant hits this today: the neutral-preset callers at `artifact-renderer/index.ts:247` and the bundle-build script are static-vertical, which return at `admitEngine`. The only test driving a tenant document over the neutral source, `decision-reach.test.ts:191`, asserts channel reach and never the admission. So the drift is live in code and untested. Fix direction for D6-2c: the door computes the baseline once and passes it to `admitThemeIntent`, the admission owner drops its `baselineFor` import, and this gate then pins that the admission owner never calls `baselineFor`. The amended comment must not claim the override reaches every station until that lands.
FABLE_EXIT=0
