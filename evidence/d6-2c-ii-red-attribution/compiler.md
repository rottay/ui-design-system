# D6-2c-ii-RED — grupo `compiler` (13 archivos, 42 aserciones)

## Corrección a la pre-clasificación (lo más importante de este reporte)

**Las 42 aserciones de mi grupo fallan IDÉNTICAS en el HEAD prístino.** Ninguna es del lote 2c-ii.

Método: corrí los mismos 13 archivos en `/private/tmp/claude-502/-Users-daniel-Developer-Rottay-r4-recon-opus/778bf176-68ee-4238-8118-28710cce6025/scratchpad/head-2cii/ui-design-system/packages/core` (`git archive HEAD`). Resultado: 42/42 títulos rojos en las dos ramas, 0 lot-only. La comparación es válida porque los artefactos que estas suites leen son **byte-idénticos** entre HEAD y el árbol (`cmp` sobre `artifacts/generated/css/verticals/{bithire,rottay}/index.css` y `src/foundation/tokens/css/facade/artifacts/bithire/index.css`).

La causa que la pre-clasificación atribuye ("receipts pineadas a la autoridad autorada retirada") es correcta como MECANISMO, pero el lote que la produjo es **D6-2c-i** (el pipeline neutro, ya en HEAD), no 2c-ii. 2c-ii las hereda. La adjudicación (A) se aplica igual; lo que cambia es la atribución.

Logs: `/private/tmp/claude-502/-Users-daniel-Developer-Rottay-r4-recon-opus/778bf176-68ee-4238-8118-28710cce6025/scratchpad/red/compiler-before.log` (42 rojos), `/private/tmp/claude-502/-Users-daniel-Developer-Rottay-r4-recon-opus/778bf176-68ee-4238-8118-28710cce6025/scratchpad/red/compiler-head.log` (HEAD), `/private/tmp/claude-502/-Users-daniel-Developer-Rottay-r4-recon-opus/778bf176-68ee-4238-8118-28710cce6025/scratchpad/red/compiler-vitest.log` (final).

## Resultado

`vitest run` sobre los 13 archivos: **458 passed / 0 failed (13 files)**. Antes: 42 failed / 414 passed. La población **creció en 2 tests**; no se borró ninguna aserción sin sujeto nombrado, no se bajó ningún umbral.

`tsc --noEmit` 0. `typecheck:tests` OK sobre baseline 0. `eslint` sobre los 16 archivos tocados: exit 0.

---

## Clase 3 · premium-regression (10 aserciones) — todas (ii) RE-ANCHORED

Medido, por artefacto, qué chrome sobrevive:

| vertical | sobrevive | ausente |
|---|---|---|
| bithire | `--ds-sidebar-{bg,text,item-color,item-bg-active}`, `--ds-button-primary-{bg,color}`, `--ds-input-{bg,border,border-focus}`, `--ds-table-header-bg` | geometría de sidebar, variantes de botón ≠ primary, layout, shell |
| rottay | los 4 canales de tono del sidebar | todo el resto (sin semillas de paleta) |
| evnto | los 4 canales de tono del sidebar | todo el resto (sin semillas de paleta) |

La SECCIÓN 2 declaraba: "these premium vars exist in handwritten CSS but are NOT yet generated by the shared pipeline". Esa premisa está muerta: el CSS handwritten se retiró y el pipeline es el único productor. Re-anclada sobre lo que el pipeline PRODUCE, y lo que no produce queda **aseverado por ausencia**, de modo que el día que el carril de derivación entregue un productor la fila se pone roja.

| aserción | disposición | medición |
|---|---|---|
| bithire CSS has sidebar chrome | (ii) | 4 de 6 sobreviven; `--ds-sidebar-group-font-size` y `--ds-sidebar-icon-size` sin productor |
| rottay CSS has sidebar chrome | (ii) | 1 de 7 (`--ds-sidebar-bg`); las 6 de geometría/footer sin sujeto |
| evnto CSS has sidebar chrome | (ii) | 3 de 4; `--ds-sidebar-group-font-size` sin productor |
| bithire CSS has button chrome | (ii) | `primary-{bg,color}` sí; `secondary-*` sin productor. Las dos aserciones negativas (`-text`) se conservan intactas |
| rottay CSS has full button variant chrome | (ii) | **sujeto muerto**: rottay no autora paleta por alcance del owner, así que no compila chrome de botón alguno. Los 5 canales quedan aseverados por ausencia |
| evnto CSS has button and input chrome | (ii) | **sujeto muerto**, misma causa; 4 canales por ausencia |
| rottay/evnto CSS has table header chrome (2) | (ii) | **sujeto muerto** por la misma causa; bithire lo conserva y se asevera aparte |
| rottay CSS has layout chrome | (ii) | **sujeto muerto**: el tema autorado de rottay era la única fuente de `--ds-layout-*` |
| rottay CSS has shell grid chrome | (ii) | **sujeto muerto**: ídem `--ds-shell-grid-*` |

Nada se borró: cada canal perdido quedó en una lista aseverada por ausencia, con su razón.

## Clase 5 · sidebar-group-channels (4) — todas (ii) RE-ANCHORED

La lectura **se movió**, no se perdió. El corte de familias le dio al menú sus propios canales: el deriver de chrome emite ahora `--ds-menu-*` con el canal de sidebar como primer operando, y el skin lee el nombre del menú.

| campo | canal sidebar | canal menú que lo encadena | default encadenado |
|---|---|---|---|
| groupMarginTop | `--ds-sidebar-group-margin-top` | `--ds-menu-group-margin-block` | `var(--ds-spacing-1)` |
| groupMarginBottom | `--ds-sidebar-group-margin-bottom` | `--ds-menu-group-margin-block` | `var(--ds-spacing-1)` |
| groupPaddingTop | `--ds-sidebar-group-padding-top` | `--ds-menu-group-padding-block` | `var(--ds-spacing-2)` |
| itemIndent | `--ds-sidebar-item-indent` | `--ds-menu-child-padding-inline` | `var(--ds-spacing-2)` |

La cadena queda aseverada **de punta a punta y con un eslabón MÁS** que antes: edición tipada → canal sidebar emitido → canal menú que lo encadena con su default → el skin lee el canal menú. El default de un tenant que no setea nada cambió de literal (`0.5px` / `calc(6px * density)` / `0px`) a la rampa de espaciado; es un cambio del corte de familias, preexistente, y queda escrito en el test.

## Clase 6 · db-row-canary (5) — (ii) RE-ANCHORED

Medido: el row DB emite `--ds-density-mode-factor: 1.15`, `--ds-elevation-1`, `--ds-color-primary`, `--ds-type-scale`; NO emite `--ds-radius-scale`, `--ds-effect-intensity`, `--ds-sidebar-bg`. El baseline bithire emite `radius-scale 0.8`, `effect-intensity 0.2`, `sidebar-bg var(--ds-color-neutral-900)`, `density-mode-factor 0.85`; NO declara `--ds-elevation-1` (sólo lo lee).

| aserción | disposición | medición |
|---|---|---|
| 'radius' diverges | (ii) | el row autora 0.8 y el preset dice 0.8 → el delta retira el canal. Divergencia aseverada con el eje movido a 1.15, y la coincidencia aseverada al lado |
| 'effect intensity' diverges | (ii) | ídem: row 0.2 = preset 0.2; movido a 0.55 |
| 'sidebar chrome' diverges | (ii) | ídem: el row pide `inverse`, que el preset ya selecciona; movido a `strong` |
| density diverges (expectativa INVERTIDA) | (ii) | el preset decide `density.mode: compact`, así que el baseline **sí** emite la postura (0.85) donde el tema autorado no lo hacía. Es una MEJORA: la ley de vocabulario único queda aseverada en AMBOS lados (1.15 vs 0.85) en vez de en uno |
| elevation is concrete on both paths | (ii) | el baseline deja la rampa a la fundación y la **lee** (`var(--ds-elevation-1)`), el tenant la declara concreta. Aseverado: un solo nombre de canal, tenant concreto, baseline consumidor |

Convención reutilizada: la misma `baselineAlreadyStates` + `moved` que el bloque REACHABLE de este archivo ya usaba.

## Clase 4 · tone-ink-authority (4) — (ii) RE-ANCHORED

Medido, resuelto sobre los bundles shipped:

| vertical | info-ink | warning-ink |
|---|---|---|
| bithire | `#0369A1` (era `#3A6FB0`) | `color-mix(in srgb, #B45309 55%, #171717 45%)` (era `#D6A04E` sobre `#191919`) |
| rottay | `#60a5fa` (era `#60A5FA`) | `color-mix(in srgb, #d97706 55%, #171717 45%)` |
| evnto | `#60a5fa` (era `#475569`) | `color-mix(in srgb, #d97706 55%, #171717 45%)` |

- **El pin de rottay era SÓLO mayúsculas** (`#60A5FA` vs `#60a5fa`), como anticipaba el brief. Corregido como tal y dicho en el comentario.
- bithire: cambio de valor REAL y la autoridad **sigue intacta** — sus mixes resuelven contra las semillas que su preset declara (`palette.status-seeds`), donde antes usaban las del tema autorado; el operando neutro pasa de su `#191919` propio al `#171717` de la fundación.
- rottay/evnto: sin semillas de status por alcance del owner, resuelven contra las de la fundación y por eso **coinciden entre sí**.
- `follows the vertical tone seeds instead of a frozen literal`: el conjunto distinto pasa de 3 a 2, y la aserción se re-ancló **sin perder fuerza**: bithire debe llevar su propia semilla (`#B45309`) y diferir de rottay; rottay y evnto deben coincidir *porque ninguna declara semillas*; el conjunto debe tener 2 miembros. Si el mix se aplanara a una constante, el conjunto colapsa a 1 y la fila falla, que es exactamente lo que la ley anti-aplanamiento pide.

## Clase 9 · verify-and-point

### adapters/tests/index.test.ts (5) — (iii) DEFECT FIXED en producto

Causa medida: la superficie autorada del engine modern lee **2** de los 6 canales declarados por `navigation.sidebar-tone` (`--ds-sidebar-item-bg-hover`, `--ds-sidebar-item-bg-active`). Las otras 3 que la celda nombraba (`--ds-sidebar-text`, `--ds-sidebar-text-muted`, `--ds-sidebar-item-color-active`) llegan a pintura por una cadena que **sólo existe en el artefacto generado** (`--ds-menu-item-color: var(--ds-sidebar-text, ...)`), y este instrumento cuenta lecturas sobre CSS **autorado** por diseño (`producers` excluye artefactos explícitamente).

Probé primero declararlas como `carriers`; el instrumento las rechaza correctamente ("no CSS producer derives ... "), porque la cadena no está en CSS autorado. Así que la declaración se corrigió a la verdad medida:

- `modern`: `read: ["--ds-sidebar-item-bg-hover", "--ds-sidebar-item-bg-active"]`, con la razón escrita.
- `classic` / `rustic`: `unaccounted: 5 -> 2` (la brecha es la de los canales que **otro** engine sí lee; los demás no los lee ninguno y el instrumento los excusa), y la razón reescrita para decirlo.

No mueve un solo byte emitido: la celda es declaración de la matriz, no entra en `project` ni en la emisión.

### El resto (12 aserciones) — (ii) RE-ANCHORED, todas preexistentes en HEAD

| suite | aserción | medición y disposición |
|---|---|---|
| i0-inventory (3) | `%s artifact uses canonical button color vars` | bithire tiene `--ds-button-primary-color`; nadie tiene `-secondary-color`; rottay/evnto no compilan chrome de botón. La **ley de nomenclatura** (nunca `-text`) se conserva a full fuerza para los 3, y la presencia se asevera donde hay productor. +1 test |
| cascade-layers (3) | `the %s artifact contests exactly the pinned channels` | el censo de canales disputados es **vacío** en los tres: el chrome de badge/card que lo poblaba era autorado. Es el estado que el propio comentario describe como la meta, no una supresión; la fila sigue fallando si un artefacto vuelve a declarar un canal del bridge |
| tokens.contract (1) | `pins the references that only resolve through their fallback` | el censo fallback-only es vacío: los 5 canales ya **no son referenciados** por ningún espejo público (verificado: tampoco están declarados bajo el CSS root, así que lo que se fue fue la referencia) |
| undeclared-chain-repair (1) | `keeps --ds-color-bg tenant-causal across all three` | bithire lo declara (tiene paleta); rottay/evnto no (estructural-neutro) y lo resuelven de la fundación, que la fila hermana ya asevera que lo declara. Re-anclado a "resolvable para los tres", con las tres atribuciones aseveradas |
| bithire-motion-interaction (1) | `projects the compiled motion tokens into the artifact (parity)` | las 3 duraciones intactas; el easing sigue a `motion.character`, que el preset de bithire declara `mechanical` → `cubic-bezier(0.4, 0, 0.2, 1)`. El par §2.6 organic sigue aseverado en el brazo organic, que ya pasaba |
| a11y-floors-contract (1) | `declares the canonical channel and enforces it in every core control skin` | el menú lee `var(--ds-menu-touch-target-min, var(--ds-touch-target-min))`: el piso **sí** es alcanzable, pero el resolver del test exigía un literal y no seguía el fallback anidado (su regex cortaba en el primer `)`). Instrumento **fortalecido**: ahora sigue la cadena como la sigue la cascada, con profundidad limitada; una cadena que no llegue a 44px sigue fallando |
| spring-precompute (2) | rottay/evnto ship a precomputed `linear()` | **(i) REGISTERED**, hueco (s1)/D-27 ya registrado: ningún preset declara `motion.useSpring`, así que **ningún** vertical precomputa y los tres resuelven el `cubic-bezier` de la fundación. Pineado en las dos direcciones, para que enrojezca cuando el carril devuelva el precompute |
| decisions (2) | contraste a nivel de decisión | el "before" (1.08:1 y 2.27:1) se midió sobre bundles que ya **no declaran** esos canales. Re-anclado: se asevera la AUSENCIA en el bundle (la mitad viva de "no longer ships") y el "before" queda como cifra registrada. El "after" se mide sobre el baseline shipped y da **5.62:1**, sobre el piso de 3. En evnto, con semillas **ambas** rampas derivan y difieren (`#1C3FBF` claro / `#79A1FF` oscuro), lo que es mejor que la afirmación original ("la oscura se queda en el gris del baseline") |

Nota medida para el DT sobre `decisions`: el bloque oscuro de bithire **no re-declara el ground**, así que el scope oscuro resuelve el de base — la brecha de mode-palette seeding que ya registraste. La aserción lo nombra en vez de esconderlo.

## Archivos tocados

**Tests (13)** — los 13 de `g-compiler.txt`.

**Producto (3)** — sólo la clase (iii) de adapters:
- `src/infrastructure/compilers/runtime/theme/presentation/adapters/presentation/modern/index.ts`
- `src/infrastructure/compilers/runtime/theme/presentation/adapters/presentation/classic/index.ts`
- `src/infrastructure/compilers/runtime/theme/presentation/adapters/presentation/rustic/index.ts`

Ninguno cambia bytes emitidos. No toqué `roadmap/**`, el changeset ni ninguna baseline de gate.

## Registro propuesto (texto exacto para el coordinador)

> 2026-09-15 (D6-2c-ii-RED, grupo compiler): las 42 aserciones de las 13 suites de compilador/tokens estaban ya rojas en HEAD (A/B contra `git archive HEAD`, artefactos byte-idénticos): las hereda 2c-ii de D6-2c-i, no las causa. Re-ancladas sobre el contrato neutro+preset, con lo que el pipeline NO produce aseverado por ausencia para que el carril lo enrojezca al aterrizar: geometría de sidebar (`--ds-sidebar-{group-font-size,icon-size,border,width,collapsed-width,header-height,item-font-size,footer-bg}`), variantes de botón ≠ primary (`--ds-button-{secondary,default,ghost,success}-bg`), `--ds-layout-*`, `--ds-shell-grid-*`, y el chrome alimentado por paleta en rottay/evnto (estructural-neutro por alcance del owner). Censo de canales disputados de cascade-layers ahora VACÍO en los tres (nada tiene dos dueños). Censo fallback-only de tokens.contract VACÍO (la referencia se retiró; los 5 canales siguen sin declarar). Hueco (s1) spring confirmado: ningún preset declara `motion.useSpring`, los tres verticales resuelven el cubic-bezier de fundación. DEFECTO CORREGIDO en producto (no mueve bytes emitidos): las celdas `navigation.sidebar-tone` de los tres adaptadores declaraban 5 canales leídos donde la superficie autorada del modern lee 2 — las otras 3 llegan a pintura por una cadena `--ds-menu-*` que sólo existe en el artefacto generado, y este instrumento cuenta CSS autorado por diseño; `unaccounted` de classic/rustic 5 -> 2. Instrumento a11y touch-target FORTALECIDO: sigue el fallback anidado `var(--ds-menu-touch-target-min, var(--ds-touch-target-min))` como lo sigue la cascada, en vez de exigir un literal. Verificado: 458 passed / 0 failed en los 13 archivos (antes 42 rojos), población +2, tsc 0, typecheck:tests 0, eslint 0.
