Permission allow rule (.claude/settings.local.json): Write(/Users/daniel/Developer/Rottay/ui-design-system/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(/Users/daniel/Developer/Rottay/ui-design-system/**) instead (Edit rules cover all file-editing tools).
Permission allow rule (.claude/settings.local.json): Bash(cp -r /Users/daniel/Developer/Rottay/dm-recruiter/dist/* /Users/daniel/Developer/Rottay/app-bithire/node_modules/@rottay/recruiter/dist/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(cp -r /Users/daniel/Developer/Rottay/dm-scoring/dist/* /Users/daniel/Developer/Rottay/app-bithire/node_modules/@rottay/scoring/dist/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg create[A-Z][a-zA-Z]*SurfaceConfig /Users/daniel/Developer/Rottay/app-evnto/src -g *.tsx -g *.ts --no-filename) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(grep -r "Outlined.*from ''''@rottay/design-system''''\\|from ''''@rottay/design-system''''.*Outlined" /Users/daniel/Developer/Rottay/app-bithire/src/ --include=*.tsx --include=*.ts -l) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(grep -r "Outlined.*from ''''@rottay/design-system''''\\|from ''''@rottay/design-system''''.*Outlined" /Users/daniel/Developer/Rottay/app-evnto/src/ --include=*.tsx --include=*.ts -l) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(cp /Users/daniel/.claude/plans/home-ai-audit-2026-04-07/*.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-04-07-home-ai-agent-audit-davila/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg -nl import.*bDetailHeaderb src/ 2) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg -n import.*bHeaderb src/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(mv /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-*-davila.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-full-audit/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(mv /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-dm-recruiter-*-davila.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-full-audit/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
**Fable review, WO-EVI-05 debrief (radio/shape scoped semantic exclusion). Verdict: CHANGES.**

Verification performed read-only on r4-recon-opus at HEAD cb792de04. The debrief pins base 3dd32ac68; that commit is an ancestor, two docs-only commits behind HEAD, and the four reviewed files are byte-identical between the two, so every pin below holds at both.

Facts confirmed:
- Radio Modern skin has exactly one CSS file and exactly two radius declarations, both `var(--ds-radius-full)`, on `[data-part='circle']` at line 64 and `[data-part='dot']` at line 203. The root rule (lines 32-47) paints no border, background or radius. The focus ring is an outline on the circle (lines 183-187). The touch target is `min-block-size` (line 42). The BASE radio token file declares no radius channel, unlike toggle, which has `--ds-toggle-track-border-radius` and `--ds-toggle-dot-border-radius`.
- Shape head channels in the catalog are `--ds-radius-scale`, `--ds-radius-md`, `--ds-radius-nest-inset`, `--ds-radius-nest-ratio`, `--ds-radius-button`. `--ds-radius-full` is not one, so radio's shape membership today comes only from the `byProperty` path in `familyAxisDeclarations`. Excluding those two declarations does remove the family from shape with no other signal remaining.
- `radio-group` is a separate population family by folder under the agnostic skin root, in shape by both property and head channel (line 92 reads `--ds-radius-md`), mountable via `.ds-radio-group[data-part='root']` at line 30, and not in the WO-FAM-01 roster, so not in the pilot.
- The committed pilot record (`test-artifacts/gates/axis-difference-pilot/index.json`, catalog 2a25f7c6068407fb, Chromium 149) shows both orientations with exactly the two radio/shape failures, 16 negative cells all at zero and all evidential, and shape readings 4/5 in both modes. 29/30 is measured, not asserted.
- The fleet probe uses `axisPopulations` from the same owner for its effective denominators, so a change inside `familyAxisDeclarations` propagates to pilot and fleet without a second mechanism.

**(i) Scope.** The shape of the amendment is right: parts-bound, closed data, applied inside the one derivation function. Two gaps. First, the current owner has no selector awareness at all; `declaredProperties` is a regex over the joined text. A selector-bound exclusion needs a rule-level tokenizer in the population owner, applied uniformly to every family, and reusing the `SELECTOR_RULE` vocabulary the axis-difference probe already has rather than a second grammar. Second, and this is the substantive defect: the exclusion is bound to the selector but the semantic identity lives in the value. A circle is round because the authored value is the full radius. If line 64 later becomes `border-radius: var(--ds-radio-circle-radius, var(--ds-radius-full))`, which is exactly what toggle received in c98d199e7, the corner is tenant-configurable, the family must be in shape, and a selector-only exclusion would keep it N/A. The registry entry must pin selector and authored declaration verbatim, and the guard must require that the excluded selector carries no other shape-authored longhand and no other value anywhere in the family's skin, including repeated rules with the same selector text and rules under at-rules. The exclusion must also be declared to act on the `byProperty` path only; a head-channel read is never excluded.

**(ii) N/A honesty.** 29/29 plus 1 N/A is honest only if the N/A is carried wherever the denominator is printed. Required: `pilotReadings` and `populationLine` carry a per-axis `notApplicable` count beside `moved/denominator`, so no record or runner output can be read as 4/4 without the N/A; the pilot pin and the fleet floor are republished in the same commit with old and new membership, the floor moving 217 to 216 under a provenance entry that says reviewed exclusion, not fold and not shrink; the pilot test reads the N/A set from the owner's live derivation and asserts it equals the published pin, otherwise `NOT_YET_GREEN` is only renamed. The executable acceptance also mis-states its own count: the population suite is exactly 15 cases today, so "15/15 with the new registry drills" is impossible; publish the actual new count.

**(iii) Mutants.** (a), (b), (c) are necessary and not sufficient. Add (d): in a sandbox, front the excluded declaration with a channel or replace its token, and require the exclusion to invalidate, radio to re-enter shape, and `checkPilotPopulation` to fail against the pin. Add (e): a second rule with the same selector text and a different radius, later in the file, with the same required outcome. Add (f): a registry entry whose selector does not exist in the named file fails, checked on comment-stripped text so a comment cannot keep a stale exclusion alive (the F-23 class). Add (g): a registry entry for a family that still declares radius on a non-excluded selector has no effect. Mutant (a) should be stated precisely: it is proven at the population level without a browser, because the pin mismatch fires in the first pilot assertion.

**(iv) Radio.Group.** No duplicate skin is needed and the exclusion cannot reach `radio-group`, since it is keyed by family id and file path. The obligation as written is loose in two ways. It must name the population family id `radio-group`, not "the radio family", because the family-cut roster folds Radio.Group segments into `radio` while the population counts `radio-group` as its own row, and the two instruments disagree on ownership. And it must include a fleet mount with `buttonStyle` stamped: without a mount the probe reads only the family element, which is the root, and the root declares no radius, so the line 92 corner is never measured. Declared shape-applicable and never read is existence, not integration.

**(v) Drift left unguarded.** The value drift in (i) and the same-selector repeat in (iii) are the two real holes. Smaller ones: the registry schema should admit `axis` generically but a test must pin the admitted set to exactly `radio/shape` so any addition returns to review; a new `presentation/components/skin/radio/` folder would join the family through `skinFamilies` and its radii must count as non-excluded; and the appearance of `--ds-radius-full` as a shape head channel in a future catalog row must re-enter the family automatically, which the byProperty-only rule guarantees if stated.

**Verdict: CHANGES.** The direction is correct and the owner's constraints are respected. Before implementation: (1) bind the exclusion to selector plus verbatim authored declaration, byProperty only, with mutants (d) through (g); (2) carry `notApplicable` through readings, runner line, pin, floor and the pilot assertion, derived from the owner, with the acceptance count corrected; (3) rewrite the Radio.Group obligation to name `radio-group` and require a button-style fleet mount. With those three landed and audited, ACCEPT.
