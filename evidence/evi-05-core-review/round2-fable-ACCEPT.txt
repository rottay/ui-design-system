Permission allow rule (.claude/settings.local.json): Write(/Users/daniel/Developer/Rottay/ui-design-system/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(/Users/daniel/Developer/Rottay/ui-design-system/**) instead (Edit rules cover all file-editing tools).
Permission allow rule (.claude/settings.local.json): Bash(cp -r /Users/daniel/Developer/Rottay/dm-recruiter/dist/* /Users/daniel/Developer/Rottay/app-bithire/node_modules/@rottay/recruiter/dist/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(cp -r /Users/daniel/Developer/Rottay/dm-scoring/dist/* /Users/daniel/Developer/Rottay/app-bithire/node_modules/@rottay/scoring/dist/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg create[A-Z][a-zA-Z]*SurfaceConfig /Users/daniel/Developer/Rottay/app-evnto/src -g *.tsx -g *.ts --no-filename) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(grep -r "Outlined.*from ''''@rottay/design-system''''\\|from ''''@rottay/design-system''''.*Outlined" /Users/daniel/Developer/Rottay/app-bithire/src/ --include=*.tsx --include=*.ts -l) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(grep -r "Outlined.*from ''''@rottay/design-system''''\\|from ''''@rottay/design-system''''.*Outlined" /Users/daniel/Developer/Rottay/app-evnto/src/ --include=*.tsx --include=*.ts -l) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(cp /Users/daniel/.claude/plans/home-ai-audit-2026-04-07/*.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-04-07-home-ai-agent-audit-davila/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg -nl import.*bDetailHeaderb src/ 2) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(rg -n import.*bHeaderb src/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(mv /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-*-davila.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-full-audit/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
Permission allow rule (.claude/settings.local.json): Bash(mv /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-dm-recruiter-*-davila.md /Users/daniel/Developer/Rottay/docs-engineering/archive/audits/2026-06-09-bithire-full-audit/) has a wildcard before the rest of the command, so it also matches any options inserted at that position and approves them without a prompt. Replace that * with the exact value you mean, or only use * after the subcommand.
**Fable confirmation, WO-EVI-05 radio/shape, compliance package against 2d537ae85. Verdict: ACCEPT.**

Verification performed read-only on r4-recon-opus. HEAD is 7f872b4de, one docs commit above 2d537ae85. Every reviewed path is byte-identical between the two and carries no working-tree modification. The pinned source commit 0867fb986 exists and is an ancestor five commits below. I re-ran the two node suites on this tree (they write only to tmpdir sandboxes) and the population CLI in pilot, floor and exclusions modes. I did not re-run the Chromium pilot, because it rewrites the committed record, so I verified the committed record instead. I did not re-run typecheck:tests. That figure remains the auditor's.

Measured on this tree:
- Population suite 31/31 (was 15, the 16 new cases are the exclusion block). Axis-difference 62/62.
- Population CLI: pilot shape 4 (1 N/A), fleet shape 216 (1 N/A), exclusions revision 34bd866512177f9b, all three modes exit 0. The live digest of the committed registry equals the digest in the pilot pin, the floor and the record.
- Committed record `test-artifacts/gates/axis-difference-pilot/index.json`: Chromium 149.0.7827.55, bundle fresh, catalog 2a25f7c6068407fb with 29 decision rows. Both orientations have failures [] and refusals []. 12 positive cells per run, all evidential, shape movedIds exactly button/checkbox/segmented/toggle in light and dark. 16 negative cells per run, all evidential at 0 moved. pilotReadings carry moved/denominator/notApplicable/declared, shape 4/4 plus 1 N/A, every other axis 5/5 plus 0. applicablePairs 29, notApplicablePairs ['radio/shape']. radio-group-continuity is evidential in light and dark, moved true, property border-top-left-radius.

**Condition (1), selector plus verbatim declaration, byProperty only, mutants d to g: MET.**
- The registry pins both declarations as selector, property and value. They match the radio skin at lines 64 and 203 character for character, and the suite asserts they are the only radii the file authors. `EXCLUSION_PATHS` is ['byProperty']. A head-channel path is refused by name in the registry check. The head-channel read at index.mjs:435 never consults the registry, so `--ds-radius-full` appearing as a shape head channel re-enters the family by construction, and the suite asserts it is not one today.
- `cssRules` is a brace-aware, quote-aware, at-rule-aware rule tokenizer. The suite asserts it yields the same property set as the joined-text reader over the whole corpus, attributes every SELECTOR_RULE match, and shares the probe's literal. Matching is by normalized selector plus lowercased property plus normalized value, so a changed value, a fronted channel, `!important`, another longhand on the same selector, or a repeated selector with another radius fails to match and the family re-enters. An identical declaration repeated under an at-rule matches, which hides nothing because the value is the pinned full radius.
- Drills present and green: (d) in three variants with the registry naming the STALE circle declaration, (e) duplicate selector later in the file, (f) selector absent and absent-in-comment, (g) an ineffective entry on checkbox leaving every published denominator unchanged, the admitted set pinned to exactly radio/shape, duplicate, unadmitted, head-channel path and foreign skin refused on both gates, and a reworded registry refused by revision on both gates. Mutant (a) is stated and proven at population level without a browser, in both suites.

**Condition (2), notApplicable end to end and the count corrected: MET.**
- `familyAxisDeclarations` returns notApplicable with reason, review and the excluded declarations. A family that still authors the axis anywhere keeps membership with exclusionEffective false and is listed as ineffective, never withdrawn. `populationReport` carries notApplicable and its count per axis. `populationLine` prints the N/A beside every denominator. `pilotPopulation` carries notApplicable, its counts and exclusionsRevision. `pilotReadings` carries notApplicable beside moved/denominator, and `withNotApplicable` adds the declared total.
- The pilot pin is 4 applicable plus 1 N/A with previousPin 5/0 and shape removed ['radio'] under "reviewed exclusion (neither fold nor shrink)". The floor is 216 with notApplicable.shape 1, previousPin 217, the same provenance kind, and the physical-longhand widening published with newEntrants []. The pilot check refuses overlap, pinned-N/A-now-declared, declared-now-N/A, reason or review drift, count drift and revision drift. The floor check refuses N/A count drift and exclusions revision drift.
- The pilot test reads the N/A set from the live derivation with revision checking on, asserts it deep-equals the pin, pairs equal ['radio/shape'], failures [] in both orientations, effectiveFamilies.shape excludes radio while typography includes it, every reading moved == denominator with N/A 1 on shape and 0 elsewhere, and 29 applicable pairs. NOT_YET_GREEN no longer exists in the file. The published count is the real one.

**Condition (3), radio-group: MET.**
- The registry entry names the lawful owner as the population family `radio-group`. The pilot test mounts the real RadioGroup with buttonStyle="solid", asserts the markup carries the button class, the option part and the button-style stamp, runs it through the same probe in Chromium in both modes, asserts effectiveFamilies.shape contains radio-group, both shape cells evidential with radio-group in movedIds, and that its presence changes no pilot verdict. The committed record shows the option corner moving in light and dark. Population drill (c) proves the exclusion cannot reach radio-group and that a severed chain keeps it applicable.

Not blocking, recorded for whoever picks them up:
1. The probe's own CLI summary at axis-difference/index.mjs:1389-1401 prints "denominators (declared by check/theme/population): shape 216" and the reconciliation rows without the "(1 N/A)". The automation runner prints populationLine, which carries it, and every pin and gate carries it, so nothing can pass on the omission. One line to append the N/A count there.
2. The fleet probe has no mounts registry, so a fleet run still reads radio-group on its root, where no radius is authored. Its corner is measured only by the continuity drill. That is honest, a non-move and never a hidden pass, and outside EVI-05, but a fleet shape claim should not cite radio-group as moving until the fleet run mounts it.
3. The 17:20 progressLog entry still carries the earlier wording "future shape-owner obligation ... no Modern skin owner". The landed registry field, drill and record supersede it by naming radio-group and measuring it. Append-only history, no action.
4. On (iii-f), Codex's call: I do not require a browser-level CSS mutant. The exclusion is value-bound and acts before any browser runs, and the browser-reachable hiding path would need an identical declaration the browser paints differently, which does not exist.

**Verdict: ACCEPT.** The three conditions are landed, drilled and audited as required. The published figure is 29 applicable plus 1 reviewed N/A, never 30, with both negative controls at zero and the severed mutant detected.
