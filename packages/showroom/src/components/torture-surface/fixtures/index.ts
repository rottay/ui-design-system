/**
 * Showroom-owned whitelabel proof fixtures (COPY-FIRST, 2026-07-18).
 *
 * Re-homed from the @rottay/design-system public barrel after commit 5d2157a8
 * drained them (tenant-named proof fixtures are not public DS API). See each
 * child module's provenance header. These are PROBE fixtures, never product
 * tenants.
 */

export { tortureDarkBrandTheme, tortureLightBrandTheme, TORTURE_PROBE_VARS } from './torture';
export { themanagementmiamiBrandTheme } from './themanagementmiami';
