/**
 * Curated DS icon set.
 *
 * This is the public entry for `@rottay/design-system/icons`.
 * It exposes the DS's own icon primitives (BaseIcon, UserIcon, SearchIcon, etc.)
 * — a small, intentional set.
 *
 * For the full Ant Design icon catalog, import directly from `@ant-design/icons`
 * (it is a peer dependency and always installed in consuming apps).
 */
export * from './icons/index';
