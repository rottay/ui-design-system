/**
 * @fileoverview Compilers — shared compilation logic for the DS.
 *
 * brand-theme/: BrandTheme bridge functions + compileBrandTheme
 * _shared/: color math, scale builders (used by runtime + static)
 */
export * from './brand-theme';
