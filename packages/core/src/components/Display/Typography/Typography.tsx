import { Typography as AntTypography } from 'antd';

// Re-export main component and subcomponents
export const Typography = AntTypography;
export const { Title, Text, Paragraph, Link } = AntTypography;
