'use client';

/**
 * @fileoverview ColumnSettingsDropdown -- Rustic engine.
 * Re-exports the classic implementation since ColumnSettingsDropdown is built
 * entirely on DS primitives and does not require engine-specific rendering.
 */

export { default } from './classic';
