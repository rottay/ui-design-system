/**
 * @fileoverview Spinner Compound Components - Rottay Design System
 * @description Compound component definitions for the Spinner component.
 * Currently a placeholder for future compound component additions.
 *
 * @remarks
 * The Spinner component is primarily a simple indicator and does not require
 * compound components in most use cases. However, this file exists for:
 * - Future extensibility (e.g., Spinner.Overlay, Spinner.Container)
 * - Consistency with other component structures in the design system
 * - Potential custom spinner content wrappers
 *
 * @example Future compound components (not yet implemented)
 * ```tsx
 * // Potential future usage
 * <Spinner.Overlay visible={isLoading}>
 *   <Content />
 * </Spinner.Overlay>
 *
 * <Spinner.Container fullscreen>
 *   <Spinner size="xl" label="Loading application..." />
 * </Spinner.Container>
 * ```
 *
 * @module Spinner/Compound
 * @category Feedback
 * @package @rottay/design-system
 */

// ============================================================================
// Compound Components
// ============================================================================

// Compound components will be added here as needed
// Potential future additions:
// - SpinnerOverlay: Full-screen or container overlay with spinner
// - SpinnerContainer: Wrapper with centering and backdrop
// - SpinnerText: Enhanced label with animation support
