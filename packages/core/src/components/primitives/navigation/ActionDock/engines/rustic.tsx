/**
 * @fileoverview Rustic engine for ActionDock.
 *
 * ActionDock is engine-agnostic (uses DS primitives Box/Flex which
 * resolve through engines themselves), so all three engine entry points
 * delegate to the shared implementation.
 */

export { default } from '../ActionDock';
