/**
 * @fileoverview Classic engine for MobileHeader.
 *
 * MobileHeader is engine-agnostic (uses DS primitives Box/Flex/Text which
 * resolve through engines themselves), so all three engine entry points
 * delegate to the shared implementation.
 */

export { default } from '../MobileHeader';
