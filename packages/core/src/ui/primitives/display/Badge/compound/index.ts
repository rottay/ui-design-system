/**
 * @fileoverview Badge Compound Components - Rottay Design System
 * @description Compound components for enhanced Badge functionality.
 * Part of the Rottay Design System's display primitives collection.
 *
 * @remarks
 * This module is reserved for compound components that extend Badge functionality.
 * Future compound components will be added as needed.
 *
 * **Planned Components:**
 * - `Badge.Ribbon` - Ribbon-style badge for card corners
 * - `Badge.Count` - Specialized count display with animation
 *
 * @example Future Ribbon Usage
 * ```tsx
 * <Card>
 *   <Badge.Ribbon text="New" color="red">
 *     <Card.Body>Content</Card.Body>
 *   </Badge.Ribbon>
 * </Card>
 * ```
 *
 * @see {@link Badge} for the main component
 * @module BadgeCompound
 * @category Display
 * @package @rottay/design-system
 */

// Note: Badge compound components (Ribbon, Count) are defined in the types
// but implementations will be added in future iterations.
// The main Badge component handles all current use cases.

// Future compound components to be implemented:
// export { BadgeRibbon } from './Ribbon';
// export { BadgeCount } from './Count';
// export type { BadgeRibbonProps, BadgeCountProps } from './types';
