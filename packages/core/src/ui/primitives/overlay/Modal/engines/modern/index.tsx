/**
 * @deprecated Compatibility adapter.
 *
 * `feedback/Modal` owns the single Modern Modal implementation. This path is
 * retained only for direct legacy imports and deliberately contains no second
 * engine.
 */

export { ModernModal as default } from '../../../../facade';
