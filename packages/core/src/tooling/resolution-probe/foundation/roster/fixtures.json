{
  "$schema": "./schema.md",
  "note": "A fixture that names a real component takes its element shape FROM THAT COMPONENT — rendered, then pasted — never from the CSS. Deriving the shape from the CSS is what let `button-modern-md` and `input-modern-md` sit on `.ds-btn`/`.ds-input` for months: a selector states what an element must carry to be MATCHED, and says nothing about whether any component emits it, so a CSS-derived fixture can measure a rule that never paints and still look green. Regenerate a component shape by rendering the engine implementation with `renderToStaticMarkup` and pasting the result, minus React-generated ids, plus `data-probe`. The synthetic fixtures (the two skeleton controls and the token readout) are exempt: they exist to read a dial, not to stand for a component. `requiresSelectors` is not documentation either: the harness asserts each string occurs verbatim in the CSS it is about to measure and marks the fixture `unmatched` if it does not, so a fixture that stops being matched is reported, never silently measured as zero. Pin selectors and the paint statement, never a declaration's VALUE — a precedence ruling would then read as a broken fixture.",
  "fixtures": [
    {
      "id": "card-modern-md",
      "why": "The canonical derived-radius consumer. Chain: border-radius <- --ds-card-radius <- --ds-card-border-radius <- --ds-radius-lg <- calc(--ds-radius-lg-base * --ds-radius-scale). Four hops from the tenant dial to a painted pixel.",
      "requiresSelectors": [
        ".ds-card.ds-card--modern[data-part='root'][data-radius='md']",
        "--ds-card-border-radius: var(--ds-radius-lg)"
      ],
      "html": "<div class=\"ds-card ds-card--elevated ds-card--modern\" data-variant=\"elevated\" data-radius=\"md\" data-size=\"md\" data-padding=\"md\" data-part=\"root\" data-probe=\"card-modern-md\"><div data-part=\"body\">card body</div></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='card-modern-md']",
          "properties": [
            "border-top-left-radius",
            "border-bottom-right-radius",
            "background-color",
            "color",
            "border-top-color",
            "border-top-width",
            "box-shadow",
            "padding-top",
            "font-family",
            "font-size"
          ]
        }
      ]
    },
    {
      "id": "button-modern-md",
      "why": "The modern Button as the engine actually renders it. Until 2026-08-11 this fixture carried `.ds-btn`, a RUSTIC theme rule no component can produce (both engines render `rottay-button--{modern,rustic}`), so every reading attributed to 'the modern button' measured a rule that never paints. The token defect it exposed happened to be real, which is the dangerous case: a lying instrument that returns a true finding teaches you to trust it. Chain: border-radius <- --_ds-button-resolved-radius <- the size rule's precedence between --ds-button-md-radius and --ds-radius-md.",
      "requiresSelectors": [
        ".rottay-button.rottay-button--modern[data-part='trigger']",
        "border-radius: var(--_ds-button-resolved-radius)",
        ".rottay-button.rottay-button--modern[data-size='md']:not([data-size-responsive])",
        "--_ds-button-resolved-radius: var("
      ],
      "html": "<button data-recipe=\"feedback.press\" data-recipe-state=\"final\" data-variant=\"primary\" data-size=\"md\" data-shape=\"default\" data-part=\"trigger\" type=\"button\" class=\"rottay-button rottay-button--modern rottay-button--primary rottay-button--md rottay-button--default\" style=\"--ds-recipe-enter:0ms;--ds-recipe-exit:0ms;--ds-recipe-settle:0ms;--ds-recipe-cycle:0ms;--ds-recipe-x:0px;--ds-recipe-y:0px;--ds-recipe-scale-from:1;--ds-recipe-curve:var(--ds-motion-ease-out)\" aria-disabled=\"false\" aria-busy=\"false\" data-probe=\"button-modern-md\"><span data-part=\"content\" data-state=\"visible\"><span data-part=\"label\">button</span></span></button>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='button-modern-md']",
          "properties": [
            "border-top-left-radius",
            "background-color",
            "color",
            "border-top-color",
            "border-top-width",
            "box-shadow",
            "font-family",
            "font-size",
            "font-weight"
          ]
        }
      ]
    },
    {
      "id": "input-modern-md",
      "why": "The modern Input as the engine actually renders it, inside the field wrapper it always ships with — the root IS the input element, not a container. Carried `.ds-input` until 2026-08-11 for the same reason the button did, and was wrong the same way. A distinct chrome family for the colour channels, and a third radius chain: border-radius <- --ds-input-resolved-radius <- the size rule's precedence between --ds-input-md-radius and --ds-radius-md.",
      "requiresSelectors": [
        ".rottay-input.rottay-input--modern {",
        "border-radius: var(--ds-input-resolved-radius)",
        ".rottay-input.rottay-input--modern[data-size='md']:not([data-size-responsive])",
        "--ds-input-resolved-radius: var("
      ],
      "html": "<div class=\"rottay-input-field\" data-part=\"field\"><input id=\"probe-input-modern-md\" type=\"text\" class=\"rottay-input rottay-input--modern\" data-part=\"root\" data-variant=\"outline\" data-size=\"md\" data-filled=\"true\" value=\"input\" data-probe=\"input-modern-md\" /></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='input-modern-md']",
          "properties": [
            "border-top-left-radius",
            "background-color",
            "color",
            "border-top-color",
            "border-top-width",
            "font-family",
            "font-size"
          ]
        }
      ]
    },
    {
      "id": "skeleton-card-line",
      "synthetic": true,
      "why": "THE POSITIVE CONTROL. This rule reads the dial DIRECTLY -- `calc(4px * var(--ds-radius-scale, 1))` -- with no derived token in between. If this element does not move when the dial turns, the harness itself is broken and every 'inert' verdict in the same run is void.",
      "requiresSelectors": [
        ".rottay-skeleton-card [data-part='line']",
        "calc(4px * var(--ds-radius-scale, 1))"
      ],
      "html": "<div class=\"rottay-skeleton-card\" data-part=\"root\" data-probe=\"skeleton-card\"><div data-part=\"body\"><div data-part=\"line\" data-probe=\"skeleton-card-line\">&nbsp;</div></div></div>",
      "targets": [
        {
          "id": "line",
          "selector": "[data-probe='skeleton-card-line']",
          "properties": ["border-top-left-radius", "background-color", "height"]
        }
      ]
    },
    {
      "id": "skeleton-form-action",
      "synthetic": true,
      "why": "Second direct dial consumer on a different literal -- `calc(6px * var(--ds-radius-scale, 1))`. Two controls, two literals: a single moving fixture cannot be a coincidence of one rule.",
      "requiresSelectors": [
        ".rottay-skeleton-form [data-part='action']",
        "calc(6px * var(--ds-radius-scale, 1))"
      ],
      "html": "<div class=\"rottay-skeleton-form\" data-part=\"root\" data-probe=\"skeleton-form\"><div data-part=\"action\" data-probe=\"skeleton-form-action\">&nbsp;</div></div>",
      "targets": [
        {
          "id": "action",
          "selector": "[data-probe='skeleton-form-action']",
          "properties": ["border-top-left-radius", "background-color", "height"]
        }
      ]
    },
    {
      "id": "token-readout",
      "synthetic": true,
      "why": "Not a component: a bare element inside the tenant scope whose custom properties are read directly. It answers 'what value does this token RESOLVE to for this tenant' without an intervening paint rule, which separates 'the token is wrong' from 'the component does not read it'.",
      "requiresSelectors": ["--ds-radius-md: calc(var(--ds-radius-md-base) * var(--ds-radius-scale, 1))"],
      "html": "<div data-probe=\"token-readout\"></div>",
      "targets": [
        {
          "id": "tokens",
          "selector": "[data-probe='token-readout']",
          "properties": [
            "--ds-radius-scale",
            "--ds-radius-sm",
            "--ds-radius-md",
            "--ds-radius-lg",
            "--ds-radius-xl",
            "--ds-radius-sm-base",
            "--ds-radius-md-base",
            "--ds-radius-lg-base",
            "--ds-radius-xl-base",
            "--ds-card-border-radius",
            "--ds-card-radius",
            "--ds-button-md-border-radius",
            "--ds-button-md-radius",
            "--ds-input-md-border-radius",
            "--ds-text-eyebrow-letter-spacing",
            "--ds-text-eyebrow-transform",
            "--ds-text-eyebrow-size",
            "--ds-table-header-letter-spacing",
            "--ds-table-header-text-transform",
            "--ds-letter-spacing-body",
            "--ds-type-scale",
            "--ds-font-size-xs"
          ]
        }
      ]
    }
  ]
}
