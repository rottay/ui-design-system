{
  "$schema": "./schema.md",
  "note": "A fixture that names a real component takes its element shape FROM THAT COMPONENT — rendered, then pasted — never from the CSS. Deriving the shape from the CSS is what let `button-modern-md` and `input-modern-md` sit on `.ds-btn`/`.ds-input` for months: a selector states what an element must carry to be MATCHED, and says nothing about whether any component emits it, so a CSS-derived fixture can measure a rule that never paints and still look green. Regenerate a component shape by rendering the engine implementation with `renderToStaticMarkup` and pasting the result, minus React-generated ids, plus `data-probe`. The synthetic fixtures (the two skeleton controls and the token readout) are exempt: they exist to read a dial, not to stand for a component. `requiresSelectors` is not documentation either: the harness asserts each string occurs verbatim in the CSS it is about to measure and marks the fixture `unmatched` if it does not, so a fixture that stops being matched is reported, never silently measured as zero. Pin selectors and the paint statement, never a declaration's VALUE — a precedence ruling would then read as a broken fixture.",
  "fixtures": [
    {
      "id": "card-modern-md",
      "why": "The canonical derived-radius consumer. Chain: border-radius <- --ds-card-radius <- --ds-card-border-radius <- --ds-radius-lg <- calc(--ds-radius-lg-base * --ds-radius-scale). Four hops from the tenant dial to a painted pixel. It is ALSO the decoration-intensity consumer: this fixture is data-variant='elevated', and the elevated rule is the one that composes --ds-effect-intensity into two painted properties -- box-shadow's inset optical keyline (calc(72% * EI) inside a color-mix) and background-image's surface tint (--ds-card-surface-gradient <- --ds-gradient-surface, whose three rgba alphas are each calc(k * EI)). background-image was added 2026-08-23 because the tint is a second, independent painted witness of the same channel and measuring only the keyline would have reported a tenant that flattens one but not the other as fully flattened. A real header/title was added 2026-08-23 for experience.profile: --ds-letter-spacing-heading is the one declared channel of that control with an unambiguous, non-radius painted chain, and it only exists on [data-part='title']. The title is rendered by the REAL ModernCard (title=\"Card title\"), so the roster drift test covers it exactly like the root; the markup here is renderToStaticMarkup output with data-probe re-added, not hand-derived.",
      "requiresSelectors": [
        ".ds-card.ds-card--modern[data-part='root'][data-radius='md']",
        "--ds-card-border-radius: var(--ds-radius-lg)",
        "inset 0 1px 0 color-mix(in srgb, var(--ds-color-bg-elevated) calc(72% * var(--ds-effect-intensity)), transparent)",
        "letter-spacing: var(--ds-card-title-letter-spacing, var(--ds-letter-spacing-heading))"
      ],
      "html": "<div class=\"ds-card ds-card--elevated ds-card--modern\" data-variant=\"elevated\" data-radius=\"md\" data-size=\"md\" data-padding=\"md\" data-has-header=\"true\" data-part=\"root\" data-probe=\"card-modern-md\"><div data-part=\"body\"><div data-part=\"header\"><div data-part=\"header-main\"><h3 data-part=\"title\">Card title</h3></div></div>card body</div></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='card-modern-md']",
          "properties": [
            "border-top-left-radius",
            "border-bottom-right-radius",
            "background-color",
            "color",
            "border-top-color",
            "border-top-width",
            "box-shadow",
            "background-image",
            "padding-top",
            "font-family",
            "font-size"
          ]
        },
        {
          "id": "title",
          "selector": "[data-probe='card-modern-md'] [data-part='title']",
          "properties": [
            "letter-spacing",
            "font-family",
            "font-size",
            "color",
            "background-color",
            "border-top-style"
          ]
        }
      ]
    },
    {
      "id": "button-modern-md",
      "why": "The modern Button as the engine actually renders it. Until 2026-08-11 this fixture carried `.ds-btn`, a RUSTIC theme rule no component can produce (both engines render `rottay-button--{modern,rustic}`), so every reading attributed to 'the modern button' measured a rule that never paints. The token defect it exposed happened to be real, which is the dangerous case: a lying instrument that returns a true finding teaches you to trust it. Chain: border-radius <- --_ds-button-resolved-radius <- the size rule's precedence between --ds-button-md-radius and --ds-radius-md.",
      "requiresSelectors": [
        ".rottay-button.rottay-button--modern[data-variant]",
        "border-radius: var(--_ds-button-resolved-radius)",
        ".rottay-button.rottay-button--modern[data-size='md']:not([data-size-responsive])",
        "--_ds-button-resolved-radius: var("
      ],
      "html": "<button data-recipe=\"feedback.press\" data-recipe-state=\"final\" data-variant=\"primary\" data-size=\"md\" data-shape=\"default\" data-part=\"trigger\" type=\"button\" class=\"rottay-button rottay-button--modern rottay-button--primary rottay-button--md rottay-button--default\" style=\"--ds-recipe-enter:0ms;--ds-recipe-exit:0ms;--ds-recipe-settle:0ms;--ds-recipe-cycle:0ms;--ds-recipe-x:0px;--ds-recipe-y:0px;--ds-recipe-scale-from:1;--ds-recipe-curve:var(--ds-motion-ease-out)\" aria-disabled=\"false\" aria-busy=\"false\" data-probe=\"button-modern-md\"><span data-part=\"content\" data-state=\"visible\"><span data-part=\"label\">button</span></span></button>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='button-modern-md']",
          "properties": [
            "border-top-left-radius",
            "background-color",
            "color",
            "border-top-color",
            "border-top-width",
            "box-shadow",
            "font-family",
            "font-size",
            "font-weight"
          ]
        },
        {
          "id": "hitbox",
          "why": "The button root IS its own hit target -- no separate expanded touch-area wrapper exists in the modern engine's markup, so this target reads the same element as `root` under a name that says what it is being asked for: real sizing geometry rather than paint. `@rect-inline-size`/`@rect-block-size` are read from getBoundingClientRect, so a padding/border/transform mutation that changes what actually renders cannot hide behind an unmoved computed longhand.",
          "selector": "[data-probe='button-modern-md']",
          "properties": [
            "block-size",
            "height",
            "min-block-size",
            "min-height",
            "max-block-size",
            "max-height",
            "inline-size",
            "width",
            "min-inline-size",
            "max-inline-size",
            "min-width",
            "max-width",
            "aspect-ratio",
            "scale",
            "zoom",
            "contain-intrinsic-size",
            "@rect-inline-size",
            "@rect-block-size"
          ]
        }
      ]
    },
    {
      "id": "input-modern-md",
      "why": "The modern Input as the engine actually renders it, inside the field wrapper it always ships with — the root IS the input element, not a container. Carried `.ds-input` until 2026-08-11 for the same reason the button did, and was wrong the same way. A distinct chrome family for the colour channels, and a third radius chain: border-radius <- --ds-input-resolved-radius <- the size rule's precedence between --ds-input-md-radius and --ds-radius-md.",
      "requiresSelectors": [
        ".rottay-input.rottay-input--modern {",
        "border-radius: var(--ds-input-resolved-radius)",
        ".rottay-input.rottay-input--modern[data-size='md']:not([data-size-responsive])",
        "--ds-input-resolved-radius: var("
      ],
      "html": "<div class=\"rottay-input-field\" data-part=\"field\"><input id=\"probe-input-modern-md\" type=\"text\" class=\"rottay-input rottay-input--modern\" data-part=\"root\" data-variant=\"outline\" data-size=\"md\" data-filled=\"true\" value=\"input\" data-probe=\"input-modern-md\" /></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='input-modern-md']",
          "properties": [
            "border-top-left-radius",
            "background-color",
            "color",
            "border-top-color",
            "border-top-width",
            "font-family",
            "font-size"
          ]
        }
      ]
    },
    {
      "id": "skeleton-card-line",
      "synthetic": true,
      "why": "THE POSITIVE CONTROL. This rule reads the dial DIRECTLY -- `calc(4px * var(--ds-radius-scale, 1))` -- with no derived token in between. If this element does not move when the dial turns, the harness itself is broken and every 'inert' verdict in the same run is void.",
      "requiresSelectors": [
        ".rottay-skeleton-card [data-part='line']",
        "calc(4px * var(--ds-radius-scale, 1))"
      ],
      "html": "<div class=\"rottay-skeleton-card\" data-part=\"root\" data-probe=\"skeleton-card\"><div data-part=\"body\"><div data-part=\"line\" data-probe=\"skeleton-card-line\">&nbsp;</div></div></div>",
      "targets": [
        {
          "id": "line",
          "selector": "[data-probe='skeleton-card-line']",
          "properties": [
            "border-top-left-radius",
            "background-color",
            "height"
          ]
        }
      ]
    },
    {
      "id": "skeleton-form-action",
      "synthetic": true,
      "why": "Second direct dial consumer on a different literal -- `calc(6px * var(--ds-radius-scale, 1))`. Two controls, two literals: a single moving fixture cannot be a coincidence of one rule.",
      "requiresSelectors": [
        ".rottay-skeleton-form [data-part='action']",
        "calc(6px * var(--ds-radius-scale, 1))"
      ],
      "html": "<div class=\"rottay-skeleton-form\" data-part=\"root\" data-probe=\"skeleton-form\"><div data-part=\"action\" data-probe=\"skeleton-form-action\">&nbsp;</div></div>",
      "targets": [
        {
          "id": "action",
          "selector": "[data-probe='skeleton-form-action']",
          "properties": [
            "border-top-left-radius",
            "background-color",
            "height"
          ]
        }
      ]
    },
    {
      "id": "token-readout",
      "synthetic": true,
      "why": "Not a component: a bare element inside the tenant scope whose custom properties are read directly. It answers 'what value does this token RESOLVE to for this tenant' without an intervening paint rule, which separates 'the token is wrong' from 'the component does not read it'. Extended 2026-08-11 with the rhythm pair -- --ds-rhythm-scale is the raw tenant input (enum stop or override), --ds-rhythm-effective-scale is the clamped 0.8..1.25 channel every consume site reads -- so a causal rhythm run has a direct-read control fixture on the SAME element as the radius readout, rather than needing a second synthetic fixture for a second control. Extended 2026-08-23 with --ds-effect-intensity, the single declared channel of surfaces.effect-intensity. It is a REGISTERED @property (syntax '<number>', initial-value 1), which is exactly why a direct read matters here: an invalid lowering is replaced by the initial value rather than dropped, so a broken ingress arm and a genuinely unreachable channel both paint the baseline. Reading the channel itself separates them. Extended 2026-08-23 with the five channels experience.profile declares that this readout did not already carry -- directControlFixtureIds requires ONE target to read EVERY declared channel, so a partial read gives the control no direct-read fixture and every causal run resolves harness-suspect. border-top-style is a HYDRATION WITNESS, not a channel: `color remains fixed` is every-measured-target, so it injects background-color into every target's plan, and on a bare div that longhand is legitimately at its initial value - which made the unhydrated guard fire on an element whose custom properties demonstrably moved. The DS base layer sets border-top-style to `solid` on every element, so it is decidable, non-initial, and not a channel any control can move. Extended 2026-08-23 with the density chain, for density.mode. The control declares TWO channels (--ds-density-mode-factor, --ds-density-scale) and directControlFixtureIds requires ONE target to read EVERY declared channel, so both are read here or the control has no direct-read fixture at all. The other two are what make the reading ATTRIBUTABLE rather than merely present: --ds-density-global-effective-scale is the tenant-level composition (scale x mode-factor, clamped 0.5..3) and --ds-density-effective-scale is the channel 71 real consumers read (global x local, clamped again). Without the intermediate, a reader cannot separate a tenant composition from a local posture. --ds-density-local-factor is deliberately NOT read: it is a component-posture channel never written at tenant scope, so on a bare div it would contribute an absent row and feed the unhydrated/absent-measurement guards rather than any claim. The reason this matters more here than for the other controls: --ds-density-scale is emitted UNCONDITIONALLY by the compiler (brand-theme/index.ts, the vars seed), so a lowering can be non-empty while encoding no stop -- exactly the false-INERT vector shape.radius-scale reported and left open. Reading the mode-factor DIRECTLY is what closes it. Extended 2026-08-24 with --ds-radius-button, the single declared channel of shape.button-style. Not the same channel `--ds-button-md-radius`/`--ds-button-md-border-radius` already read here: those are what button.css actually paints through (`var(--ds-button-md-radius, var(--ds-radius-md))`), and the buttonStyle posture branch (appearance-posture:145-154) writes only --ds-radius-button, never the per-size siblings. The two channels are independent declarations with independent producers (borders.css:27 seeds --ds-radius-button unconditionally to var(--ds-radius-md); nothing seeds --ds-button-md-radius), so a direct read of --ds-radius-button is the only way to attribute a stop to the compiler's own dial output rather than inferring it from a paint chain the dial does not reach. Extended 2026-08-24 with the three declared channels of surfaces.elevation-posture (--ds-elevation-1/2/3). directControlFixtureIds requires ONE target to read EVERY declared channel, so a partial read (only 1 of 3) gives the control no direct-read fixture at all. `soft`, the closed-domain default, is the EMPTY posture preset (appearance-posture.ts's ELEVATION_PRESET.soft = {}) -- absent an authored override the three channels resolve to the DS foundation seed (default.css:807 for level 1, siblings for 2/3), so the baseline and the `soft` stop are the SAME value by construction, same shape as shape.button-style's `soft` stop. A real, painted consumer already exists two hops downstream (card.css:618, `box-shadow: var(--ds-card-shadow, var(--ds-elevation-1))`), but a direct read here is still what attributes a stop to the compiler's OWN posture branch rather than to whatever a card happens to paint.",
      "requiresSelectors": [
        "--ds-radius-md: calc(var(--ds-radius-md-base) * var(--ds-radius-scale, 1))",
        "--ds-rhythm-scale: 1",
        "--ds-rhythm-effective-scale: clamp(0.8, var(--ds-rhythm-scale, 1), 1.25)",
        "@property --ds-effect-intensity",
        "--ds-density-global-effective-scale: clamp(",
        "--ds-density-effective-scale: clamp(",
        "--ds-radius-button: var(--ds-radius-md)",
        "--ds-elevation-1: 0 1px 2px"
      ],
      "html": "<div data-probe=\"token-readout\"></div>",
      "targets": [
        {
          "id": "tokens",
          "selector": "[data-probe='token-readout']",
          "properties": [
            "--ds-radius-scale",
            "--ds-radius-sm",
            "--ds-radius-md",
            "--ds-radius-lg",
            "--ds-radius-xl",
            "--ds-radius-sm-base",
            "--ds-radius-md-base",
            "--ds-radius-lg-base",
            "--ds-radius-xl-base",
            "--ds-card-border-radius",
            "--ds-card-radius",
            "--ds-button-md-border-radius",
            "--ds-button-md-radius",
            "--ds-input-md-border-radius",
            "--ds-text-eyebrow-letter-spacing",
            "--ds-text-eyebrow-transform",
            "--ds-text-eyebrow-size",
            "--ds-table-header-letter-spacing",
            "--ds-table-header-text-transform",
            "--ds-letter-spacing-body",
            "--ds-type-scale",
            "--ds-font-size-xs",
            "--ds-rhythm-scale",
            "--ds-rhythm-effective-scale",
            "--ds-effect-intensity",
            "--ds-experience-profile",
            "--ds-letter-spacing-heading",
            "--ds-edge-standard-width",
            "--ds-material-canvas-texture",
            "--ds-elevation-lift-strength",
            "border-top-style",
            "--ds-density-mode-factor",
            "--ds-density-scale",
            "--ds-density-global-effective-scale",
            "--ds-density-effective-scale",
            "--ds-radius-button",
            "--ds-elevation-1",
            "--ds-elevation-2",
            "--ds-elevation-3",
            "--ds-color-primary",
            "--ds-color-primary-500",
            "--ds-button-primary-bg",
            "--ds-chart-series-1",
            "--ds-color-text-on-primary"
          ]
        }
      ]
    },
    {
      "id": "flex-modern-preset-gap",
      "why": "The modern Flex as the engine actually renders it, with a preset ('md') uniform gap. Chain: gap <- calc(--ds-flex-gap * --ds-rhythm-effective-scale). One hop: the scaled rule multiplies the md rung directly by the tenant rhythm dial in the gap declaration itself, held at the base rule's specificity by `:where()` so source order -- not escalation -- decides. Proves the rhythm dial reaches Flex's layout gap the way the six fixtures above prove the radius dial reaches radius.",
      "requiresSelectors": [
        ".rottay-flex[data-gap=\"uniform\"]:where(",
        "[data-gap-preset=\"md\"]",
        "gap: calc(var(--ds-flex-gap) * var(--ds-rhythm-effective-scale, 1))"
      ],
      "html": "<div class=\"rottay-flex rottay-flex--modern\" style=\"min-inline-size:0;--ds-flex-gap:var(--ds-spacing-4, 1rem)\" data-gap=\"uniform\" data-gap-preset=\"md\" data-component=\"flex\" data-probe=\"flex-modern-preset-gap\"><span>a</span><span>b</span></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='flex-modern-preset-gap']",
          "properties": [
            "gap",
            "column-gap",
            "row-gap"
          ]
        }
      ]
    },
    {
      "id": "grid-modern-preset-gap",
      "why": "The modern Grid as the engine actually renders it, with a preset ('md') gap routed through --ds-grid-gap rather than the inline gap a numeric measurement would use. Chain: gap <- calc(--ds-grid-gap * --ds-rhythm-effective-scale). One hop, anchored on the engine class (rottay-grid--modern) rather than a repeated attribute, so the scaled rule outranks the unscaled base rule by source order alone -- the same law Flex's `:where()` enforces by specificity parity instead.",
      "requiresSelectors": [
        ".rottay-grid.rottay-grid--modern:where(",
        "[data-gap-preset=\"md\"]",
        "gap: calc(var(--ds-grid-gap) * var(--ds-rhythm-effective-scale, 1))"
      ],
      "html": "<div class=\"rottay-grid rottay-grid--modern\" style=\"display:grid;min-inline-size:0;grid-template-columns:repeat(2, minmax(0, 1fr));--ds-grid-gap:var(--ds-spacing-4, 1rem)\" data-component=\"grid\" data-gap-preset=\"md\" data-probe=\"grid-modern-preset-gap\"><div>a</div><div>b</div></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='grid-modern-preset-gap']",
          "properties": [
            "gap",
            "column-gap",
            "row-gap"
          ]
        }
      ]
    },
    {
      "id": "stack-modern-preset-gap",
      "why": "The modern Stack as the engine actually renders it, with a preset ('md') spacing rung -- the one layout primitive where rhythm reaches the paint with no inline value at all: the data-spacing attribute alone selects the scaled rule. Chain: gap <- var(--_ds-stack-gap-current) <- calc(--ds-spacing-4 * --ds-rhythm-effective-scale). Two hops, one more than Flex/Grid/Space, because the rung is mirrored into a private channel so the divider can center itself inside the same governed rhythm instead of adding its own separation on top of it.",
      "requiresSelectors": [
        ".rottay-stack[data-spacing=\"md\"]",
        "--_ds-stack-gap-current: calc(var(--ds-spacing-4) * var(--ds-rhythm-effective-scale, 1))",
        "gap: var(--_ds-stack-gap-current)"
      ],
      "html": "<div class=\"rottay-stack rottay-stack--modern\" style=\"min-inline-size:0\" data-spacing=\"md\" data-component=\"stack\" data-probe=\"stack-modern-preset-gap\"><div>a</div><div>b</div></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='stack-modern-preset-gap']",
          "properties": [
            "gap",
            "column-gap",
            "row-gap"
          ]
        }
      ]
    },
    {
      "id": "space-modern-preset-gap",
      "why": "The modern Space as the engine actually renders it, with a preset ('md') gap. Chain: gap <- calc(--ds-space-instance-gap * --ds-density-effective-scale * --ds-rhythm-effective-scale). One hop, but the only one of the four where rhythm compounds with density in the same declaration -- density sizes the md token, rhythm sizes the room around it -- held at the base rule's specificity by `:where()` so source order decides, exactly as Flex's split-gap rules do.",
      "requiresSelectors": [
        ".rottay-space.rottay-space--modern[data-part=\"root\"]:where(",
        "[data-size=\"md\"]",
        "gap: calc(var(--ds-space-instance-gap, var(--ds-space-small-size, var(--ds-spacing-2))) * var(--ds-density-effective-scale) * var(--ds-rhythm-effective-scale, 1))"
      ],
      "html": "<div class=\"rottay-space rottay-space--modern\" style=\"--ds-space-instance-gap:var(--ds-space-middle-size)\" data-part=\"root\" data-direction=\"horizontal\" data-align=\"center\" data-size=\"md\" data-with-split=\"false\" data-component=\"space\" data-probe=\"space-modern-preset-gap\"><span>a</span><span>b</span></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='space-modern-preset-gap']",
          "properties": [
            "gap",
            "column-gap",
            "row-gap"
          ]
        }
      ]
    },
    {
      "id": "flex-modern-numeric-gap",
      "why": "The negative half of flex-modern-preset-gap: the modern Flex with a NUMERIC gap (8), not a preset rung. `resolveFlexAttributes`/`flexGapPresetSpelling` stamp NO data-gap-preset for a number (the function returns undefined for typeof 'number'), so the rhythm-scaled rule cannot match at all -- only the unscaled base rule `.rottay-flex[data-gap='uniform'] { gap: var(--ds-flex-gap); }` reaches it. Proves numeric-instance-gaps-exact instead of asserting it in prose.",
      "requiresSelectors": [
        ".rottay-flex[data-gap=\"uniform\"] {",
        "gap: var(--ds-flex-gap)",
        "gap: calc(var(--ds-flex-gap) * var(--ds-rhythm-effective-scale, 1))"
      ],
      "html": "<div class=\"rottay-flex rottay-flex--modern\" style=\"min-inline-size:0;--ds-flex-gap:8px\" data-gap=\"uniform\" data-component=\"flex\" data-probe=\"flex-modern-numeric-gap\"><span>a</span><span>b</span></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='flex-modern-numeric-gap']",
          "properties": [
            "gap",
            "column-gap",
            "row-gap"
          ]
        }
      ]
    },
    {
      "id": "grid-modern-numeric-gap",
      "why": "The negative half of grid-modern-preset-gap: the modern Grid with a NUMERIC gap (8), not a preset rung. A numeric grid gap never reaches --ds-grid-gap or a data-gap-preset attribute at all -- `buildGridStyles` writes it as a plain inline `gap` CSS property, the strongest position in the cascade, so no stylesheet rule (scaled or not) can reach it. Proves numeric-instance-gaps-exact instead of asserting it in prose.",
      "requiresSelectors": [
        ".rottay-grid.rottay-grid--modern:where(",
        "gap: calc(var(--ds-grid-gap) * var(--ds-rhythm-effective-scale, 1))"
      ],
      "html": "<div class=\"rottay-grid rottay-grid--modern\" style=\"display:grid;min-inline-size:0;grid-template-columns:repeat(2, minmax(0, 1fr));gap:8px\" data-component=\"grid\" data-probe=\"grid-modern-numeric-gap\"><div>a</div><div>b</div></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='grid-modern-numeric-gap']",
          "properties": [
            "gap",
            "column-gap",
            "row-gap"
          ]
        }
      ]
    },
    {
      "id": "stack-modern-numeric-gap",
      "why": "The negative half of stack-modern-preset-gap: the modern Stack with a NUMERIC gap (8), which stamps `data-spacing='custom'` rather than a named rung. `.rottay-stack[data-spacing='custom']` has specificity (0,2,0) and carries no `:where(.rottay-stack--modern)` scaled companion, so it outranks the unscoped modern-default override at (0,1,0) and reads the caller's own --ds-stack-gap channel with no rhythm multiplication anywhere in the chain. Proves numeric-instance-gaps-exact instead of asserting it in prose.",
      "requiresSelectors": [
        ".rottay-stack[data-spacing=\"custom\"]",
        "--_ds-stack-gap-current: var(--ds-stack-gap)",
        "--_ds-stack-gap-current: calc(var(--ds-spacing-4) * var(--ds-rhythm-effective-scale, 1))"
      ],
      "html": "<div class=\"rottay-stack rottay-stack--modern\" style=\"min-inline-size:0;--ds-stack-gap:8px\" data-spacing=\"custom\" data-component=\"stack\" data-probe=\"stack-modern-numeric-gap\"><div>a</div><div>b</div></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='stack-modern-numeric-gap']",
          "properties": [
            "gap",
            "column-gap",
            "row-gap"
          ]
        }
      ]
    },
    {
      "id": "space-modern-numeric-gap",
      "why": "The negative half of space-modern-preset-gap: the modern Space with a NUMERIC size (8), not a preset spelling. The bare number lands in `data-size` unmatched by the enumerated `:where([data-size='small'], ..., [data-size='lg'])` scaled-rule list, so only the unscaled root rule `gap: var(--ds-space-instance-gap, ...)` reaches it. Proves numeric-instance-gaps-exact instead of asserting it in prose.",
      "requiresSelectors": [
        ".rottay-space.rottay-space--modern[data-part=\"root\"] {",
        "gap: var(--ds-space-instance-gap, var(--ds-space-small-size, var(--ds-spacing-2)))",
        "gap: calc(var(--ds-space-instance-gap, var(--ds-space-small-size, var(--ds-spacing-2))) * var(--ds-density-effective-scale) * var(--ds-rhythm-effective-scale, 1))"
      ],
      "html": "<div class=\"rottay-space rottay-space--modern\" style=\"--ds-space-instance-gap:8px\" data-part=\"root\" data-direction=\"horizontal\" data-align=\"center\" data-size=\"8\" data-with-split=\"false\" data-component=\"space\" data-probe=\"space-modern-numeric-gap\"><span>a</span><span>b</span></div>",
      "targets": [
        {
          "id": "root",
          "selector": "[data-probe='space-modern-numeric-gap']",
          "properties": [
            "gap",
            "column-gap",
            "row-gap"
          ]
        }
      ]
    }
  ]
}
