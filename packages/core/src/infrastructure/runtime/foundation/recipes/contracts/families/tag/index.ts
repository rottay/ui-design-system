/**
 * Authored tag recipe definition (DS-S001).
 *
 * The modern Tag shell classes. Variant/size/radius stay on the `data-*` skin
 * contract; profile-tunable axes arrive with DS-R00x.
 */

export const TAG_RECIPE_DEFINITION = {
  name: 'tag',
  slots: { root: ['rottay-tag-shell', 'rottay-tag-shell--modern'] },
  axes: {},
  defaults: {},
};
