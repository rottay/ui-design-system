/**
 * @deprecated Compatibility re-export. Canonical source: `runtime/tenant/presets/`.
 * Import from `@rottay/design-system` instead.
 */
export { resolvePersonalityPreset } from '../../runtime/tenant/presets';
export type { PersonalityPreset } from '../../runtime/tenant/presets';
