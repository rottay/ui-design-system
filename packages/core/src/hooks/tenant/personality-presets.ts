/**
 * @deprecated Compatibility re-export. Canonical source: `runtime/tenancy/personality-presets`.
 * Import from `@rottay/design-system` instead.
 */
export { resolvePersonalityPreset } from '../../runtime/tenancy/personality-presets';
export type { PersonalityPreset } from '../../runtime/tenancy/personality-presets';
