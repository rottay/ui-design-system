/**
 * Public tokens entrypoint.
 *
 * Keeping a dedicated top-level entry lets the package expose
 * `@rottay/design-system/tokens` with a real runtime module instead of a
 * documentation-only path.
 */

export * from './tokens/index';
