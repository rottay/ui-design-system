{
  "schemaVersion": 1,
  "controlId": "palette.seeds",
  "lifecycleState": "OPERATIONAL",
  "tier": "standard",
  "scope": "tenant",
  "semanticOwner": {
    "owner": "design-system",
    "registryPath": "packages/core/src/foundation/contracts/composition/tenants/capabilities/index.ts",
    "registryDigest": "88622b9370257a8793f8bfdadfcf408edef6b12289afb9deea7c0425a7328301"
  },
  "title": "Brand palette seeds",
  "valueType": "color-set",
  "domain": {
    "kind": "color-set",
    "enumValues": [],
    "bounds": null,
    "defaultBehavior": "vertical baseline palette"
  },
  "ingress": {
    "staticBrandThemePath": "palette.{primaryColor,secondaryColor,accentColor,backgroundColor}",
    "dbTenantThemePath": "appearance.general.palette.{primary,secondary,accent,background}"
  },
  "declaredOutputs": {
    "channels": [
      "--ds-color-primary",
      "--ds-color-primary-500",
      "--ds-button-primary-bg",
      "--ds-chart-series-1",
      "--ds-color-text-on-primary"
    ],
    "rootAttributes": [],
    "representativeOnly": true
  },
  "dependsOn": [],
  "compatibility": "additive, unset-to-rollback",
  "productiveConsumerWitness": {
    "consumer": "src/foundation/tokens/css/runtime/engines/modern/skin/button.css",
    "symbol": "var(--ds-button-primary-bg"
  },
  "calibration": {
    "assessmentState": "UNKNOWN",
    "normalizedStops": [
      {
        "id": "primary/crimson",
        "role": "primary",
        "value": "#DC2626"
      },
      {
        "id": "primary/indigo",
        "role": "primary",
        "value": "#4F46E5"
      },
      {
        "id": "primary/identity",
        "role": "primary",
        "identity": true
      }
    ],
    "staticDbParityEvidenceIds": [],
    "exactRestoreEvidenceIds": [],
    "negativeControls": [
      "font metrics remain fixed",
      "border width and style remain fixed",
      "motion remains fixed"
    ],
    "nextAction": "ROTTAY RULING (DT). B-2 is done and closed the last measured blocker: the static arm declares the patch's authorship, bithire and evnto are equivalent on both stops with 0 divergent rows, and no receipted scenario moved. What remains is not an instrument packet and not a measurement -- it is the adjudication of ROTTAY_DB_DOOR_INERT_FOR_PALETTE: either an adjudicated calibration.stopDiscriminationException (with adjudicatedBy) that lets this control emit receipts while rottay's DB door stays inert, or a ruling that rottay's palette door is a product defect to be fixed before this control can close. Until one of the two lands, assessmentState stays UNKNOWN and the five B-2 runs stay MEASURED-NOT-RECEIPTED.",
    "sourceBindings": [
      "packages/core/src/foundation/contracts/composition/tenants/capabilities/index.ts",
      "packages/core/src/foundation/contracts/composition/tenants/themes/index.ts",
      "packages/core/src/foundation/kernel/color/oklch/ramp/index.ts",
      "packages/core/src/foundation/kernel/color/oklch/index.ts",
      "packages/core/src/infrastructure/compilers/kernel/runtime/brand-theme/index.ts",
      "packages/core/src/infrastructure/compilers/composition/tenant-theme/index.ts",
      "packages/core/src/foundation/tokens/css/runtime/engines/modern/skin/button.css"
    ],
    "assessmentStateNote": "STILL UNKNOWN, and the reason CHANGED in B-2. The instrument is now complete on all three halves: M-1 taught the static arm the mode scopes, H-3 phase (a) taught the DB arm, and B-2 taught the static arm to declare the authorship of the patch it lowers. Every divergent row is gone on both measurable verticals -- bithire 10 -> 3 (dark, H-3(a)) -> 0 (light, B-2), evnto 5 -> 0 -- across two stops each, with pass=true, restore exact and negatives held. WHAT BLOCKS THE CONTROL IS NOW ONLY ROTTAY, and it is a DT ruling rather than a measurement: rottay's DB door lowers ZERO of the declared channels (ROTTAY_DB_DOOR_INERT_FOR_PALETTE, OPEN_DT), so the harness refuses the run, and enlisting this control in the H-2 drill 'every control that carries receipts passes, both arms' would enlist a vertical it cannot pass. The escape the guard itself names is an adjudicated calibration.stopDiscriminationException carrying an adjudicatedBy -- a DT ruling, not a writer's to take. So no receipt is emitted in B-2 either, and the control waits for that one adjudication rather than closing on two verticals of three.",
    "evidenceKindLabellingRule": "NO RECEIPT IS EMITTED, and the reason is a gate rather than a doubt about the numbers. evnto IS equivalent on both stops (0 divergent rows, pass=true, restore exact, negatives held) and was receipted during this packet -- and emitting it enlisted palette.seeds in the H-2 drill \"every control that carries receipts passes, both arms\", which the control cannot pass on rottay: its DB arm lowers ZERO witnesses there, and discrimination is not decidable below two. The escape the guard names is an adjudicated calibration.stopDiscriminationException carrying an adjudicatedBy, which is a DT ruling and not a writer's to take. So the receipts were WITHDRAWN and the two evnto runs are persisted unreceipted at packages/core/test-artifacts/quality-evidence/wo-cra-23/F4B/palette-seeds/{evnto-crimson,evnto-indigo}.MEASURED-NOT-RECEIPTED.json. They are the evidence B-2 will cite.",
    "negativeControlsScopeNote": "Three, at every-measured-target scope. \"color remains fixed\" is deliberately absent: it is what this control MOVES. The three that remain — type, stroke, motion — are orthogonal to a colour seed and held in every scenario measured, on both arms.",
    "stopSemantics": "A STOP IS A (ROLE, HEX) PAIR: the mutation of ONE role, not of the palette. Measured: each of primaryColor / secondaryColor / accentColor moves exactly its own ten ramp steps and crosses ZERO into another role, which is what makes the per-role negative decidable with an exact zero instead of a tolerance. The alternatives were refuted by absence, not by preference: no preset palette catalog exists anywhere in src (and that shape already has its own domain kind, profile-id, with its calibration.catalog law), and no hue-rotation mechanism exists (the ramp holds hue constant by design). An IDENTITY stop declares no value and resolves against the arm's own baseline: it is the first identity in this programme that differs per vertical (#FFFFFF / #3A6FB0 / #171717), and it fails closed rather than inventing one.",
    "measuredResult": {
      "instrument": "packages/core/src/tooling/resolution-probe, Chromium, --bundle fresh, Node 22.17.0, plus direct compiles through the productive doors for step 0 and the identity law.",
      "step0Apca": "RUN FIRST, BEFORE ANY STOP WAS AUTHORED, and it disqualified an axis. Every background candidate THROWS TenantThemeValidationError on the DB arm in 3/3 verticals: #123456 and #0B0B0C leave --ds-color-text-primary at APCA Lc 0.0 against the new ground, #F7F7F5 leaves --ds-color-text-muted/-disabled below the floor (Lc 48.2 / 50.2 / 36.2). The ground is NOT independently writable: it needs a paired foreground, which is a different stop shape. The background role is therefore NOT declared as a stop. The three role seeds compile in 3/3.",
      "perRoleIsolation": "Measured on deriveTenantColorRamps over a fixed 7-seed palette: primaryColor, secondaryColor and accentColor each move exactly 10 channels — their own role's ten steps — and 0 channels of any other role. backgroundColor moves 63 of 70: nine of ten steps in ALL SEVEN roles, including success/warning/error/info, which this control does not seed. The ground is the ramp ground, not a fourth peer seed. Drilled (F4B-8 drills 5 and 6).",
      "identityLaw": "W-A, measured on both arms. STATIC: writing the vertical's own value at the role keypath moves ZERO channels — 3 roles x 3 verticals, nine cases, all zero. The identity law holds exactly. DB: it does NOT — writing the vertical's own colour still emits a non-empty delta (bithire 7 vars, evnto 5, the same count for all three roles, so it is colour-independent), and rottay/primary (#FFFFFF) even throws on APCA. The DB arm takes no baseline by law (H-1 V5), so an identity stop is a STATIC-ARM claim; the harness refuses it on the DB arm rather than inventing the vertical value there.",
      "invalidSeedIsSilent": "THE FALSE GREEN THIS CONTROL BRINGS, measured not theorised. A non-hex seed does not throw and does not produce NaN: hexToRgbFloat runs parseInt(value, 16), NaN >> 16 & 255 is 0 in JS, so the seed becomes BLACK and derives a plausible GREY ramp. rebeccapurple and rgb(59, 130, 246) — both valid CSS colours — collapse this way. The channel MOVES, so a probe asking \"did it move?\" certifies as live a control that discarded the tenant colour. The signature that betrays it: --ds-color-primary passes the raw string through, the ramp goes achromatic, and --ds-chart-series-1 DISAPPEARS (it is behind isHexColor; the ramp derivation is not). The color-set branch refuses non-hex for exactly this reason (drill 3).",
      "scenariosRun": "Six causal runs over two stops (primary/crimson #DC2626, primary/indigo #4F46E5) x three verticals, light and dark, full roster. rottay: REFUSED by the empty-lowering guard — the DB arm emits none of the declared channels. bithire: harness-live, restore exact on both arms, negatives held, and 10 divergent rows. evnto: same, 4 divergent rows. Both stops give identical divergence counts, so the divergence is a property of the axis, not of the colour.",
      "witnessMatrix": "Of the five declared channels, only --ds-color-primary and --ds-chart-series-1 move on all three verticals; --ds-color-primary-500 is inert on rottay (it authors all seven palette.ramps, which win over derivation); --ds-button-primary-bg and --ds-color-text-on-primary are inert on all three. --ds-color-primary is the only reliable direct witness, and structurally so: brand-theme:825 assigns it straight from the seed.",
      "b2StaticAuthorship": "B-2, MEASURED 2026-08-24 on the same instrument and the same two stops. The static arm now declares the authorship of the patch it lowers (tenantAuthoredPaths = the one keypath the stop wrote), so the governed seed-derivation ladder fires on BOTH arms. Result, five causal runs: bithire/crimson and bithire/indigo are EQUIVALENT (0 divergent rows, pass=true, restore exact, negatives held, static moved=7 db moved=7) -- the three LIGHT rows that CHROME_SEED_PROVENANCE_ASYMMETRY named are gone, and --ds-button-primary-bg now reads var(--ds-color-primary) on the static arm exactly as it always did on the DB arm, where it used to read bithire's baked #3A6FB0. evnto/crimson and evnto/indigo re-verify UNCHANGED (0 divergent rows, moved=5 on both arms): only --ds-button-primary-bg among the five declared channels can move under the flag, and evnto already authors the alias, so the arm's load is byte-identical to before B-2 -- the predicted no-op, confirmed rather than assumed. rottay/crimson still REFUSED by the empty-lowering guard, same message and same shape as before, so ROTTAY_DB_DOOR_INERT_FOR_PALETTE is untouched by this packet. Artifacts: test-artifacts/quality-evidence/wo-cra-23/F4B/palette-seeds/B2-{bithire,evnto}-{crimson,indigo}.MEASURED-NOT-RECEIPTED.json. THE BLAST RADIUS WAS DERIVED BEFORE THE RUN AND HELD: of every control the registry declares a static door for, exactly one member lands on a field the compiler consults (palette.seeds -> palette.primaryColor), so none of the 42 receipted CSS scenarios could move -- asserted executably by B-2 drill 6 against the contract source rather than restated here.",
      "b2IdentityRuling": "W-B, measured by Fable and re-measured in this packet. An IDENTITY stop does NOT declare authorship, and the ruling is load-bearing rather than cosmetic: with the flag, writing the vertical's OWN value at the role keypath moves 7 channels on rottay, 7 on bithire and 5 on evnto, and bithire's --ds-button-primary-bg leaves #3A6FB0 for var(--ds-color-primary) -- the identity stop would stop being an identity, because the seed derivation reads the CLAIM and not the VALUE. Without it, all three verticals move 0 channels against their own baseline, which is the identity law F4B-8 seated. Adjudicated by the DT 2026-08-24; written beside the branch that resolves identity in tooling/resolution-probe/runtime/ingress and fenced by B-2 drill 7."
    },
    "unmeasuredScope": [
      "ARM EQUIVALENCE IS NOT CLAIMED and no receipt is emitted. Ten divergent rows on bithire, four on evnto, reproducible across two stops.",
      "NO FAMILY CELL IS WRITTEN. primitive/inputs/button was the candidate; a cell would have to name evidence that does not exist.",
      "rottay is not measurable on this axis today: the DB arm emits nothing for a palette write, so there is no second arm to compare against.",
      "The background/ground axis is unmeasured: step 0 proved it is not independently writable through the DB door.",
      "The secondary and accent roles have no causal scenario. Their per-role isolation is drilled on the compiler, but the five declared channels are all primary-flavoured, so a secondary or accent stop emits none of them and cannot be a witness. Widening declaredOutputs is a REGISTRY change and was out of this packet.",
      "The correctness of the OKLCH derivation itself — perceptual evenness, no hue drift at step 700 — has no home in negative-control-shaped machinery and is not claimed."
    ],
    "knownDefects": [
      {
        "familyId": "MODE_BLOCK_SPECIFICITY_OUTRANKS_THE_STATIC_ARM",
        "finding": "MEASURED IN THE ARTIFACTS, not inferred. Each vertical artifact declares --ds-color-primary TWICE: at :is(html[data-tenant=X], :where([data-ds-root][data-vertical=X])) and again at that SAME selector plus [data-theme='dark'] (rottay: .light). bithire index.css:323 and :1371 are the two lines. The mode block carries one extra attribute, so it outranks the static arm's appended tenant block by SPECIFICITY — appending later does not help. The DB arm writes inline on the document element and is not outranked. Result: the static arm is structurally inert, in that mode, for every channel the artifact re-declares per mode. Accounts for all 7 dark rows on bithire and all 4 on evnto.",
        "disposition": "CLOSED",
        "classNote": "SUPERSEDED BY M-1, and the previous reading was WRONG rather than incomplete. It called this an instance of OPEN_VERTICAL_CASCADE_DEFECT owned by the vertical-artifact generator. M-1 measured the compilers and refuted that: the double declaration is the CORRECT OUTPUT of compileModeBlocks (a base block plus one block per authored non-default mode, under a one-attribute-stronger selector), and the two compilers AGREE in that mode — the tenant seed does not reach the overlay (bithire: base moves 22 channels, the dark block 0), and compileTenantThemeConfig even ADDS a dark delta re-asserting the vertical values (0 -> 29 vars) precisely to hold that line. The generator has no defect here. What was wrong was the INSTRUMENT: the static arm wrote only the base scope (fixed in M-1: it now emits 1+n blocks at the compiler's own themeModeSelector), and the DB arm still drops compiled modeDeltas and writes inline on the document element, where it outranks every mode block and produces a reading no production path produces. The remaining question — SHOULD a tenant seed move the non-default mode — is a PRODUCT adjudication, not a defect of this control.",
        "owner": "the DB arm of the instrument (fidelity decision, H-3 phase (a)) plus one product question; NOT the vertical-artifact generator",
        "supersededBy": "M-1 (modeBlocks in the probe)",
        "resolvedBy": "H-3 phase (a) — the DB arm serves the compiled artifact (base + one block per modeDelta, re-scoped)",
        "resolutionMeasured": "The dark half of this row is CLOSED, measured on both verticals: palette.seeds bithire went from 10 divergent rows to 3 (all seven dark rows gone; the three that remain are light and belong to the --ds-button-primary-bg class), and evnto went from 5 to 0 with pass=true. The experience.profile bithire dark residue that B-1 opened is gone too: 0 divergent rows, pass=true, measured in the same pass.",
        "predictedResidueThatDidNotMaterialise": "Two candidates were named before the run and NEITHER became a row, which is recorded because the prediction was written down: --ds-color-text-on-primary on evnto (the design measured the DB dark delta as silent for it where the static arm carries #131210) and --ds-material-canvas-texture on bithire (the preaudit W-C measured the DB arm silent in BOTH base and delta). One class — the DB arm entirely silent in base and delta for a channel — and it produced no divergence: a channel neither arm moves reads identically on both, so mutual silence IS agreement. It is named here so a future run that DOES surface it is recognised as this class rather than as something new."
      },
      {
        "familyId": "COMPILERS_DISAGREE_ON_BUTTON_PRIMARY_BG",
        "finding": "The two productive compilers emit DIFFERENT values for the same declared channel on bithire: the static arm lowers the baked chrome hex #3A6FB0 (bithire authors chrome.controls.buttonPrimary.bg), the DB arm lowers the alias var(--ds-color-primary). The alias tracks the seed; the baked hex does not. Accounts for the three bithire LIGHT rows (the channel itself plus the button background-color and border-top-color it paints). evnto has no light divergence because both arms emit the alias there.",
        "disposition": "SUPERSEDED",
        "owner": "the chrome lowering, not this control",
        "supersededBy": "CHROME_SEED_PROVENANCE_ASYMMETRY",
        "supersessionNote": "SUPERSEDED, and it was WRONG rather than incomplete. It said \"the two productive compilers emit different values for the same declared channel\". Measured: it is ONE compiler, one input, and the only difference is whether the caller declares the seed tenant-authored."
      },
      {
        "familyId": "ROTTAY_DB_DOOR_INERT_FOR_PALETTE",
        "finding": "The DB arm emits ZERO variables for every palette-seed write on rottay that compiles (#DC2626, #4F46E5, #3A6FB0 -> 0 vars; #22C55E and #A0A0A5 throw on APCA), while the same door emits a delta for density and typography on the same vertical. The inertia is palette-specific. The static arm meanwhile moves 12 channels there. The harness refuses the run rather than reporting it, which is correct: an arm that emits none of the declared channels is a finding, not an arm.",
        "disposition": "OPEN_DT",
        "supersedes": null,
        "designStageCorrection": "This REFUTES the F4B-8 design prediction that rottay would be a DESIGNED_NULL with both arms equally inert. Measured, the arms diverge there rather than agreeing at zero. Recorded as a correction because the design said it in writing.",
        "stillTrueUnderTheFixedInstrument": "RE-VERIFIED after M-1 and H-3 phase (a): the DB arm still emits none of the declared channels on rottay, and the harness still refuses the run rather than reporting it. The form is unchanged and still correct -- an arm that emits nothing is a finding, not an arm."
      },
      {
        "familyId": "HARNESS_ACCEPTS_HEX_ONLY_ON_THIS_AXIS",
        "finding": "The color-set branch refuses any non-hex stop value. That is a restriction OF THE HARNESS, not of the product: the DB compiler accepts functional colour syntax (tenant-theme-compiler.test.ts:1187). What the branch refuses to do is lower a value whose downstream meaning it cannot predict on both arms — measured, a non-hex seed goes silently grey through the ramp derivation while the isHexColor-guarded channels skip entirely.",
        "disposition": "ACCEPTED_HARNESS_RESTRICTION"
      },
      {
        "familyId": "CHROME_SEED_PROVENANCE_ASYMMETRY",
        "finding": "THE LAST THREE ROWS, and they are not a disagreement between compilers. On bithire in LIGHT, --ds-button-primary-bg reads #3A6FB0 on the static arm and var(--ds-color-primary) on the DB arm, which also moves the button background-color and border-top-color it paints. Measured cause: `applyTenantSeedDerivations` (kernel/runtime/brand-theme:1614) re-derives the primary family a TENANT seed owns, and it fires ONLY when `provenance.seedIsTenantAuthored`. bithire is the one vertical whose baseline bakes a literal colour at chrome.controls.buttonPrimary.bg (#3A6FB0; rottay and evnto author var(--ds-color-primary) and are therefore unaffected), and the rule replaces exactly that kind of value: \"a BASELINE_LEAF cannot beat a TENANT_DERIVED value\".",
        "refutedExplanations": "TWO were checked and BOTH are false. (a) A baseline divergence: the two transports do NOT receive different verticals -- the DB baseline is FIRST_PARTY_THEMES[verticalKey] = brandThemeToTheme(bithireBrandTheme), the same source the static arm composes over. (b) A compiler defect: it is the SAME lowering; `compileTheme` and `compileBrandTheme` live in one file and share `applyTenantSeedDerivations`.",
        "decisiveExperiment": "Same compiler, same theme, same patch, one variable changed: compileBrandTheme({brandTheme: bithire, tenantPatch: {palette:{primaryColor:\"#DC2626\"}}, tenantSlug:\"bithire\"}) emits --ds-button-primary-bg = #3A6FB0; adding tenantAuthoredPaths: Set([\"palette.primaryColor\"]) to the SAME call emits var(--ds-color-primary) -- byte-identical to the DB arm. The provenance flag alone explains the divergence.",
        "adjudication": "BRANCH (c): legitimate behaviour of two transports for two tenant classes, with the caveat that it is a PROBE FIDELITY GAP rather than a permanent product truth. The compiler contract says so in its own words: tenantAuthoredPaths is \"supplied only for the tenant leg of a DB compile; a baseline compile supplies nothing and therefore compiles exactly as it did before provenance existed\", and the rule's doc adds \"every static first-party compile takes this path\". So the DB arm is right, the static arm is right about a FIRST-PARTY compile, and the probe's static arm is modelling something in between: it lowers a TENANT patch (B-1) while compiling as a baseline.",
        "answerToOptionB": "YES -- under option B a tenant seed SHOULD move bithire's button, and the compiler already does exactly that on the transport that knows it is a tenant. The channel is a vertical chrome leaf, and `SEED_SHADOWING_FIELDS` names it: a TENANT leaf at that path would outrank the tenant's own seed, but bithire's #3A6FB0 is a BASELINE leaf and does not.",
        "remediation": "B-2 (instrument, own packet): the static arm supplies the authorship of the patch it lowers. It already holds the patch (`buildIngressInput().patch`); the compiler already accepts the set. NOT done here: it changes what every static-arm scenario measures, which is the same class of change M-1 and H-3 phase (a) each took a design, a preaudit and a two-part invariance to land.",
        "disposition": "CLOSED",
        "owner": "the instrument (static arm provenance), not the compiler and not the baselines",
        "resolvedBy": "B-2 -- the static arm declares the authorship of the patch it lowers",
        "resolutionMeasured": "CLOSED, measured on both bithire stops: 3 divergent rows -> 0, pass=true, restore exact, negatives held. The remediation this entry named is what landed, and it landed where the entry said it belonged -- in the instrument, with no compiler and no baseline touched.",
        "adjudicationClauseAged": "THE QUOTATION IN `adjudication` AGED, and B-2 corrected the source rather than the quote. It cited the compiler contract saying tenantAuthoredPaths is 'supplied only for the tenant leg of a DB compile'. That clause described the only producer that existed; the gate run before B-2 measured that baseline+patch is the ONLY composition production performs and that production never performs it WITHOUT a set, so an arm composing both floors silently was the defect, not the exception. The clause is amended at src/infrastructure/compilers/kernel/runtime/brand-theme/index.ts (compileTheme.tenantAuthoredPaths). The rest of the adjudication -- branch (c), the probe fidelity gap, and `answerToOptionB` -- stands unchanged and is what B-2 executed."
      }
    ]
  },
  "retiredAliases": []
}
