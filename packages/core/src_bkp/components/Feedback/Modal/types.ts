/**
 * Modal Component Types
 *
 * Re-exports unified types from central types directory.
 * These types are compatible across all engine implementations.
 */

export type {
  ModalProps,
  ModalBaseProps,
  ModalConfirmProps,
} from '../../../types/components/modal';
