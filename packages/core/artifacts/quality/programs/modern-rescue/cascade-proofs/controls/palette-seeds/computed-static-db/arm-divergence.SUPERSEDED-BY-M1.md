# Supersession note — arm-divergence.MEASURED-NOT-RECEIPTED.json (M-1, 2026-08-24)

This is a NEW file, additive. The original artifact
(`arm-divergence.MEASURED-NOT-RECEIPTED.json`) is immutable evidence and is
NOT edited, per the immutable-snapshot law (a future correction is a new
file, never an edit).

## What is superseded

The `divergenceClasses` reading recorded in the original artifact, which
classified the dark-scope diverging rows of `palette.seeds` (bithire 7,
evnto 4) as an instance of `OPEN_VERTICAL_CASCADE_DEFECT` owned by the
vertical-artifact generator.

## The superseding reading (M-1, verified by Fable 5 with own evidence)

- Both compilers AGREE in dark: the tenant's base seed does NOT enter the
  mode block (bithire: base moves 22 channels, dark block 0; the DB
  compiler ADDS `modeDeltas` of 0→29 vars precisely to hold that line).
  The dark double declaration in the vertical artifact is CORRECT
  `compileModeBlocks` output, not a generator defect.
- The dark divergences measured in F4B-8 were produced by the INSTRUMENT:
  the probe's DB arm discarded `modeDeltas` (`ingress/index.mjs:879` at the
  time) and wrote inline on the document element, beating every mode block
  and producing a reading no production path produces.
- The class is therefore re-adjudicated: owner = the probe's DB arm (H-3
  phase (a), its own packet) PLUS a product question for the owner (should
  a tenant seed move the non-default mode under option B? — today the
  compiler deliberately prevents it).

Carriers of this supersession: `manifest/controls/palette.seeds.json#
knownDefects[0]` (classNote + supersededBy + owner + nextAction),
`checkpoint/index.json#blockedOn`, and the M-1 seat of
`docs/ROADMAP-EJECUCION-2026-08-19.md`. The measured facts in the original
artifact (specificity, lines, selectors) remain true; what was refuted is
the attribution, not the fact.

## Addendum (H-3 phase (a), 2026-08-24)

The instrument half is now RESOLVED: the probe's DB arm serves
`modeDeltas` as re-scoped blocks (`composeArmCss`, shared with the static
arm, grammar borrowed from brand-theme and registered in
`themeModeSelectorSource`). The dark half of the divergence is closed:
palette.seeds bithire 10→3 (the 7 dark rows disappeared; the remaining 3
are the light chrome class `--ds-button-primary-bg`, owned by the chrome
lowering), evnto 5→0, and the experience.profile bithire dark residual
(R7) is at 0 rows. What remains open by design: whether a tenant seed
should move the non-default mode (product question for the owner), and
whether the provider preview can express mode deltas (deliberately
unmeasured; the inline path is preserved with its act in
`runtime/measure/index.mjs`).

## Addendum (B-2, 2026-08-24)

The LIGHT half is now closed too, and it closed where this note said it would:
in the instrument, with no compiler and no baseline touched.

`CHROME_SEED_PROVENANCE_ASYMMETRY` named the last three rows and named their
remedy — the static arm must supply the authorship of the patch it lowers. B-2
did exactly that: `lowerStop` hands `compileBrandTheme` a
`tenantAuthoredPaths` set holding the ONE keypath the stop wrote, named from
`buildIngressInput` rather than collected, so the governed seed-derivation
ladder fires on both arms.

Measured, five causal runs on the same two stops:

- **bithire crimson and indigo — 3 divergent rows -> 0.** `pass=true`, restore
  exact, negatives held, static `moved=7` / db `moved=7`.
  `--ds-button-primary-bg` now reads `var(--ds-color-primary)` on the static
  arm, byte-identical to the DB arm; it used to read bithire's baked `#3A6FB0`.
- **evnto crimson and indigo — unchanged at 0 rows**, `moved=5` on both arms.
  Predicted before the run and confirmed: of the five declared channels only
  `--ds-button-primary-bg` can move under the flag, and evnto already authors
  the alias.
- **rottay — still REFUSED** by the empty-lowering guard, same message and same
  shape. `ROTTAY_DB_DOOR_INERT_FOR_PALETTE` is untouched by this packet.

Artifacts: `B2-{bithire,evnto}-{crimson,indigo}.MEASURED-NOT-RECEIPTED.json` in
this directory. Still unreceipted, and now for a different reason than before:
what blocks the control is no longer a measurement but the DT ruling on rottay
(an adjudicated `calibration.stopDiscriminationException`, or a finding that
rottay's palette door is a product defect).

Two things B-2 deliberately did NOT do. It did not declare authorship for an
IDENTITY stop — that stop writes the value the vertical itself ships, and
declaring it would move 7 channels on bithire and stop the identity from being
an identity (owner ruling, measured, fenced by drill 7). And it did not move
any receipted scenario: of every control the registry declares a static door
for, exactly one member lands on a field the compiler consults
(`palette.seeds -> palette.primaryColor`), so none of the 43 receipted CSS
scenarios could change — derived by drill 6 against the contract source, and
verified by re-lowering all 43 against their committed evidence (0 moved).
