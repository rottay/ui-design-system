# WO-CRA-23 round report — R0

This report is evidence subordinate to `roadmap/registry.json`; it is not a
status authority.

## 1. Round identity

- Round: **R0 — Truth, instrumentation and baseline**
- Work-order state before: `todo` (dependencies WO-GAT-05 and WO-GAT-06 both `done`)
- Start HEAD: `a97ddd7361d0a6bade35b47e30a62ed85d5dda40` on `main`
- Start porcelain digest: `b8f2f21de181575ebc7555e37e40356dcdb23e40b55682ccf95d55ec8468bf71` over 161 entries
- Stashes observed: 1, untouched
- Selected contract digest: the nine modern-rescue contract files, validated green by `program-check.mjs`
- Executor: `claude-modern-rescue`

## 2. Measurable objective

- Families in scope: 252 (inventory only — R0 writes quality tooling and contracts, no family styling)
- Required threshold: none. **No family carries a craft score in this round.**
- Visual-craft incidents targeted: none directly; R0 prepares the instrumentation that R1 will be judged by
- Failure-taxonomy receipt: **not applicable to R0.** No family receipt was produced, so no 20/120 taxonomy run was owed. The taxonomy is now *executable* — `eligibility.mjs` rejects an omitted category, an unexplained N/A and any applicable FAIL.
- Semantic icon census: 33 placeholder `TagIcon` call sites and 3 functional unicode stand-ins were located and partitioned across seven lab modules; 1 inline SVG is allowlisted chart geometry; 0 universal icon wells. Drain status in section 4.
- Calibration or cohort checkpoint ID: none. R0 has no visual cohort and no Codex GO was consumed.

## 3. Agent orchestration

- Conflict graph artifact: `conflict-graph.json`, verdict **safe** (validated by `cli.mjs conflict-graph`)
- Safe independent-set size: 10 read-only scouts in cohort 1; 5 write lanes in cohort 2
- Model routing, recorded once: **fable** for architecture scouting, the tenant-canon reconciliation and the compiler integrator; **opus** for the delicate verbatim lab extraction where zero rendered-output delta was the bar, and for coordination; **sonnet** for the six deterministic census scouts.
- Integrator-owned shared files: the architecture integrator owns the appearance and tenant-theme compilers and their tests; the quality integrator owns `scripts/quality-evidence/**`, `tokens-catalog.mjs`, the family inventory and the evidence root.
- Reassignments or ownership conflicts: none. No file was written by two lanes.

## 4. Outcomes

R0 produces **no family statuses**. Nothing in this round is `ELEVATED`,
`ALREADY_REFERENCE_GRADE` or otherwise scored; the round's unit of work is
instrumentation and truth.

### Closed in this round

1. **252-of-252 inventory correspondence.** Every family now resolves to a real source
   owner and a public-export chain, with a layer profile, an acceptance threshold and a
   tenant-divergence minimum. `family-inventory/index.json` no longer carries
   `sourceResolution: R0_REQUIRED` on any row — its own `resolutionLaw` is satisfied, and
   the resolution is regenerable rather than hand-maintained.
   - Resolution is **source-derived, not name-guessed**: the inventory's `components` field
     carries internal family names (`DetailPanel`) while the public surface exports the
     prefixed identity (`PatternDetailPanel`), so the resolver discovers the export from the
     owning folder instead of assuming a naming convention.
   - Two rows named components that do not exist in source (`RecordContent`,
     `DashboardInsights`); their real owners are `structures/record/content` and
     `structures/dashboard/insights`, now recorded.
   - Canonical `SemanticSurface` is present; deprecated `OverlayModal` holds no independent
     family row; the primitive denominator stays 100.

2. **quality-evidence v2**, with binary eligibility and the 100-point sighted craft score in
   **separate modules**. `craft-score.mjs` never receives a contract result, so a green gate
   cannot raise a sighted score. **85 negative drills execute and pass**, covering: a scored
   dimension without observable evidence scoring zero; an N/A without a falsifiable reason;
   an unresolved layer profile failing closed instead of borrowing a threshold; one hard veto
   blocking eligibility; an omitted failure-taxonomy category; a taxonomy N/A without a
   reason; any applicable taxonomy FAIL; a dropped `stressMatrix` floor case; color-only
   tenant divergence; Claude recording Codex sighted approval; `ELEVATED` without a
   productive source change; `ALREADY_REFERENCE_GRADE` with a productive edit; a shallow
   elevation leaving dimensions below the premium floor; a stale artifact hash; a stale
   source digest; the sighted approver as producer; an empty artifact; a lane packet missing
   an observable defect; a family-writer owning a reserved path; two lanes writing one file;
   two lanes claiming a singleton role; a reviewer declaring a write set; an R0 capture; an
   evidence artifact declaring work-order status; a stale `SHA256SUMS` entry.
   - v1 is now explicitly labelled **HISTORICAL BASELINE**, and the minimum-reliable-evidence
     policy plus work-order admission are **enforced in code**, not advisory.

3. **The control-impact baseline is honest, and one mischaracterization was fixed at its
   root.** Live counts confirm 4398 operational tokens / 38 EXACT / 836 INFERRED / 3524
   UNKNOWN with no drift. The impact map's "Layout/measure impact" column was a **regular
   expression matched against the control's id string**, not an analysis of what its channels
   reach — so `spacing.rhythm` reported "paint-only" while `--ds-rhythm-effective-scale`
   multiplies `padding` and `gap` across fifteen-plus skins. The column is now derived from
   the authored-CSS walk and **names the properties it found**, making the claim falsifiable.
   `experience.profile` and `profiles.expressive` were mischaracterized the same way and are
   likewise corrected.

4. **Static-versus-DB parity is measured honestly and remains unproven.** Three layers of
   machinery exist — reachability, channel-name containment, and per-path propagation — and
   none of them compares the two paths' resolved values. The R0 claim that equivalence is not
   proven control-by-control stands. The 21 active capabilities are enumerated.

5. **The envelope-versus-Advanced distinction is proven, and the discarded blocker stays
   discarded.** The six Standard dials are bounded per vertical; the 294-name Advanced
   allowlist is bounded by membership plus a 200-per-document budget and is **not** clamped to
   the dial ranges. Only `--ds-density-scale` and `--ds-effect-intensity` are range-checked as
   overrides. Exact premium radii and semantic status tones therefore need no envelope
   widening and no new vertical.

6. **Both tenant directions have a closed, versioned migration plan with zero proposals.**
   Every one of the twelve required art-direction groups is expressible today by composing
   existing recipe and expressive selections, Standard dials, anatomy variants and Advanced
   allowlist entries. Published profile ids keep their meanings — The Management's move is a
   *selection swap* to `technical-sharp@1`, not a redefinition of `editorial-round@1`. The two
   registries are proven independent. `canonClosure = 1`.
   - Reach-test result: `dots`, `deco-fan` and `ambient-orbs` are **declared but unreached** —
     in the closed motif vocabulary with no expansion rows. They are capacity to spend before
     any new motif may be proposed.

7. **Stale claims reconciled at their owning sources or generators**, never by hand-editing
   generated output: the `profiles.icon` "frontier" comment (it is an active Pro capability);
   the lifecycle view's bare "80 ledger entries", now "80 ledger entries (80 retired, 0 live)"
   — all 80 verified retired by full enumeration; and the two adjacent read metrics, which now
   state their distinct denominators.

8. **Three compatibility consumer censuses are complete** for `OverlayModal`,
   `BrandTheme.engineBridge` and `ComponentExtensions`/`EngineAwareProps.extensions` — with
   runtime-versus-type-only separation, successors, and an explicit list of what is still
   missing before any retirement decision. **None has death proof, and R0 retires nothing.**
   - `engineBridge` is the outlier: it carries **no `@deprecated` marker at all**, is read once
     by the compiler and never read again downstream, and has no named successor.
   - A docs claim that `OverlayModal` is used internally by `Sheet` and `Drawer` was verified
     **false** and must be corrected before anyone uses it to judge removal safety.

9. **A premium claim was found and labelled.** `docs/rottay-design-platform-10-10/COMPONENT-LEDGER.md`
   asserted 14 components as `accepted | accepted | 90-97` using the v1 tool's id space and
   vocabulary. No non-example scorecard has ever been produced by `scoreQualityEvidence()`;
   those scores were hand-authored. The ledger now carries an explicit historical-baseline
   header. This directly serves the R0 exit "baseline contains no premium claim".

10. **The reference-lab monolith is split behind scene-owned import boundaries.**
    `whitelabel-torture/page.tsx` went from **5,805 lines to 118** — a thin static dispatcher
    over 28 extracted section modules plus a shared frame and a component-free flag registry.
    - **The mechanism decision is load-bearing.** `next/dynamic` and `React.lazy` defer *load
      and render*; under Next 16.2.4 with `--webpack` and no lazyCompilation their async chunks
      remain in the route entry's build graph, so cold compile would have stayed monolithic.
      Only static per-scene route entries — each App Router page being its own webpack entry —
      satisfy the exit criterion. `ssr: false` was forbidden outright because it would break the
      first-paint contract the same round must meet.
    - **Measured transitively, with a control.** Per-scene tsconfig + `tsc --listFilesOnly`,
      counting transitive `torture-sections` modules excluding the shared frame and registry:
      legacy dispatcher **28** (control) versus `all` **0**, dashboard 2, workflow 4, surfaces 4
      (+1 external long-tail fixture), forms 5, shell 6, data 7, primitives 11. A transitive
      closure is used deliberately — a direct-import count could not see an indirect pull-in.
      The control is what makes a low count a measured exclusion rather than a vacuous result.
      Scope stated plainly: this is the static TypeScript module graph, which for static ESM
      imports the webpack entry graph follows. It is **not** a bundler chunk inspection;
      `next build` was deliberately not run because a build is a singleton and several lanes
      were active concurrently. Chunk-level evidence remains available to Codex on request.
    - The public test interface survives: route path, all 29 gates, multi-flag composition,
      render order and **220 `data-testid` handles, byte-identical before and after**.
    - The 28 sections are a verified disjoint partition with zero shared symbols, so the split
      moves code and never copies it. The eight scenes overlap by design; a shared static import
      is not a duplicated tree.
    - **`detailpanel` was added to the `data` scene** as a deliberate, coordinator-approved
      widening: 29 gates against a 28-flag scene union left `detail-panel` in no scene, live in
      the pixel baselines but absent from the lab and therefore denied the very boundary the
      exit criterion requires.
    - A drift gate was added (`e2e/diagnostics/torture-scene-registry.unit.test.mjs`, 7 checks
      including a negative drill) because the scene `flags` field became declarative-only after
      the retarget — a writer with no reader is exactly the drift this program forbids.

    Correction worth recording: HEAD's `page.tsx` is **5,796** lines, matching the contract text;
    the working tree held 5,805 because the file was already among the 161 inherited
    entries. The `data-testid` parity conclusion holds against both baselines.

11. **The Advanced `tokenOverrides` cap now fails closed instead of truncating silently.**
    The census ran first and satisfied the prerequisite: **no document in the repository
    exceeds the cap** — the largest is 36 entries, across three quality-evidence tenant
    fixtures and four divergence fixtures.
    - The compiler no longer drops entries past the bound. `appearanceAdvancedToVariables`
      previously did `if (count >= MAX_TOKEN_OVERRIDES) break;` with no error, no log and no
      return signal; it now rejects over-budget input with a named error quoting both the
      actual count and the bound, matching the skin-pack house pattern.
    - The document path gained the enforcement it was missing: `validateEnvelopeShape` already
      enforced every *other* member of the same frozen `limits` object — `maxDepth`,
      `maxObjectFields`, `maxDocumentBytes` — and `maxTokenOverrides` was the only one never
      checked there. It now emits one named issue at
      `$.visualFoundation.advanced.tokenOverrides`.
    - **The architectural fact behind the defect**: `TENANT_THEME_OVERRIDE_TOKENS` allowlists
      **294** names while the per-document budget is **200**. A fully schema-legal document
      authoring more than 200 of its 294 allowlisted tokens passed validation and was then
      silently truncated. The reconciliation is **not** to widen the cap — 294 is the
      vocabulary and 200 is the budget, two structurally different mechanisms. The cap is
      declared in exactly one place and was not moved.
    - The test that asserted the truncation as correct behaviour was **respecified, not
      deleted** — it built cap+25 entries and asserted exactly 200 survived. New boundary tests
      cover the canonical `compileTenantThemeConfig` path at 201 and 200, closing a coverage gap
      that path never had.

12. **One change was misread as out-of-scope and briefly reverted; the misreading, not the
    change, was the error.** The removal of the `--ds-motion-ambient` emission from
    `appearancePostureToVariables`, on the reasoning that a keyword custom property had no
    productive CSS reader, was **inherited** from a prior session; no lane in this round
    authored it. It was briefly reverted on a coordinator misattribution and then reinstated
    before close — the incident and its proofs are item 15. What survives as a standing fact
    here is the mechanical signal: because the inherited state is preserved, it disagrees with
    the HEAD-pinned artifact digests and three assertions in `canonical-digest-identity.test.ts`
    fail. That is the *expected* consequence of preserving inherited work, and it is recorded
    among the pre-existing failures in section 8. **No digest was re-anchored at any point.**
    Whether ambient belongs in CSS at all remains open and un-censused — open item 7.

13. **`program-check` was strengthened after the split broke it.** Moving the scene vocabulary
    into the new component-free registry removed the literal "Coverage atlas" from the lab page,
    and the contract checker went red. Rather than relax the assertion, `program/index.json` now
    declares `sceneRegistry` and `sceneRoutes`, the checker scans page and registry together for
    the same four required markers, and two new assertions were added: the registry must import
    no section component, and the eight scene routes on disk must match the eight declared
    scenes.

14. **Server first paint now carries tenant authority on both binding paths.** The defect had
    three links, and the third was the decisive one: the probe was a client page calling
    `useSearchParams` with no dynamic server API, so Next statically prerendered it and the
    subtree bailed out — **the served body was empty** and the root element carried no `data-*`
    at all. The dark ground was simply the DS default on bare `:root` showing through.
    - The routes now resolve the tenant on the server and emit a first-in-body stamp plus the
      compiled artifact inline. Awaiting `searchParams` is the cure rather than a workaround: it
      removes the prerender bailout, and `useSearchParams` then resolves during SSR. All 8 scene
      routes flipped from `○ (Static)` to `ƒ (Dynamic)`.
    - No second authority is created: client providers *claim* the root attributes rather than
      owning them, and the DB path declares `compiled-artifact` so the provider is silenced.
    - **Proven over served bytes, not screenshots** — R0 permits no captures, and a settled
      screenshot cannot see the frame the veto is about. `test:first-paint` is **27/27** against
      a real production build, with a 10/10 unit layer that needs no build.
    - **The harness is proven falsifiable.** Its first run failed 3/25 on a real bug, and during
      that failure the "no dark stamp" negative passed **vacuously**. Both negatives now assert
      the stamp exists and declares a theme before asserting it is not dark, and an inverted
      drill proves the authority assertion is not vacuous either.
    - Deliberate non-coverage, stated rather than hidden: `torture-dark`, `torture-light` and the
      legacy non-canonical-db management fixture get no stamp, because their CSS is compiled in
      the provider at render time and stamping would either match nothing or newly activate a
      vertical baseline and move live snapshot baselines. `torture-light` flashes exactly as
      before — unchanged, and outside R0's binding objective.

15. **A coordination incident, recorded rather than folded away.** The coordinator misattributed
    an *inherited* working-tree change to an active lane and ordered it reverted. It was not
    authored this round: it carried a ledger note describing it as a previously reconciled
    decision from a prior session. For a window inside the round, three files stood at HEAD
    text and their uncommitted prior-session content was not on disk. **The preservation
    contract was genuinely breached for that window** — it binds continuously, not only at
    round close — **and it is honoured at close.** Both statements are recorded; neither
    cancels the other.
    - **Reinstated before close; byte-exactness proven for two of the three.** The executing
      lane's captured pre-revert `git diff` carries the pre-revert blob hash in its `index`
      line for the two fully-reverted files; the third file's capture was a verbatim read of
      the affected region only, so no pre-revert whole-file hash exists for it and none is
      manufactured. Two files were restored and verified by hash
      (`fcf4584fa…`, `4214ed0ef…`, each also confirmed to *differ* from its HEAD blob); the
      third mixes the inherited hunk with this round's own `tokenOverrides` work, was never
      hashed pre-revert, and was restored from a verbatim pre-revert read — that weaker proof
      is stated rather than glossed. A `comm` of the round-start porcelain against the final
      porcelain yields an **empty loss set**.
    - The coordinator's supporting argument was also wrong: the claim that the change broke
      R1's `ambient: 'off'` ignored that the plan authors ambient in the *document* and the
      policy moved to the layer `MotionProvider` consumes. The capability was never removed.
    - What went right: the lane reported the misattribution with the ledger citation instead of
      accepting blame; it stopped a singleton suite mid-run rather than publish mixed-state
      results; and **no digest was re-anchored at any point**.

16. **Corrections applied after the first seal, at owner instruction.** R0 was reopened for
    close-out only; no R1 work was started.
    - **The v2 drills were unreachable from the umbrella suite — twice.** First because
      `node --test scripts/*.test.mjs` is a non-recursive glob. Then, after adding a second
      glob, because it sat behind `&&` after a block carrying 21 inherited failures, so it
      short-circuited and never ran. The first "verification" was invalid: it executed the
      glob directly instead of the umbrella path. Fixed by running the drills **first**, and
      by registering both `quality-evidence-v2-drills` and `modern-rescue-program-contract`
      as blocking entries in `ci-gates.manifest.mjs` — the repository's declared single
      inventory, consumed by both `pretest` and CI. That file's own docstring describes this
      exact failure mode: gates that looked enforced were not, while their self-drills ran,
      making the dashboard greener than the code.
      Verified through the real umbrella output: the drills are the first block, now 35/35
      after the ownership drills landed (item 17); the inherited block is unchanged at
      825 tests / 804 pass / 21 fail.
    - **The showroom coverage figure was wrong: 216 of 237, not 218.** The earlier number
      subtracted two known-bad rows from the raw count under an *unstated* rule. A recount
      that resolves each row's `name` against the inventory finds **four** defective rows:
      two double-coverage (`overlay-modal:Modal` onto `primitive/feedback/modal`;
      `onboarding:OnboardingSurface` onto `surface/forms/wizard`, already covered by
      `wizard:WizardSurface`) and two naming no export at all (`page-shell:PageShell`,
      `record:Record`). 220 raw − 2 unresolvable = 218 resolving − 2 collapses = **216**.
      The two misnamed rows are also why `PatternPageShell` and `RecordContent` read as
      missing — those families are reachable in the showroom but mislabelled, not absent.
    - **The `tokenOverrides` behaviour change is now documented** in the active capability
      map: 294 is the vocabulary, 200 is the per-document budget, exactly 200 is accepted,
      and 201 or more fails closed with one named error on both the document and compiler
      paths. No truncation.
    - **Every command in `manifest.json` now states its working directory.** The
      `core-structure-audit` entry resolves its source root relative to the package and was
      not runnable as recorded; it now carries `cwd` plus a `pnpm --filter` equivalent.
    - **The incident receipt contradicted itself and the tree, and was rewritten.** Codex
      named it the single audit blocker. It carried a FULLY RECOVERED header and the matching
      hashes, but several sections were written *during* the incident and were never
      re-tensed: they still said the files were identical to HEAD, that inherited content
      remained lost, that the tree was internally inconsistent, and that Codex had to decide
      whether to reinstate a state that had already been reinstated.
      `receipts/incident-inherited-worktree-revert.json` now splits into
      `finalStateAtRoundClose` (present tense, every hash re-verified against the repository,
      loss set independently reproduced as **empty**, census confirmed coherent with source)
      and `historicalIncident` (past tense, mid-round facts preserved verbatim, nothing
      deleted). The contract breach is **still recorded as a real breach** scoped to a closed
      window — a contract that binds continuously can be broken and then honoured again — and
      the recovery decision previously offered to Codex is **withdrawn**, because no recovery
      decision remains. The architectural question of whether ambient belongs in CSS stays
      open and un-censused (open item 7); it is not a recovery blocker.
    - **Three collateral reconciliations followed from that**, each because the wording was
      false at round close and for no other reason.
      Item 12 above claimed the architecture lane authored the ambient removal and that the
      revert stood — both false, and self-contradictory against item 15; it now records the
      change as inherited and keeps only its still-true content.
      Item 15 said "three files lost uncommitted prior-session content" with no temporal
      scope, which contradicted this report's own closing line that zero pre-existing entries
      were lost; and its "byte-exactness proven" lead over-claimed for the third file. Both are
      now scoped, and the breach is stated explicitly instead of being implied by the recovery.
      `manifest.json#worktreeFreeze` called all 161 round-start porcelain entries "modified
      files"; the recorded start porcelain is 148 modified files plus 13 untracked paths, and
      the law now says so. The entry count and the porcelain digest are unchanged.
      `SUMMARY.json` needed nothing: it is generated by `seal-round.mjs` from the program
      contracts and carries no incident wording.
    - **A second receipt carried the same stale narrative, and was missed on the first pass.**
      `receipts/advanced-token-overrides-fail-closed.json` had an `outOfScopeChangeRejected`
      block repeating the misattribution, claiming the three digest assertions "returned to
      green" and that "the rejected design is not encoded anywhere" — all false at round close
      — plus `gates.compilerUnitTests: "972 tests, 971 pass, 1 fail"`. Re-run and verified:
      the real split is **972 tests, 968 pass, 4 fail** across 2 files (3 from
      `canonical-digest-identity`, 1 from `sidebar-group-channels`). The total of 972 is
      unchanged, which confirms the same scope and isolates the drift to the split. The block
      is corrected and kept, not deleted; `appearanceSuite: 77 of 77` was re-run and is true.
    - **The round-start porcelain is now an artifact, not a coordinator-held file.** Every
      load-bearing preservation claim — the 161 entries, the 148/13 composition, and the empty
      round-close loss set — rested on a capture no auditor could reach.
      `receipts/round-start-porcelain.txt` is now sealed in this directory and hashes to
      `b8f2f21de…`, the digest `manifest.json#worktreeFreeze` already declared, so the comm is
      re-runnable from the evidence alone.

17. **The R1 ownership proposal asserted conflict-freedom instead of computing it.** The
    second R0 exit blocker. `receipts/r1-canary-ownership-proposal.json` carried
    `ownershipCollisions: []` and `conflictFree: true` as literals while four concrete
    parent/child overlaps existed.
    - **Root cause: a resolution pointer used as a write boundary.** Each writeSet was built
      by taking the family's `sourceOwner` from `family-inventory/index.json` and appending `/**`.
      For three families that pointer is the whole *category* directory — `pattern-data-table`
      resolves from `patterns/data`, `app-shell` from `structures/shell`, `pattern-page-shell`
      from `patterns/shell` — so `patterns/data/**` swallowed four sibling families belonging
      to three other lanes.
    - **Why the gate could never have caught it.** `validateConflictGraph` compared write sets
      with exact string equality, so `patterns/data/**` and `patterns/data/list-toolbar/**`
      were two unrelated strings. Containment was structurally invisible. The boolean was not
      merely wrong; nothing existed that could have falsified it.
    - **Rebuilt from the real tree.** PatternDataTable narrowed to
      `patterns/data/data-table/**` — verified self-contained: nothing under `data-table/`
      imports a sibling family, and the mobile projection lives inside it, so no companion
      owner is warranted. CollectionHeader-PageShell narrowed to `patterns/shell/page-shell`,
      leaving `cockpit-header`, `workbench-header` and `feature-workspace-frame` deliberately
      unclaimed. The AppShell within-lane redundancy was removed. Two singleton integrator
      lanes now own category barrels, shared contracts and tests, which family-writers reach
      by declared request. 14 lanes, 91 pairs, **0 collisions**.
    - **The claim is now executable.** New `ownership-overlap.mjs` computes collisions over
      real filesystem expansions (so a declared exclusion actually resolves an overlap),
      glob containment over scopes (so a dangerous spelling is named even when neutralised),
      and reserved/`mayNotEdit` rules per expanded **file** rather than per glob string.
      Wired into `validateConflictGraph`, exposed as `cli.mjs ownership`, and covered by six
      new drills. **Demonstrated fail-closed**: restoring the broad parent in a copy makes the
      CLI exit 1 and surfaces 5 collisions — the 3 the audit named plus 2 more against the new
      integrator lanes. The gate finds more than the manual review did.
    - **The chart canary could not deliver its own name.** `Chart-frame-Line-Bar-tooltip-legend`
      owned only `families/{line,bar}-chart`, but frame, tooltip and legend are category-shared
      chart chrome living in `presentation/family-frame`, `presentation/tooltip` and
      `presentation/scaffold` (legend is inside scaffold), with tooltip behavior in
      `runtime/interaction/tooltip-state` — verified: `line-chart` imports
      `../../presentation/tooltip` and `../../presentation/scaffold`. The companions are now
      enumerated exactly and proven disjoint (no other R1 lane writes anything under
      `charts/`). Renaming the canary instead would have silently shrunk R1 scope, which is an
      owner decision — flagged, not taken. `presentation/crosshair` is deliberately not claimed.
    - **The integrator write sets are derived, not hand-listed.** Every family root contributes
      `/**/contracts/**` to the architecture-integrator and `/**/tests/**` to the
      quality-integrator, so changing a family root cannot leave its tests or contracts owned
      by nobody — a gap the first hand-built version had for the new chart owners.
    - **Singleton authority cardinality was never enforced — the eighth denial.** The contract
      marks both integrator classes `singleton: true` and the write-domain law says a shared
      authority has exactly one owning lane, but the ownership analyzer only ever asked
      *file-level* questions. Reproduced: carve one real file out of the architecture
      integrator via `writeSetExcludes`, then add a **second** architecture-integrator lane
      owning exactly that file. Sixteen lanes, two integrators, a genuinely split authority —
      and `conflictFree` stayed **true** with zero findings in every blocking category, because
      the split is deliberately non-overlapping so no collision exists and coverage is still
      satisfied. Admission would have accepted either packet, since each resolves to its own
      lane. The split was invisible at every layer that existed.
      `checkSingletonCardinality` now **derives** the singleton classes from
      `laneTypes[*].singleton === true` rather than naming them, so a future singleton class
      binds with no code change — pinned by a drill that marks `lab-writer` singleton in a
      contract copy and requires the check to bind it.
      The legitimate twelve-lane family partition is untouched: `family-writer` is not a
      singleton, so cardinality governs shared *authorities* while unit coverage keeps
      enforcing one owner per canary root. A drill asserts both together so a future change
      cannot collapse one into the other.
    - **The gate still failed OPEN on three authority invariants — the seventh denial.** Each
      was reproduced in memory before being fixed, and each shows the same shape: the checker
      asked less than the contract does.
      **Owner class per unit went unchecked.** Flipping one canary lane's `laneClass` to
      `lab-writer` left the verdict green, because unit coverage required a single owner but
      never that the owner be the domain's class — and the existential class check elsewhere
      was satisfied by any sibling.
      **`sharedRequest.domainId` was decorative.** Renaming one request's domain to
      `invented-domain` left it green. That same request also carried only four of the eight
      fields the family requests use: a field that is merely conventional is one a plan can
      omit, and an omitted field is one the gate cannot judge. The schema is now closed —
      unique `requestId`, `fromLane` matching its container, a registry-known `domainId`, a
      target of the domain's *authoritative class*, non-empty covered paths, an allowed
      `change`, a boolean `dependsOnCompletion`. **Closing it immediately caught a real defect
      in the sealed plan**: the reference-lab request was one of the incomplete ones.
      **`declaredEdges` reduced to an unordered pair.** Appending the reverse of a real edge —
      a two-node cycle — satisfied the very sequencing requirement it breaks. Edges now
      validate real endpoints and cohorts, non-empty sequencing, a derived counterpart *in
      that direction*, condition agreement, no duplicates or self-edges, and the graph is
      **proven acyclic** by cycle detection rather than assumed to be.
      All three now turn the verdict red, and the 5s performance gain holds: 81 drills in 7s.
    - **The gate I built was too slow to be usable, and that was a blocker in its own right.**
      The focal v2 suite went from ~6.8s to **over seven minutes** at full CPU, holding the
      umbrella behind it. A blocking gate that rescans the tree per drill would have made the
      policy it enforces impractical for R1–R6. Profiled rather than guessed: two causes, both
      mine — `analyzeOwnershipProposal` called `deriveAnatomyEdges` **twice** per analysis for
      no extra information, and inside it `effectiveFiles(other)` sat in the per-import loop,
      re-walking the whole tree once per import of every file.
      Fixed by computing the edges once, hoisting per-lane work out of the loops, and
      memoizing directory expansion, module resolution and file reads for the duration of one
      invocation. Measured: `deriveAnatomyEdges` 21887ms → **291ms**,
      `analyzeOwnershipProposal` 44754ms → **496ms**, focal suite → **5s**, umbrella → **179s**.
      **Nothing was weakened to get there.** The speedup is pure memoization and is proven not
      to decide outcomes: identical 36 edges with identical keys, same verdict, zero findings
      across every check. Caching is scoped to the immutable repository tree — temporary roots
      that drills write bypass it entirely — and two drills pin exactly that: one asserts a
      mutable root is never served from cache, the other clears the caches and requires
      byte-identical findings. No drill shares a mutable verdict; what is shared is read work
      over a tree no analysis writes.
    - **The full graph still went green on false data — the sixth denial, seven defects, all
      mine.** Reproduced before any fix.
      **The anatomy edges were fabricated by the same collapse I had already fixed once.**
      `deriveAnatomyEdges` truncated a write-set entry at its first wildcard, so the
      integrator's per-family `contracts` glob collapsed to the family's module root — and a
      module importing *itself* appeared to consume the contracts and tests integrators. 25 of
      49 sealed edges rested on that. Imports now resolve to a real **file** (a directory
      specifier resolves to its index entrypoint) and membership is tested against exact scope.
      36 edges survive, and the surviving integrator edges are genuine: `Button/compound/Group`
      really does import `Button/contracts/index.ts`, which the family excludes.
      **The readSets were decorative** — ten lanes carried an identical 57-path block while
      AppShell carried two, and the gate only checked non-empty, so `readSet: ["x"]` passed.
      **The edges governed nothing**: `conflictFree` ignored them and cohort safety only
      re-ran the file check, so an intra-cohort dependency changed no outcome.
      **The requests contradicted my own classifier**: every family asked the integrator for
      its own `<family>/index.ts`, which the barrel adjudication calls that family's
      implementation entrypoint and which no integrator write set contains.
      **Coverage was existential** — `family-source` could have lost eleven of twelve canary
      roots and stayed green. **And the `product-applications` no-write rule was vacuous**: its
      only glob used a negative lookahead the matcher silently dropped, leaving an empty list
      that always passed.
      All seven are closed and now govern the verdict. Hardening the gate immediately produced
      **186 request findings, 37 unsequenced edges and 1 readSet finding** against the
      previously sealed plan — that number is the honest measure of how far that seal was from
      true, and it is recorded rather than smoothed away.
      While fixing this I reproduced the *same* truncation bug a third time, in my own new
      probe-path construction. `probePath` now replaces wildcard segments with a concrete
      token so a channel survives, and a drill pins it.
    - **The plan was conflict-free but could not have EXECUTED R1 — the fifth denial.** A
      valid write partition is not a plan. 111 write-set entries and **zero** readSets; the
      `sharedRequests` were prose with literal ellipses; and **nothing owned the reference
      lab**, although `rounds/index.json` makes `/probe/ds-reference` the only canvas and evidence
      source R1 has. Nothing owned the R1 evidence surface either, though the evidence
      contract requires twenty artifacts plus three cohort GO receipts. Nothing owned i18n,
      shared recipes/tokens/styles or manifests, though "extract reusable semantic group
      recipes" is an explicit R1 objective. And the analyzer computed only the first of the
      graph's **four** edge conditions, over files that already exist — which is why an
      incomplete plan read as conflict-free.
      `rounds/index.json` now carries an **R1 write-domain registry**: nine required domains and
      five no-write domains *with reasons*, because the audit requires a domain that must not
      be written to be recorded rather than left implicit. The checker reads that list from
      the **contract**, never from the proposal, so a plan cannot self-certify by omission.
      A fifteenth lane, `R1-reference-lab`, owns the canvas. Its class `lab-writer` was
      already in use by R0's own conflict graph **without being declared in `laneTypes`** —
      a gap that mattered, since the new unknown-class check would have blocked a lab packet.
      Every writing lane now declares a readSet, and family readSets are **derived from real
      imports** rather than hand-authored. `sharedRequests` carry `domainId`, `targetLane`,
      exact paths and a reason.
      Edges are now **computed**: **49** anatomy edges (38 direct-path, 11 named-through-barrel)
      and 132 shared-authority sequencing edges.
      **A correction to what this section previously claimed.** The first version recorded
      *73* anatomy edges "computed from real imports". That count was inflated and the
      description overstated it: the resolver treated any import resolving *above* a lane root
      as reaching every lane beneath, so one
      `import { Box, Button, Flex, Stack, Text } from "../../../primitives"` produced edges to
      all six lanes under `primitives` when only the Button one is real. A drill asserted one
      of the fabricated edges and **passed** — it was validating the bug. A specifier
      resolving above a lane root now yields an edge only when a named specifier actually
      names one of that lane's families.
      Stated limits rather than hidden: only relative specifiers are traced; coupling
      expressed in comments or grammar precedent is invisible; a re-export chain that renames
      a symbol would be missed. Derivation *under*-reports — it can prove an edge exists,
      never that none does.
      **The gate found three defects in my own first draft** — a coverage matcher blind to
      mid-path wildcards, family lanes not excluding `styles/` so all twelve collided with the
      integrator, and an ownership check demanding sole occupancy that `family-source` cannot
      satisfy. And one new drill was wrong rather than the checker: it asserted a
      list-toolbar→data-table edge taken from the defect description, but that reference is a
      prose comment in a `contracts/` file which the lane excludes anyway. The drill now
      asserts a hand-verified real edge and requires every edge to cite its import.
    - **Five more gaps, found by the advisors' dictamens after that seal.** Each was
      reproduced before being fixed, and two were serious.
      **An unknown `laneClass` failed open** — the worst defect in this closure, and mine:
      the very generalization that closed the integrator hole gated on "is a writing class",
      so a packet declaring a class nobody defined skipped *both* lane resolution and the
      family-writer reserved loop. Reproduced: `laneClass: 'totally-made-up'` claiming
      `packages/core/src/index.ts` returned `admitted: true` with **zero** blockers.
      **A latent R1 deadlock**: `effectiveFiles` is expansion-based, so a claim on a file that
      does not exist yet always blocked — and admission runs *before* the write, so the gate
      was unsatisfiable for the quality-integrator, whose charter is creating new tests.
      Existing files are still judged by `effectiveFiles`; a not-yet-existing path is judged
      against the same lane's declared scope, which is no weaker, with an agreement drill
      proving the pattern twin is never looser than the expansion authority on any real file.
      Also closed: the analyzer's owning-lane invariant was held in **prose** — my law claimed
      the analyzer "structurally cannot express" ownership, which both advisors independently
      flagged as false, since it holds both `lane` and `owningLane` at the classification site.
      The law now states the true and stronger fact and the invariant is **asserted
      executably**. Plus `proposalSha256` provenance so a verdict names the proposal that
      authorized it, and a named drill for `laneClass` spoofing.
      The advisors **disagreed** on the new-file question; the disagreement is adjudicated in
      the receipt, adopting the literal-compliance half of one and the deadlock fix of the
      other.
    - **Consumer parity, take two — the third denial, and it caught two things at once.** The
      first parity fix proved the *function*, not the *command*: `cli.mjs` called
      `admitWriterLane(readJson(argument))` with no options, so the wired path never received
      the proposal and every family entrypoint blocked. My recorded "end-to-end admission
      verdicts" claim was false for the public path — the drills had called the function
      directly. Worse, admission computed the owning lane and **discarded** it: a proposal with
      LANE-A=Button and LANE-B=Input admitted a LANE-A packet declaring `Input/index.ts`, so
      one lane could claim another lane's component and pass.
      The command now takes the authorized proposal as an explicit second positional argument,
      never inferred from the evidence root or from the packet; a missing proposal is an
      evaluation blocker rather than a usage error, so the refusal is machine-readable and
      still exits 1. Admission also resolves `packet.lane` to exactly one proposal lane and
      blocks lane-absent, lane-duplicated, `laneClass` mismatch, any owned file outside that
      lane's effective write set, and any adjudicated hit whose `owningLane` is not the
      packet's. **Five drills drive the real command and assert its exit code**; measured own
      entrypoint 0, foreign entrypoint 1, missing proposal 1.
      This is layering, not a second semantics — both consumers still call the one shared
      classifier for the *class* of a hit, and only admission adds *local ownership*, which a
      proposal-wide analyzer cannot express. Written into the contract as `localOwnershipLaw`.
      Two of my own drills had to move with the law: one asserted a family-writer was admitted
      with no proposal at all, and one built its packet so the required-field defaults
      overwrote `lane`. Both were repaired to test what they name, not relaxed to pass.
    - **Consumer parity — the second GO denial, and it was right.** A canonical authority with
      two consumers that classify differently is not closed. `admitWriterLane` still rejected
      every reserved hit while the ownership analyzer permitted family entrypoints, so opening
      R1 would have blocked the lanes on their own components. `admitWriterLane` now routes
      through the **same exported classifier** — one implementation, no second semantics — and
      three drills run **both** consumers over the same files requiring identical verdicts:
      entrypoint admitted by both, shared/category barrel blocked by both, unclassifiable
      reserved index blocked by both. Admission was hardened in two further ways while open:
      it evaluates `reservedPaths` **per pattern** rather than any-match, so a file matching
      both the adjudicated index pattern and an unrelated reserved pattern still blocks on the
      unrelated one; and `matchesReserved` was aligned to the corrected globstar builder,
      closing the hole where `packages/core/src/index.ts` escaped admission entirely.
    - **Two further holes in the predicate, both verified before being fixed.** With family
      roots taken from write sets alone, a hypothetical `structures/shell/index.ts` classified
      as an *implementation entrypoint* — wrong, since `bottom-tab-bar` nests inside it. Roots
      now also include any `sourceOwner` lying **strictly inside** one of that same lane's
      roots; the strictly-inside filter is what makes reading a resolution pointer safe at all.
      And a directory inside **two** lanes' roots silently picked the deepest; it now fails
      closed as `unclassified`.
    - **The vacuous `barrels` rule is now enforceable.** `mayNotEdit` lists `barrels`, enforced
      by a substring test for a directory named `barrels/` that exists nowhere in this tree —
      dead since inception. The contract binds the word to the classifier instead.
      One defect here was found by neither the advisor nor the drills: the first wiring
      classified **every** file positionally, so a `README.md` and a `.tsx` component were
      reported as shared barrels. The real proposal caught it; a regression drill now covers it.
    - **The barrel rule was adjudicated in the contract, not reinterpreted in the checker.**
      This was the audit's second GO denial and it was correct. `reservedPaths` declares
      `packages/core/src/**/index.ts` for shared barrels, but the structure law makes every
      authored unit a `folder/index.ts`. The first fix classified **every** such hit as a
      "contract ambiguity" on the strength of the filename and then let `conflictFree` ignore
      the whole bucket — neutralising a binding rule wholesale and asserting by omission
      exactly what this round exists to stop. Deferring it to a later round was also wrong:
      R0's scope covers tooling **and contracts**.
      `orchestration/index.json` now carries a `barrelOwnership` block. `reservedPaths` itself
      is untouched, so no downstream consumer's semantics move. The rule is positional and the
      checker **reads it from the contract**: an index file is a *shared barrel* if it lies
      outside every declared family root **or is a strict ancestor of one**; it is an
      *implementation entrypoint* only if it sits at or inside a family root and is not such
      an ancestor. Anything unclassifiable — no family roots, a different reserved pattern, or
      no rule present — **fails closed**.
      The ancestor arm is the load-bearing half: without it a lane could launder a category
      barrel into its own source just by declaring the category as its root, which is the very
      shape that failed the first audit. All 38 hits are now **positively** classified as
      implementation entrypoints, each naming the family root that justifies it; none is waved
      through, and none sits unadjudicated inside the verdict.
      **Demonstrated on the wired path**: restoring the broad parent makes the CLI exit 1 and
      turns `patterns/data/index.ts` blocking as a shared barrel. Three drills cover it.
      Option (b) — treating every hit as blocking and giving all index files to the integrator
      — was rejected because it would leave family-writers with no editable entrypoint at all
      and deadlock R1 rather than govern it.

## 5. Canon reconciliation

- Proposals created: **0**. Adjudicated: 0. `canonClosure = 1`.
- Three candidates were rejected before becoming proposals under existing-axes-first:
  a network-node motif, a `snap` easing keyword, and a registration-marks motif.
- Expert allowlist changes: none.
- Kimi capabilities: none removed, none merged. Claude read **zero** raw annotations and
  edited the inbox **zero** times.

## 6. Tests and documentation

- Observable contracts added: the v2 eligibility, craft-score, receipt, admission,
  conflict-graph and round-evidence gates.
- Negative drills: 85, all executable and passing (29 first pass; 6 write-set ownership; 1 reserved-matcher globstar; 3 barrel adjudication; 3 consumer parity; 4 predicate and regression; 5 real admission CLI; 1 integrator lane-binding; 5 post-dictamen gaps; 8 R1 executable-plan; 9 adversarial for the sixth denial plus cache safety; 7 pinning the seventh-denial reproductions; 4 pinning singleton cardinality — see item 17).
- Obsolete tests respecified, with reasons stated rather than silently rebaselined —
  see section 8.
- Generated catalogs regenerated from their generators (`pnpm tokens:catalog:write`,
  verified by `--check`). No generated artifact was hand-edited.

## 7. Evidence

- Manifest: `manifest.json`
- Minimum reliable evidence required for R0: manifests, source/public-export inventory,
  impact baseline and new negative drills — **no captures or recordings**. Honored: this
  round produced **0 captures and 0 recordings**.
- SHA256SUMS: present, covering every artifact in this directory.

## 8. Honest exit

### Final check state

| check | result |
|---|---|
| `program-check.mjs` | PASS |
| `program-check.test.mjs` | 11/11 |
| quality-evidence **v2 drills** | **85/85** (8s cold) |
| `cli.mjs ownership` on the R1 proposal | **conflict-free**, 91 pairs, exit 0 |
| inventory correspondence | **252/252**, valid, 0 blockers |
| `roadmap:check` | PASS — 100 WOs consistent |
| `core-structure-audit --check` | PASS — no new structural debt |
| core typecheck | **0 errors** |
| showroom typecheck | **0 errors** (pinned 5.9.3) |
| showroom lint | **0 warnings** |
| scene-registry drift gate | 7/7 |
| `test:first-paint` | **27/27** against a production build |

### Pre-existing failures this round did NOT introduce and did NOT mask

- 21 failures in `node --test packages/core/scripts/*.test.mjs`, recorded as the round-start
  baseline, caused by the inherited worktree (161 porcelain entries: 148 modified files plus
  13 untracked paths).
- `brand-theme/tests/sidebar-group-channels.test.ts` — the test file is unmodified; the
  inherited worktree rewrites the `menu.css` it reads.
- 3 assertions in `canonical-digest-identity.test.ts` — HEAD-pinned digests disagreeing with
  the inherited adjudicated worktree state. Same category: worktree ahead of pins. Whether
  those pins get re-anchored is an owner decision, explicitly not this round's.

### Open items for Codex

1. **72 visual baselines across 7 spec files** are expected to differ, because placeholder
   glyphs became correct semantic ones. Nothing was regenerated, deleted or weakened.
2. **No `entity.workspace` role exists** in the 282-entry corpus. Two sites render
   `entity.organization` as a coordinator decision at the available granularity, with the
   writer lane's dissent recorded.
3. **`icon: ComponentType<any>`** on `KeyMetric`, `DataTerminalCard`, `EditHeader`, `FormHeader`
   and `HeaderActionIcon` accepts any ungoverned component *and* silently nulls a governed
   `Icon`, because none pass `label`/`decorative`.
4. **`TORTURE_SECTIONS` drift** and **`skin-rule-coverage.spec.ts` swallowing navigation
   failures** — the latter must not be cited as evidence the split preserved coverage.
5. **A docs claim that `OverlayModal` is used internally by `Sheet`/`Drawer` is false**, and
   must be corrected before anyone uses it to judge removal safety.
6. **`packages/core/dist/commercial.css` was missing**, failing every showroom build; it was
   regenerated from the package's own script.
7. The **`--ds-motion-ambient`** question — whether a keyword custom property belongs in CSS —
   remains genuinely open and was never censused.
8. **RESOLVED in this round, and both consumers are aligned.** The barrel/entrypoint
   distinction is adjudicated in `orchestration/index.json#barrelOwnership` and enforced by a
   single classifier that both `findExpansionViolations` and `admitWriterLane` call — see item
   17. `matchesReserved` was aligned to the corrected globstar builder in the same change, so
   `packages/core/src/index.ts` no longer escapes admission. Nothing on this thread is
   deferred, and the admission command now receives the authorized ownership proposal
   explicitly and binds every writing lane to its own effective write set.
   The advisor's integrator-hygiene concern — that nothing checked an integrator quietly
   claiming a family entrypoint — is closed, but by a different route than proposed: rather
   than asserting that every `index.ts` in the integrator's write set classifies as a shared
   barrel, **lane binding was generalized to every writing lane class**, so an integrator
   claiming `Button/index.ts` is blocked while its own category barrel is admitted. The
   originally suggested assertion was still not added, because `findExpansionViolations`
   deliberately scopes to family-writer lanes and the inverse claim would encode an
   expectation the contract does not state. Flagged rather than invented.
9. **`family-inventory/index.json` records a category directory as `sourceOwner` for at least three
   families** (`pattern-data-table`, `app-shell`, `pattern-page-shell`). That is legitimate as
   a *resolution pointer* but is what a naive consumer turns into an over-broad write set. The
   R1 proposal now narrows explicitly; whether the inventory should carry a separate
   `writeBoundary` field is an open contract question.

- Hard veto count: **0** — none applicable, since no family was assessed.
- Unknown targeted impacts: **0** — no family/control triple was targeted.
- Canon closure: **1**. Path parity: **not asserted** — the baseline proves it is not yet
  provable per capability, which is the honest R0 result.
- Confirmation: **no commit, push, publish, tag, stash mutation or destructive git command**
  was run at any point. Zero pre-existing worktree entries were lost; HEAD and the single
  stash are unchanged.

Required final line:

`ROUND_R0_IMPLEMENTED_PENDING_CODEX_AUDIT`
