{
  "schemaVersion": 1,
  "law": "Deuda de cascada por NOMBRE (ARCHITECTURE §1.6): un canal de componente sin camino a raiz es deuda. Decrease-only: el baseline sigue al arbol HACIA ABAJO, nunca hacia arriba.",
  "rules": {
    "a": "las raices/rampas (destinos de fallback) quedan FUERA del denominador",
    "b": "un fallback funcional que alcanza raiz -- incluso envuelto en color-mix() -- cuenta como cableado",
    "c": "cableado NO es transitivo: `transitivelyUnwired` sigue la cadena de fallbacks hasta un nombre CON PRODUCTOR (declarado en CSS autorado o emitido por un derivador de familia). Es la cota superior de la misma deuda; `debt` es la inferior"
  },
  "producedOn": "cascade-wiring-census",
  "corpus": "libraries/engine/skins/files/collectSkinFiles() -- un solo walker, importado",
  "denominator": 4342,
  "wired": 2207,
  "debt": 2135,
  "transitivelyUnwired": 2179,
  "rootsExcluded": 766,
  "skinFiles": 391,
  "rootsExcludedNote": "rootsExcluded is measured from the exact fallback-target set. --ds-input-md-icon-size remains a positional fallback target without a root-reaching fallback of its own, so it is excluded from the denominator. The executable shape drill keeps that distinction fail-closed.",
  "transitivelyUnwiredNote": "2205 -> 2204 al integrar WO-DER-04, bajado en el commit que movio el numero. El unico nombre que salio, medido por diferencia de conjuntos contra el arbol previo a la integracion (1 salida, 0 entradas): --ds-skeleton-animation-easing, cuya cadena de fallbacks moria en --ds-motion-ease-standard, un nombre que nadie escribia. Ahora `foundation/animations/transitions` lo declara, asi que la cadena alcanza terminal. 2204 -> 2203 en connection lot 2, por el mismo nombre que baja `debt`: --ds-radius-nest-inset sale del denominador al volverse destino de fallback. La cota superior sigue >= `debt` (2203 >= 2160) por construccion, y la diferencia (43) son los nombres cuyo fallback nombra otra cosa que nadie escribe. El invariante de forma lo comprueba en cada corrida. 2203 -> 2202 in WO-FAM-14 (2 exits, 1 entry): --ds-skeleton-animation-name and --ds-skeleton-button-radius leave with the compounds; --ds-skeleton-bone-radius enters, because its fallback reaches --ds-radius-scale, which the producer set does not list. 2202 -> 2179 in WO-FAM-01, by the same exits as `debt`.",
  "debtNote": "2161 -> 2160 in connection lot 2, lowered in the commit that moved it. The single name that left, measured by set difference against `main` (1 exit, 0 entries): --ds-radius-nest-inset. It did not gain a fallback; it became a fallback DESTINATION, which rule (a) is exactly about -- `components/card` now declares --ds-card-nest-inset from it and the card skin reads `var(--ds-card-nest-inset, var(--ds-radius-nest-inset))`. Its sibling operand --ds-radius-nest-ratio, which `shape.nesting` introduced in the same lot, never entered the denominator for the same reason and therefore never entered the debt. 2160 -> 2158 in WO-FAM-14, lowered in the commit that moved it, measured by set difference against 2583f10da (2 exits, 0 entries): --ds-skeleton-animation-name and --ds-skeleton-button-radius, the inline channels of the retired skeleton compounds. The anatomy renderer's bone geometry channels (--ds-skeleton-bone-{x,y,width,height}) enter wired on --ds-spacing-0 and --ds-skeleton-bone-radius on --ds-radius-scale. 2158 -> 2135 in WO-FAM-01, lowered in the commit that moved it: the deleted Switch skins (their unwired --ds-switch-* reads), the retired inline --ds-rg-color-* channels of Radio.Group and --ds-toggle-checked-hover-filter leave, and every family channel the cut reads keeps a root-reaching fallback, so no name enters.",
  "denominatorNote": "4365 -> 4366 in connection lot 2. Two entries (--ds-card-nest-inset, --ds-card-nest-ratio: the card channels the nesting law is now read through) and one exit (--ds-radius-nest-inset, reclassified as a root by rule (a)). `wired` 2204 -> 2206 is the same two entries seen from the other side: both carry a root fallback, so neither is debt. rootsExcluded 774 -> 777 counts the two nesting operands plus --ds-control-height-scale, which `shape.control-height` introduced and which the control skins already read inside another channel's fallback. 4366 -> 4367 in WO-FAM-14: five entries (--ds-skeleton-bone-{x,y,width,height,radius}) and four exits (--ds-skeleton-animation-{easing,name}, --ds-skeleton-button-radius, --ds-skeleton-header-bg, all read only by the retired compound skin or the old shimmer shorthand). `wired` 2206 -> 2209. rootsExcluded 777 -> 778 is --ds-spacing-0, now the fallback destination of the bone geometry; --ds-motion-ease-standard stays a destination through the renderer's `var(--ds-motion-ease-out, var(--ds-motion-ease-standard))`. 4367 -> 4342 in WO-FAM-01: the Switch skins leave (Modern and Rustic) and the cut's skins name roots as fallback destinations instead of literals; `wired` 2209 -> 2207 and rootsExcluded 778 -> 766 follow from the same two moves.",
  "skinFilesNote": "391 is the pinned figure and the walker measures 390 on `main` itself; the drift predates this lot, which adds and removes no skin file. No gate reads this field, so it is left as the record of when it was last taken rather than quietly re-anchored here."
}
