{
  "schemaVersion": 1,
  "law": "a family under the cut contract holds every BLOCKING arm of scripts/check/family-cut and never grows a pinned counter",
  "policy": "decrease-only: a pinned counter may shrink -- and must then be lowered here -- but may never grow",
  "roster": "a family enters this file when its cut work order opens and leaves the debt columns at 0 when the cut closes; `cut` names the owning work order",
  "families": {
    "button": {
      "cut": "WO-FAM-01",
      "calibratedBy": "WO-FAM-00",
      "denominators": {
        "skinFiles": 3,
        "sourceFiles": 6,
        "channelsRead": 253,
        "classTokens": 13,
        "partsStamped": 12,
        "partsConsumed": 11
      },
      "readWithoutProducer": 0,
      "classVocabularies": 1,
      "legacyNamespaceClasses": 0,
      "partsStampedNotConsumed": 1,
      "partsConsumedNotStamped": 0,
      "statesConsumedNotStamped": 0,
      "unpairedStatePseudoSelectors": 0,
      "colorLiteralsInSkin": 0,
      "fanOutUnreached": 1,
      "channelsReadNote": "252 -> 253 in connection lot 2: the button skin folds --ds-control-height-scale into the height it already resolves from the density scale. `readWithoutProducer` (34) does not move -- the channel is declared in `foundation/themes/default` and emitted by `derivation/shape/control-height`, so it is read AND written.",
      "cutNote": "WO-FAM-01: the 32 shadow channels and the label weight/family are emitted by derivation/chrome/button; the press recipe timing is stamped onto the button channels; one `ds-` vocabulary. Two pins stay above 0 for adjudication, never by a fabricated reader: partsStampedNotConsumed=1 is `trigger`, the default root part a composite may rename (P-79), so the skin anchors on `[data-variant]` and the part is read by the anatomy skeleton renderer instead; fanOutUnreached=1 is `recipe-profile`, whose only channel `--ds-recipe-profile` is provenance owned by WO-DER-06 while the profile reaches the button through the runtime."
    }
  }
}
