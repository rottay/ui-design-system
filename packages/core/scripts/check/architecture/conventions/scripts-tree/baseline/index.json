{
  "schemaVersion": 2,
  "law": "scripts/ converges on build, check, generate, maintain, package and libraries; capabilities use <intent>/<subdomain?>/<capability>/index.mjs and behavioral names",
  "policy": "decrease-only: listed transition debt may shrink but never grow",
  "entries": [
    {
      "rule": "R3-foreign-file",
      "path": "generate/theme-graph/derive.mjs",
      "reason": "module split of one subdomain, index.mjs remains the only entry point. Measured: this file carries no `process.argv[1]` entry guard, is imported only by `generate/theme-graph/index.mjs`, and is named by no package script and no gate-manifest row. Collapsing the three into the entry would make it a thousand-line file, which is a worse answer to the same convention: one capability, one entry, atomic units behind it. This half holds the five measured reads (catalog, deriver registry, dry-run, read census, skin tree)."
    },
    {
      "rule": "R3-foreign-file",
      "path": "generate/theme-graph/schema.mjs",
      "reason": "module split of one subdomain, index.mjs remains the only entry point. Measured: this file carries no `process.argv[1]` entry guard, is imported only by `generate/theme-graph/index.mjs`, and is named by no package script and no gate-manifest row. Collapsing the three into the entry would make it a thousand-line file, which is a worse answer to the same convention: one capability, one entry, atomic units behind it. This half holds the CascadeNode/CascadeEdge shape and the placeholder guard, and carries no data at all."
    },
    {
      "rule": "R3-foreign-file",
      "path": "generate/theme-graph/views.mjs",
      "reason": "module split of one subdomain, index.mjs remains the only entry point. Measured: this file carries no `process.argv[1]` entry guard, is imported only by `generate/theme-graph/index.mjs`, and is named by no package script and no gate-manifest row. Collapsing the three into the entry would make it a thousand-line file, which is a worse answer to the same convention: one capability, one entry, atomic units behind it. This half renders the two Markdown views and is a pure text-in/text-out function of the JSON views."
    },
    {
      "rule": "R8-single-child-subdomain",
      "path": "check/responsive",
      "reason": "domain slot with registered growth, not accidental nesting. `single-authority` is its first child, already registered in the gate manifest as the blocking `responsive-single-authority` plus its drill, and the pilot’s remaining responsive obligations (container, breakpoint) are registered for this same folder. Decrease-only applies: when the second child lands this entry goes STALE and the gate says so."
    }
  ]
}
