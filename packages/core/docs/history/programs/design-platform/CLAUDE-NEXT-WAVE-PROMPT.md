# Claude Next-Wave Prompt — finish OLA 5

Copy everything below the separator into Claude Code from
`/Users/daniel/Developer/Rottay`.

---

You are the code implementation owner finishing OLA 5 of the Rottay Design
Platform 10/10. Do not call this OLA 6. F3 is accepted, F2 is partial, and F4
and F5 have not started.

Use the newest available Opus model as the primary reasoning and coordination
model. If the client exposes that tier as “Fable”, use the newest Fable tier.
Delegate bounded read-only discovery and focused test analysis to the newest
Sonnet or Haiku tier to reduce token cost. Keep one writer per ownership area;
agents must never edit overlapping files.

Codex owns browser inspection, screenshot evidence, aesthetic judgment,
acceptance, ledger status and certified percentages. You own code,
deterministic fixtures, automated contracts and an exact inspection manifest.
Do not open browsers or self-certify visual quality.

Do not commit, push, open a PR, publish, reset, checkout, clean the dirty
worktree, discard unrelated changes or alter the existing stash. Run builds,
typechecks, aggregate tests and generated-artifact writes strictly one at a
time.

## Read completely before editing

Read these files in order:

1. `/Users/daniel/Developer/Rottay/ui-design-system/CLAUDE.md`
2. `/Users/daniel/Developer/Rottay/ui-design-system/roadmap/README.md`
3. `/Users/daniel/Developer/Rottay/ui-design-system/roadmap/registry.json`
4. `/Users/daniel/Developer/Rottay/docs-engineering/engineering/design-system/runtime/engines/modern/README.md`
5. `/Users/daniel/Developer/Rottay/ui-design-system/packages/core/docs/rottay-design-platform-10-10/MASTER-IMPLEMENTATION-PLAN.md`
6. `/Users/daniel/Developer/Rottay/ui-design-system/packages/core/docs/rottay-design-platform-10-10/SUPPLIER-ARCHITECTURE.md`
7. `/Users/daniel/Developer/Rottay/ui-design-system/packages/core/docs/rottay-design-platform-10-10/COMPONENT-LEDGER.md`
8. `/Users/daniel/Developer/Rottay/ui-design-system/packages/core/docs/rottay-design-platform-10-10/EXECUTION-BACKLOG.md`
9. `/Users/daniel/Developer/Rottay/ui-design-system/packages/core/docs/rottay-design-platform-10-10/CLAUDE-IMPLEMENTATION-RUNBOOK.md`
10. `/Users/daniel/Developer/Rottay/ui-design-system/packages/core/docs/rottay-design-platform-10-10/CODEX-AUDIT-PROTOCOL.md`
11. `/Users/daniel/Developer/Rottay/ui-design-system/packages/core/docs/rottay-design-platform-10-10/CODEX-AUDIT-OLA-5-2026-07-23.md`
12. `/Users/daniel/Developer/Rottay/ui-design-system/packages/core/docs/rottay-design-platform-10-10/CODEX-AUDIT-OLA-5-CONTINUATION-2026-07-23.md`
13. `/Users/daniel/Developer/Rottay/ui-design-system/packages/core/docs/rottay-design-platform-10-10/CODEX-AUDIT-OLA-5-F2-CLOSURE-2026-07-23.md`

`roadmap/registry.json` is the only execution-status authority. Do not change
ticket status, `ownerGo`, accepted ledger rows or certified counts. Leave those
decisions to Codex.

## Accepted baseline: preserve it

- Certified primitives: 14/92 (15.2%).
- Certified platform artifacts: 14/119 (11.8%).
- Tenant channels: 1,615 inventoried, 236 acknowledged dead.
- Ownership ratchet: 4,074 findings.
- Static first-party `BrandTheme` and DB-owned customer `appearance` compile
  through the same canonical `--ds-*` runtime.
- DaisyUI 5.5.19 remains an internal Modern projection bridge, never a public,
  tenant or visual authority.
- `SemanticSurface`, `surfaceRole`, `SemanticSurfaceRoleMap` and
  `BrandSurfaces.surfaceRoles` are the public surface vocabulary.
- OLA 5 Phase 1 is accepted as `DS-Q006 PARTIAL`.
- OLA 5 F3 / DS-A008 is accepted. Do not redesign its registry, duration
  mapping or reduced-motion policy without a focused failing regression.
- F2 is partial only because its deterministic same-tree proof is missing.
  The corrected compiler, root provider, typed density vocabulary and local
  scope separation are the accepted working authority.
- Density has three independent planes: structural `--ds-density-scale`,
  semantic global `--ds-density-mode-factor`, and nested local
  `data-density`. Do not derive semantic posture from structural scale and do
  not reapply a local factor at `:root`.
- The Arabic-safe suffix is a final compiler invariant for base, heading and
  display font families. Tenant-selected fonts remain first.
- The canonical `densityScopeAttributes()` helper is a verified non-paint prop
  bag. Do not replace it with ad-hoc writers or broaden its audit exemption.
- The corrected DataTable mobile anatomy, selector specificity and lazy-engine
  tests are accepted. Do not classify those fixes as stale or rebuild them.
- Modern `Text` without `textStyle` intentionally preserves its established
  `md` behavior. Do not silently make `body` or any semantic role implicit.
  A default migration requires a deterministic fixture and Codex sighted
  acceptance.

Do not rebuild accepted OLA 2–4 nuclei, OLA 5 Phase 1 or F3 merely because you
prefer another implementation.

## Non-negotiable architecture

- Rottay and BitHire use static typed `BrandTheme`.
- Customer tenants such as The Management use validated DB `appearance`.
- Both sources emit the same canonical `--ds-*` channels before rendering.
- The DS owns shared anatomy, accessibility, states, responsive behavior,
  motion and premium default craft.
- Applications own product composition, domain hierarchy, workflow and
  bounded local expression through public props, slots and documented hooks.
- Applications may not repair primitives, reach private anatomy, reconstruct
  controls in raw HTML, create a second token system or branch by tenant.
- All new user-facing copy uses i18n; EN, ES and AR/RTL are required.
- No generic colored left rails.
- Do not add a styling framework or behavior supplier in this continuation.
- Do not remove DaisyUI wholesale.

## Phase 0 — freeze ownership

Before editing:

1. capture HEAD, `git status --short` and `git stash list`;
2. inventory the exact files and ownership area for each writer;
3. identify overlap with the dirty worktree;
4. publish the serial command order;
5. stop only if the authoritative documents genuinely conflict.

## Phase 1 — close only the missing F2 proof

The F2 authority implementation has been remediated and accepted as the
working baseline. Do not rewrite the compiler, root provider, density CSS,
Arabic-safe helper or migrated consumers unless this proof exposes a focused
regression.

### Typography

- Prove the canonical roles `display`, `pageTitle`, `sectionTitle`, `body`,
  `supporting`, `label`, `caption`, `code`, `numeric` declared -> emitted ->
  explicitly consumed in a real Modern component.
- Prove tenant-selected Latin fonts remain first while the Arabic-safe suffix
  survives both static and DB compilation, including advanced overrides.
- Keep implicit Text and Heading behavior stable unless a dedicated fixture
  and migration contract prove the change.
- Prove wrapping, truncation, readable measure, long translations and
  tabular-number behavior.

### Density

- Create a deterministic same-tree fixture that exposes static `BrandTheme`,
  DB `appearance`, compiler output, CSS effective scale, root DOM metadata and
  JS context together.
- Prove structural brand scale remains independent from semantic posture.
- Prove semantic root posture is derived from Appearance and that DB
  Appearance overrides static Appearance.
- Prove nested `DensityScope` changes only its subtree and restores the parent
  posture without double multiplication.
- Prove visual density remains separate from layout-view preferences.
- Observe coordinated control/row height, type scale, padding, gap and
  icon/container size in the fixture.
- Keep the 44px coarse-pointer floor.
- Prove compact, comfortable and spacious on the same real DS tree under
  BitHire static and The Management DB, in EN, ES and AR/RTL.

Do not claim F2 closed unless the same-tree static-vs-DB proof and scoped
override/nesting proof both pass.

## Phase 2 — F4 two-pass P0 component tranche

Raise these six families:

1. Button and IconButton;
2. Input/TextField and FormField;
3. Card and SurfaceSectionCard;
4. Tabs;
5. Tag and Badge;
6. Tooltip and Popover chrome.

For every family perform both passes.

### Pass 1 — product contract and behavior

- one canonical public owner;
- bounded typed axes only where semantically real;
- public slots/data-parts only for legitimate composition;
- static and DB theming without markup forks;
- keyboard, pointer and touch parity;
- RTL and locale-safe content;
- disabled, loading, pending, error, empty and selected states as applicable;
- controlled/uncontrolled and form semantics where applicable;
- no supplier identity in public API, props or classes;
- no application-local repair needed for ordinary use.

### Pass 2 — adversarial craft

- authored hierarchy rather than uniform rectangles;
- coherent borders, radii, surface nesting and elevation;
- icon optical alignment and bounded icon-container geometry;
- clear header/body/footer segmentation where the anatomy needs it;
- semantic motion from the accepted F3 registry;
- smooth state transitions without layout jumps;
- long EN/ES/AR copy, extreme values, dense rows and multiline labels;
- narrow/mobile, coarse pointer, keyboard and reduced-motion states;
- no overlap, clipping, orphan controls or unexplained empty space;
- no generic colored left rails;
- opposing recipe profiles must look intentionally different without changing
  component markup.

Add deterministic Showroom fixtures for every family. Fixtures must expose
stable query parameters or test hooks for:

- BitHire static vs The Management DB;
- EN, ES and AR/RTL;
- compact vs spacious;
- default, hover/focus simulation where deterministic, disabled, loading and
  error states;
- hostile-content stress.

Do not take screenshots. Provide Codex an exact URL/query manifest and expected
observable differences.

## Phase 3 — F5 bounded Candidates canary

Only after F2 and all six F4 families are green:

- choose one bounded Candidates surface that uses several P0 families;
- replace app-local shared-chrome repair with public DS composition;
- preserve business logic, routes, permissions, i18n and data behavior;
- do not redesign the whole Candidates feature;
- do not add raw shared HTML controls, private DS selectors or a parallel token
  vocabulary;
- document every removed application exception and every remaining one.

Candidates proves reuse; it does not own the platform architecture.

## Tests and gates

Run focused tests while editing. At the end run these strictly serially:

1. affected focused suites;
2. application-boundary focused suite and live gate;
3. recipe/profile, typography, density and motion suites;
4. P0 real-engine and contract suites;
5. `pnpm --filter @rottay/design-system typecheck`;
6. `pnpm --filter @rottay/design-system pretest`;
7. only sanctioned generators reported stale by a gate;
8. rerun the affected gate after generation;
9. `git diff --check`.

Never run multiple builds, aggregate suites or typechecks concurrently. Do not
widen a baseline to make a gate pass.

## Required handoff

Return one exact handoff:

1. executive summary without claiming acceptance;
2. files changed by phase/family;
3. F2 authority diagram and same-tree proof;
4. P0 family matrix: Pass 1, Pass 2, tests, fixture URL;
5. static-vs-DB, locale, density and state matrix;
6. exact browser inspection manifest for Codex;
7. application exceptions removed/remaining;
8. serial command log with pass/fail counts;
9. generated artifacts and why;
10. known failures, debt and unverified claims;
11. dependency/provider changes: expected zero;
12. worktree and stash preservation;
13. proposed certifications for Codex, if any;
14. explicit confirmation of no commit/push/PR/publish.

Do not mark the goal complete. Stop only after the handoff is complete or one
specific blocker is documented with evidence.
