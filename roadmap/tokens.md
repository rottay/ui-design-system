---
title: "Design System Tokens Lane — Color/Token Engine and Generated Artifacts"
date: 2026-07-07
status: canonical
audience: ai-agent
sources:
  - roadmap/proposals.md (owner-review inbox; P-03, P-04, P-06 APPROVED 2026-07-07 — the source specs for this lane)
  - docs-engineering/archive/audits/2026-07-06-modern-engine-visual-audit-davila.md (evidence base: dead DaisyUI layer, hand-tuned ramps, unverified dark-surface contrast)
  - ui-design-system/CLAUDE.md (architecture contract: BrandTheme white-label chain, artifact snapshot law, engine table)
---

# Design System Tokens Lane

This lane carries the **color/token engine** work the owner approved from the proposals inbox
on 2026-07-07 (P-03, P-04, P-06 — the token-and-generation slice of round 1). Where the
`engine-modern` lane fixes the WIRING of the modern engine (tokens, dark elevation, states,
effects, drains, galleries), this lane re-engineers the token PIPELINE and VALUES themselves:
every first-party artifact becomes a mechanical build product (TOK-01), the color layer becomes
an OKLCH derivation from tenant seeds so ANY tenant palette yields an even, contrast-verified
ramp without per-tenant design work (TOK-02), and the dead DaisyUI layer is retired so the
architecture docs stop lying about what modern is (TOK-03). Each WO builds on machinery that
ALREADY exists on disk (the bithire artifact generator, `compileBrandTheme`, the tint scale, the
contrast validator) — the pattern of this repo is consolidate-and-elevate, not green-field.

The three approved proposals map 1:1 to the three WOs: TOK-01 from P-06, TOK-02 from P-04,
TOK-03 from P-03. Proposal P-07 (tenant-independent light/dark axis) stays **WITHDRAWN**
(owner 2026-07-07): there is no user-facing light/dark toggle; each tenant defines ONE palette
and correctness on that surface is owned by WO-ENG-03 (elevation) and WO-TOK-02 (ramp
derivation per tenant palette). Nothing in this lane may reintroduce a scheme axis.

**Relationship to the `engine-modern` lane.** TOK-02 deliberately waits for WO-ENG-06 (color
purity — components must consume tokens before the token VALUES are re-engineered) and TOK-03
waits for WO-ENG-08 (the theme.css drain deletes the dead DaisyUI mappings before the
dependency is dropped). Where this lane touches files the ENG lane also edits
(`compilers/brand-theme`, `tokens/css/artifacts/**`, `scripts/engine-token-audit.mjs`), the
orchestrators **coordinate windows before claiming** — the same no-conflict discipline the
README applies to the external co-editor. The `scripts/engine-token-audit.mjs` ratchet (created
by WO-ENG-01) is the lane-wide mechanical gate family; WOs here EXTEND it with new counters,
never fork it.

**External co-editor (binding).** app-evnto WO-IDN-06 extends the SAME
`packages/core/scripts/build-vertical-artifacts.mjs` and generator spec that WO-TOK-01
generalizes. The written ordering law is in WO-TOK-01's coordination note: whichever of the two
executes second re-verifies parity before certifying.

Lane-wide rules (binding on every WO):

- Repo: `/Users/daniel/Developer/Rottay/ui-design-system` (macOS, pnpm). All paths below are
  relative to the repo root unless prefixed. Token/compiler source lives in `packages/core/src/`;
  the render surface is `packages/showroom`.
- **Gates are truth.** A WO is done when its acceptance gate passes AND the DS build/tests are
  green: `pnpm --filter @rottay/design-system run build` + `pnpm test`. Executors are
  **edit-only**; the orchestrator certifies and commits. No WO commits.
- **The DS is a published package.** Editing `packages/core/src` does NOT change what any
  consuming app renders until a release + repin. No WO in this lane publishes or repins;
  app-bithire is live, pins published DS versions, and repins deliberately — any tenant-visible
  value delta this lane produces is recorded for the release note (the WO-ENG-11 pattern).
- **Sighted check is mandatory for every visual WO.** Run the showroom
  (`pnpm --filter @rottay/showroom run dev`, http://localhost:7001), capture the affected
  surfaces before/after under **both tenant palettes — a dark-surface tenant (rottay,
  `--ds-color-bg-primary: #0A0A0C`) AND a light-surface tenant (bithire or evnto)** — to
  `test-artifacts/tokens/<wo>/`, then actually LOOK at the PNGs and score them. There is **no
  user-facing light/dark toggle** (owner decision 2026-07-07): the tenant palette decides the
  surface, so both captures come from switching tenant via nested `DesignSystemProvider` columns
  (the pattern `/foundations/engines` already uses). The owner approves signature moments —
  never self-approve visuals.
- **Anti-sprawl.** New work = a new `### WO-TOK-NN` block in this file + a `registry.json` entry
  (`pnpm roadmap:check` forces the pairing). No new plan documents. Statuses change ONLY via
  `scripts/roadmap-status.mjs`.
- No emojis anywhere. Repo docs in English. Never `git checkout/restore/reset` on directories.
  `app-bithire`, the other app repos, and `docs-engineering` are READ-ONLY references in this
  lane (build DS capability; never write into them). The showroom dev server is allowed.
- **Tenant-artifact rule (inherited from the engine lane).** `tokens/css/artifacts/**` are
  `compileBrandTheme`-generated snapshots of `tokens/ts/brand-themes/*.ts` (+ each artifact's
  declared `_source/extension.css` once TOK-01 lands). Any WO that changes a `--ds-*` token the
  compiler emits, or that appears in an artifact, must update the compiler/BrandTheme mapping,
  regenerate the artifact snapshots in the same WO, and keep the `compilers/brand-theme` test
  suite green. A token change that leaves a stale artifact override is a gate failure.

Ordering is by leverage: TOK-01 first — it has no dependencies and is the regeneration pipeline
every later token change (here and in the ENG lane) ships through; TOK-02 after WO-ENG-06 +
TOK-01; TOK-03 after WO-ENG-08.

---

## WORK ORDERS

### WO-TOK-01 Single-source tokens: artifacts always generated + DTCG evaluation
- **Outcome** — Every first-party tenant CSS artifact on disk (rottay, bithire, evnto) is a build product regenerated by `packages/core/scripts/build-vertical-artifacts.mjs` from its authored sources (`tokens/ts/brand-themes/<slug>.ts` compiled via `compileBrandTheme` + a declared `_source/extension.css`), hand-editing any artifact fails a CI-chained `--check` gate, the bithire-only parity/staleness guard tests are generalized to all first-party slugs, the manual "keep .ts and CSS in sync" law is deleted from the repo docs — and a WRITTEN DTCG (W3C Design Tokens) adopt/defer decision record with explicit criteria is appended to this WO block. Zero rendered-value change.
- **Why** — P-06 (proposals.md round 1, APPROVED 2026-07-07): manual-sync laws rot; this repo already proved it with theme.css. Verified state: the generator exists but is truthful only for bithire — `packages/core/scripts/build-vertical-artifacts.mjs` states in its header "Scope: bithire only (WO-DES-02). evnto/rottay stay hand-authored until their own themes reach parity", and `FIRST_PARTY_ARTIFACT_SPECS` (`packages/core/src/runtime/tenant/storage/static/generator/index.ts:744`) lists only `bithire`. Meanwhile `packages/core/src/tokens/css/artifacts/rottay/index.css` (3,099 lines) and `evnto/index.css` (423 lines) carry a "GENERATED SNAPSHOT" header while being hand-authored — neither has a `_source/` directory and neither is in the spec list; the headers lie today (`bithire/index.css`, 6,052 lines, is the only true build output: "GENERATED — do not edit"). The `.ts` side carries the rot-prone manual law verbatim: `packages/core/src/tokens/ts/brand-themes/rottay.ts:13` says "This file MUST stay in sync with the CSS artifact", and `ui-design-system/CLAUDE.md` codifies it as a repo rule. The `--check` gate is already wired (`"lint:artifacts": "node scripts/build-vertical-artifacts.mjs --check"` at `packages/core/package.json:103`, chained into `lint` at :102) but only ever checks bithire, and the guard tests exist only for bithire (`packages/core/src/tokens/__tests__/bithire-artifact-generated.test.ts`, `bithire-artifact-parity.test.ts`). No platform artifact exists on disk (verified: `artifacts/` = bithire, evnto, rottay — platform is the sanctioned DB-branding vertical), so the lane scope is the three artifacts that exist. No DTCG JSON exists anywhere under `packages/core/src/tokens/` (verified: `css/`, `ts/`, `compat/`, tests only).
- **Depends on** — none. Unlocks WO-TOK-02 (its ramps regenerate through this pipeline). **CROSS-REPO COORDINATION (WRITTEN, binding)** — app-evnto WO-IDN-06 extends the SAME `packages/core/scripts/build-vertical-artifacts.mjs` + `FIRST_PARTY_ARTIFACT_SPECS` (evnto vertical registration/openness). Ordering law: **whichever of WO-TOK-01 and WO-IDN-06 executes second re-verifies parity** — before certifying, the second-lander runs `node packages/core/scripts/build-vertical-artifacts.mjs --check` plus the artifact guard test suite against the first-lander's landed state and reconciles any divergence in the generator/spec files. The two orchestrators coordinate the window on `scripts/build-vertical-artifacts.mjs` and `src/runtime/tenant/storage/static/generator/index.ts` so the two programs never edit them concurrently.
- **Steps** —
  1. Register rottay + evnto in the pipeline: add their specs to `FIRST_PARTY_ARTIFACT_SPECS` (`packages/core/src/runtime/tenant/storage/static/generator/index.ts:744`, using `buildTenantSelector`/an explicit selector matching each artifact's current scoping — rottay's is `html[data-tenant='rottay']:not([data-theme='light']):not(.light)`) and to the `BRAND_THEMES` map in `packages/core/scripts/build-vertical-artifacts.mjs:43-45`; delete the "Scope: bithire only" header limitation.
  2. Carve the authored sources for rottay and evnto: split each current hand-authored `index.css` into (a) what `compileBrandTheme` can express — reconcile `packages/core/src/tokens/ts/brand-themes/{rottay,evnto}.ts` so the compiled block reproduces the current rendered values — and (b) the residue (dark/light-scoped blocks, color scales, semantic sets, chrome the compiler does not yet generate) into new `src/tokens/css/artifacts/{rottay,evnto}/_source/extension.css` files (to be created, mirroring `artifacts/bithire/_source/extension.css`). Regenerate; parity target = identical rendered VALUES (the byte layout moves to the generated form; the sighted check proves zero visual delta).
  3. Delete the manual-sync law everywhere it is written: the "MUST stay in sync" comments in `tokens/ts/brand-themes/rottay.ts` (and any sibling), and the `ui-design-system/CLAUDE.md` bullet "BrandTheme `.ts` files MUST stay in sync with CSS artifacts. If you edit one, update the other" — replaced by the generated-only law: artifacts are build products; regenerate with `pnpm -C packages/core build:vertical-css`; hand-edits fail `lint:artifacts`.
  4. Generalize the guard tests: parameterize `bithire-artifact-generated.test.ts` + `bithire-artifact-parity.test.ts` over `FIRST_PARTY_ARTIFACT_SPECS` (rename to a first-party scope, e.g. `first-party-artifacts-*.test.ts`) so every registered slug gets the generated-header + staleness assertions.
  5. Verify the CI chain actually fails on hand-edits for ALL slugs: `lint:artifacts` runs inside `pnpm --filter @rottay/design-system run lint` (`packages/core/package.json:102-103`); if the repo verification path that certifies WOs (`pnpm test` / CI) does not reach `lint`, chain the check the way WO-ENG-01 chained `engine-audit:check` (a `pretest` hook or test-script chaining in `packages/core/package.json`).
  6. DTCG evaluation (decision record, not code): assess emitting/accepting the W3C Design Tokens (DTCG) JSON format (`$value`/`$type` interchange) as a converter over the `compileBrandTheme` output. Score these criteria in writing: (a) does a concrete consumer exist today (Figma/Tokens Studio sync need in any Rottay program)? (b) DTCG spec stability at evaluation date; (c) implementation cost (a converter over existing compiled variables — never a rewrite of BrandTheme); (d) whether the gates/architecture lanes need the interchange layer. Verdict = ADOPT (the converter then enters the proposals inbox as a new item — NOT scope creep inside this WO) or DEFER (with a dated re-evaluation trigger). Append the dated record as a "DTCG decision record (YYYY-MM-DD)" subsection at the end of this WO block in `roadmap/tokens.md`.
  7. Sighted proof: rottay + evnto flagship captures before/after regeneration under both tenant palettes (dark-surface rottay AND light-surface evnto or bithire) showing zero visual delta.
- **Files** — `packages/core/scripts/build-vertical-artifacts.mjs`, `packages/core/src/runtime/tenant/storage/static/generator/index.ts`, `packages/core/src/tokens/ts/brand-themes/rottay.ts` + `evnto.ts`, `packages/core/src/tokens/css/artifacts/rottay/**` + `evnto/**` (regenerated `index.css` + new `_source/extension.css`, to be created), `packages/core/src/tokens/__tests__/**` (generalized guard tests), `packages/core/package.json` (only if the check needs test-chain wiring), `ui-design-system/CLAUDE.md` (the manual-sync law bullet), `roadmap/tokens.md` (the DTCG decision record appended to this block).
- **Acceptance gate** — `node packages/core/scripts/build-vertical-artifacts.mjs --check` green for all three slugs AND proven to fail on a hand-edit (scratch-edit one artifact line, observe the failure, revert that single file — never `git restore` a directory); the generalized guard tests run green inside `pnpm test`; `pnpm --filter @rottay/design-system run build` + `pnpm test` + `pnpm --filter @rottay/design-system run lint` green; sighted before/after in `test-artifacts/tokens/tok-01/` under both tenant palettes showing zero visual delta; the DTCG decision record exists in this file with an explicit ADOPT/DEFER verdict scored against the four criteria.
- **Do NOT** — Do not change any rendered token VALUE (this WO changes generation, not design — WO-TOK-02 owns value re-engineering). Do not create a platform artifact (none exists; platform is the DB-branding vertical). Do not hand-edit any artifact after registration (regenerate instead). Do not edit `scripts/build-vertical-artifacts.mjs` or the generator spec while app-evnto WO-IDN-06 is mid-flight (coordinate the window; the second-lander re-verifies parity). Do not implement a DTCG converter inside this WO (decision record only). Never `git restore` directories.
- **Size** — M.
- **Delegation prompt** — In `/Users/daniel/Developer/Rottay/ui-design-system`, make ALL first-party tenant artifacts generated build products per proposal P-06 (`roadmap/proposals.md`, APPROVED 2026-07-07). Today only bithire is truly generated: `packages/core/scripts/build-vertical-artifacts.mjs` says "Scope: bithire only" and `FIRST_PARTY_ARTIFACT_SPECS` (`packages/core/src/runtime/tenant/storage/static/generator/index.ts:744`) lists one slug, while `src/tokens/css/artifacts/rottay/index.css` (3,099 lines) and `evnto/index.css` (423 lines) are hand-authored under a lying "GENERATED SNAPSHOT" header. (1) Register rottay + evnto in `FIRST_PARTY_ARTIFACT_SPECS` and the script's `BRAND_THEMES` map (selectors match current artifact scoping; rottay's is `html[data-tenant='rottay']:not([data-theme='light']):not(.light)`); drop the bithire-only header. (2) Carve each hand-authored artifact into its authored sources: reconcile `tokens/ts/brand-themes/{rottay,evnto}.ts` for what `compileBrandTheme` expresses, move the residue into new `artifacts/{rottay,evnto}/_source/extension.css` (mirror `artifacts/bithire/_source/`), regenerate to identical rendered VALUES. (3) Delete the manual-sync law: the "MUST stay in sync" comment in `tokens/ts/brand-themes/rottay.ts:13` and the matching `ui-design-system/CLAUDE.md` bullet — replace with the generated-only rule (regenerate via `pnpm -C packages/core build:vertical-css`; hand-edits fail `lint:artifacts`). (4) Generalize `packages/core/src/tokens/__tests__/bithire-artifact-{generated,parity}.test.ts` over all registered slugs. (5) Ensure the `--check` gate (`lint:artifacts`, `packages/core/package.json:103`) runs on the certification path; chain into `pretest`/the test script if `pnpm test` does not reach `lint`. (6) Write the DTCG adopt/defer decision record (criteria: concrete consumer today, spec stability, converter-only cost, gates/architecture need) as a dated subsection appended to the WO-TOK-01 block in `roadmap/tokens.md`; if ADOPT, the converter becomes a new proposals-inbox item, not work here. CROSS-REPO: app-evnto WO-IDN-06 extends the same script/spec — coordinate the window; whichever program lands second re-runs `--check` + the guard suite and reconciles. Gate: `node packages/core/scripts/build-vertical-artifacts.mjs --check` green for all three slugs and proven to fail on a scratch hand-edit (revert the single file); generalized guard tests green in `pnpm test`; `pnpm --filter @rottay/design-system run build` + `pnpm test` + `run lint` green; sighted before/after in `test-artifacts/tokens/tok-01/` under rottay (dark surface, `#0A0A0C`) AND a light-surface tenant (bithire or evnto) with zero visual delta — run the showroom (`pnpm --filter @rottay/showroom run dev`, http://localhost:7001) and LOOK at the PNGs. Fences: zero rendered-value change; no platform artifact; no DTCG converter code; edit-only, no commits, never git-restore directories, app-bithire and docs-engineering read-only, showroom dev server allowed.

- **AMENDMENT 2026-07-09 (orchestrator, under the owner's delegated approval)** — Surveyed against the code. The HEADLINE premise is VERIFIED TRUE, unlike the three WOs whose premises collapsed under survey: `FIRST_PARTY_ARTIFACT_SPECS` lists only bithire, `rottay/index.css` (3,116 lines) and `evnto/index.css` (423 lines) carry lying "GENERATED SNAPSHOT" headers with no `_source/` directory, the manual-sync law is real, no DTCG JSON exists, and `pretest` does not run the artifact `--check`. The evnto + spec-registration + guard-generalization + DTCG + pretest work is READY.
  **Where the spec rests on a premise the code contradicts:** the WO treats rottay and evnto symmetrically and tells step 2 to make each compiled block "reproduce the current rendered VALUES". It cannot for rottay. rottay is DARK-FIRST (its artifact's default block renders `--ds-color-primary:#FFFFFF` on `#0A0A0C`), while `compileBrandTheme` structurally emits the LIGHT palette as the default block (`brandThemeToCssVariables` sets `--ds-color-primary = palette.primaryColor = #0A0A0A`; dark values exist only as `--ds-color-dark-*` aliases). evnto is light-first and maps cleanly; rottay does not. **No gate catches this** — the artifact would render correctly while its compiled palette was dead.
  **Amended plan**: (a) execute evnto independently, it is clean; (b) for rottay, mandate the compiled block as the LIGHT base with the extension re-scoping the dark default, and record the dead-compiled-palette caveat as a WO-TOK-02 precondition — do NOT flip `rottay.ts` to dark semantics, because `rottayBrandTheme` is a LIVE registered tenant (`runtime/tenant/registry/index.ts:51`), not an artifact-only fixture, so flipping it changes runtime rendering and breaks out-of-scope suites (`i0-inventory.test.ts:283`, `premium-regression.test.ts`); (c) require the SIGHTED capture on evnto, not bithire — evnto has zero visual baselines, so it is the surface where a regression would go unseen.
  **Line drift in the spec above** (fix on next edit, recorded here so an auditor is not misled): `FIRST_PARTY_ARTIFACT_SPECS` is at `generator/index.ts:758`, not `:744`; the rottay sync-law comment is at `rottay.ts:12`, not `:13`.

### WO-TOK-02 OKLCH color engine with generated ramps
- **Outcome** — `compileBrandTheme` derives perceptually-even OKLCH color ramps from each tenant's seed colors, with **ramp derivation per tenant palette**: the derivation keys on the tenant's OWN surface (a dark-surface tenant like rottay gets a dark-tuned ramp; a light-surface tenant gets a light-tuned one — there is NO light/dark toggle, owner decision 2026-07-07). State shades mix in a perceptual space, artifacts emit P3 wide-gamut values with sRGB fallbacks, every generated text/surface pairing is APCA-checked at compile time, and all first-party artifacts regenerate through the WO-TOK-01 pipeline. Any tenant seed color yields a full, even, contrast-verified palette mechanically — no per-tenant design work.
- **Why** — P-04 (proposals.md round 1, APPROVED 2026-07-07): the biggest whitelabel win. Verified state of the color layer: the compiler passes tenant seed hex straight through (`packages/core/src/compilers/brand-theme/index.ts:183-195` sets `--ds-color-primary`/`-secondary`/`-accent`/status vars verbatim from the palette), and dark values are separate HAND-PICKED fields with no derivation (`darkPrimaryColor`/`darkSecondaryColor`/`darkAccentColor`/`darkBackgroundColor` at index.ts:110-113 and :192-195 — `tokens/ts/brand-themes/rottay.ts` hand-picks `#FFFFFF`/`#A0A0A5` over the `#0C0C0E` canvas). The foundation ramp `--ds-color-primary-50..900` is hand-tuned hex (`packages/core/src/tokens/css/foundation/themes/default.css:20-25+`, `50:#fafafa ... 500:#171717`). The interaction tint scale `--ds-tint-{4,8,12,16,24}` mixes in NON-perceptual sRGB (`setTintScaleVariables`, index.ts:345-364: `color-mix(in srgb, var(<role>) N%, var(--ds-color-bg-primary))`). The only contrast machinery is WCAG-ratio (`contrastRatio` at `packages/core/src/_internal/a11y/contrast/index.ts:219`, AA 4.5/3 pairs at :331-338) — no APCA anywhere, and OKLCH appears only in a compiler-test fixture (`compilers/brand-theme/tests/brand-compiler.test.ts:321`). Consequence: an off-palette tenant seed yields uneven tints and unverified dark-surface contrast, so "whitelabel per tenant" still needs per-tenant color design.
- **Depends on** — WO-ENG-06 (color purity: components consume tokens before the token VALUES are re-engineered), WO-TOK-01 (ramps regenerate mechanically through the artifact pipeline). Coordination: WO-GAT-04 (accessibility CI) consumes the APCA scorer built here — whichever lands second REUSES the scorer, never duplicates it; this WO owns compile-time pairing checks on generated ramps, GAT-04 owns the per-state gallery CI. The `engine-token-audit.mjs` counter added here extends the WO-ENG-01 ratchet (lane law: extend, never fork).
- **Steps** —
  1. OKLCH math in-repo: create `packages/core/src/_internal/color/oklch/` (to be created) with sRGB<->OKLCH conversion, lightness/chroma ramp interpolation, and sRGB gamut mapping — zero new runtime dependency; unit-test round-trips against known pairs.
  2. Ramp derivation in `compileBrandTheme` (`packages/core/src/compilers/brand-theme/index.ts`): from each palette role seed (primary/secondary/accent + success/warning/error/info), derive a perceptually-even OKLCH lightness ramp keyed to the tenant surface — compute the surface luminance from the tenant palette (`darkBackgroundColor`/surfaces, else the light default) and order/tune the steps for that surface. Emit as `--ds-color-{role}-{50..900}` tenant-scoped overrides; the hand-tuned neutral ramp at `foundation/themes/default.css:20+` stays as the tenant-less default. The existing hand-picked `dark*` palette fields become OPTIONAL overrides honored over the derivation (back-compat: no BrandTheme contract break).
  3. Perceptual state shades: re-express `setTintScaleVariables` (index.ts:345-364) to mix in a perceptual space (`color-mix(in oklab, ...)`), keeping the token NAMES, the five steps `--ds-tint-{4,8,12,16,24}`, and the role suffixes exactly as they are (bithire's one-blue law consumes these names; only the mixing space changes).
  4. P3 with sRGB fallbacks: extend `renderVerticalArtifact` (`packages/core/src/runtime/tenant/storage/static/generator/index.ts`) so artifacts emit sRGB values as the base declarations plus a `@supports (color: oklch(0% 0 0))` block (and `@media (color-gamut: p3)` where wide gamut matters) carrying the `oklch()` values — fallback-first, enhancement-second, per artifact.
  5. APCA pairing checks: add an APCA (Lc) scorer alongside `contrastRatio` in `packages/core/src/_internal/a11y/contrast/index.ts` and wire a compile-time check into `packages/core/scripts/build-vertical-artifacts.mjs`: every generated text/surface pairing per tenant must pass its Lc threshold or the build fails. Add a compiler test proving a deliberately-failing seed fails.
  6. Ratchet: extend `scripts/engine-token-audit.mjs` with a derived-ramp counter — per-step ramp hex hand-authored in any `_source/extension.css` or brand-theme `.ts` (beyond seeds and the sanctioned `dark*` overrides) — baseline measured at implementation, target 0, decrease-only.
  7. Regenerate all first-party artifacts via the TOK-01 pipeline; `compilers/brand-theme` suite + generalized artifact guard tests green.
  8. Sighted proof: flagship galleries before/after under both tenant palettes (dark-surface rottay AND light-surface bithire or evnto), scored for even tint progression, readable text at every step, and no muddied brand color. app-bithire pins DS versions and repins deliberately — record any perceptible bithire-tenant delta for the release note (the WO-ENG-11 pattern).
- **Files** — `packages/core/src/_internal/color/oklch/**` (new), `packages/core/src/compilers/brand-theme/index.ts` + `compilers/brand-theme/tests/**`, `packages/core/src/_internal/a11y/contrast/index.ts`, `packages/core/src/runtime/tenant/storage/static/generator/index.ts`, `packages/core/scripts/build-vertical-artifacts.mjs` (pairing-check hook), `packages/core/src/tokens/ts/brand-themes/*.ts` (only where seeds/overrides reconcile), `packages/core/src/tokens/css/artifacts/**` (regenerated snapshots), `scripts/engine-token-audit.mjs` (derived-ramp counter), a `packages/showroom` capture page.
- **Acceptance gate** — `pnpm --filter @rottay/design-system run build` + `pnpm test` green (including OKLCH round-trip tests and the failing-seed APCA test); `node packages/core/scripts/build-vertical-artifacts.mjs --check` green with the APCA pairing check active; `node scripts/engine-token-audit.mjs --check` green with the derived-ramp counter at its baseline (decrease-only, target 0); per the tenant-artifact rule, all artifacts regenerated in this WO with the `compilers/brand-theme` suite and the artifact guard tests green; sighted before/after in `test-artifacts/tokens/tok-02/` under both tenant palettes showing even ramps/tints and passing text on both surfaces; any bithire-visible delta recorded for the release note.
- **Do NOT** — Do not rename or drop the `--ds-tint-*` step names or role suffixes (consumed law). Do not build a light/dark scheme axis or honor a `data-theme` toggle (owner 2026-07-07: one palette per tenant; derivation keys on the tenant surface — P-07 stays withdrawn). Do not add a color-math runtime dependency. Do not hand-tune per-tenant ramp steps to fix a bad output (fix the derivation). Do not duplicate the APCA scorer when WO-GAT-04 lands (share it). Do not break the BrandTheme contract (dark* fields stay as optional overrides). Never `git restore` directories.
- **Size** — L.
- **Delegation prompt** — In `/Users/daniel/Developer/Rottay/ui-design-system`, build the OKLCH color engine from proposal P-04 (`roadmap/proposals.md`, APPROVED 2026-07-07). Depends on WO-ENG-06 (color purity) + WO-TOK-01 (generated artifacts) being done. Today the compiler passes seed hex through verbatim (`packages/core/src/compilers/brand-theme/index.ts:183-195`), dark values are hand-picked fields (index.ts:110-113/:192-195), the foundation ramp is hand-tuned hex (`tokens/css/foundation/themes/default.css:20-25+`), tints mix in sRGB (`setTintScaleVariables` index.ts:345-364), and contrast is WCAG-only (`_internal/a11y/contrast/index.ts:219`). (1) Create `packages/core/src/_internal/color/oklch/` (sRGB<->OKLCH conversion, ramp interpolation, gamut mapping; NO new dependency; round-trip unit tests). (2) In `compileBrandTheme`, derive perceptually-even OKLCH ramps `--ds-color-{role}-{50..900}` per palette role from the tenant seeds, keyed to the tenant's OWN surface luminance (dark-surface tenants like rottay, canvas `#0C0C0E`, get dark-tuned ramps; NO light/dark toggle — owner 2026-07-07; the hand-picked `dark*` fields stay as optional overrides). The foundation neutral ramp stays as the tenant-less default. (3) Re-express `setTintScaleVariables` to `color-mix(in oklab, ...)` keeping names/steps/suffixes of `--ds-tint-{4,8,12,16,24}` exactly. (4) Extend `renderVerticalArtifact` (`runtime/tenant/storage/static/generator/index.ts`) to emit sRGB base declarations + a `@supports (color: oklch(0% 0 0))` / `@media (color-gamut: p3)` enhancement block per artifact. (5) Add an APCA (Lc) scorer alongside `contrastRatio` in `_internal/a11y/contrast/index.ts` and wire a compile-time pairing check into `scripts/build-vertical-artifacts.mjs` (build fails on a failing pairing; add a failing-seed test); WO-GAT-04 will reuse this scorer — do not fork it. (6) Extend `scripts/engine-token-audit.mjs` with a derived-ramp counter (hand-authored per-step ramp hex in `_source/extension.css`/brand-theme `.ts` beyond seeds + `dark*` overrides; baseline now, target 0, decrease-only). (7) Regenerate all artifacts via the TOK-01 pipeline; keep `compilers/brand-theme` + artifact guard tests green. Gate: `pnpm --filter @rottay/design-system run build` + `pnpm test` green; `node packages/core/scripts/build-vertical-artifacts.mjs --check` green with APCA active; `node scripts/engine-token-audit.mjs --check` green; sighted before/after in `test-artifacts/tokens/tok-02/` under rottay (dark surface) AND a light-surface tenant (bithire or evnto) showing even ramps and readable text — run the showroom (`pnpm --filter @rottay/showroom run dev`, http://localhost:7001) and LOOK at the PNGs; record any bithire-visible delta for the release note. Fences: keep `--ds-tint-*` names; no scheme axis/data-theme toggle; no new runtime dependency; edit-only, no commits, never git-restore directories, app-bithire and docs-engineering read-only, showroom dev server allowed.

### WO-TOK-03 Retire the dead DaisyUI layer from modern
- **Outcome** — DaisyUI is gone from the DS: the residual DaisyUI class consumers among the modern engines are re-expressed on the rottay-native class/token pattern, the `@plugin "daisyui"` build stage is removed, the `daisyui` devDependency is dropped (lockfile regenerated), and every registry/contract/doc string that defines modern as the "DaisyUI/Tailwind wrapper" is corrected to what it actually is — the Rottay-native premium skin. Zero visual delta on every touched component.
- **Why** — P-03 (proposals.md round 1, APPROVED 2026-07-07): the 2026-07-06 audit measured ~zero DaisyUI classes in rendered output while the docs still define modern as the DaisyUI wrapper — untruthful architecture docs, a dead dependency, and a standing invitation to re-diverge. Verified: `daisyui: ^5.5.19` is a devDependency (`packages/core/package.json:285`) consumed by the `build:modern-css` stage — `packages/core/src/tokens/css/engines/modern/compiled.css` (14 lines) declares `@import "tailwindcss"; @plugin "daisyui";` and scans the modern engine sources, and `"build:modern-css": "postcss src/tokens/css/engines/modern/compiled.css -o dist/modern-engine.css"` runs inside `build` (`packages/core/package.json:87-88`). The DaisyUI identity persists in `packages/core/src/runtime/engines/registry.ts:60-61` (`displayName: 'Modern (DaisyUI)'`, `library: 'daisyui'`), `packages/core/src/contracts/engine/index.ts:11` ("**modern**: DaisyUI/Tailwind") and :72 (the `library` doc), `packages/core/src/_internal/testing/system/tests/engines.test.ts`, the `ui-design-system/CLAUDE.md` engine table ("modern — Tailwind / DaisyUI wrapper"), and nine modern engine files that still reference DaisyUI classes or docs (verified grep: Segmented — a REAL `join`-class consumer per its own header — plus Stepper, Steps, Link, Breadcrumb, Rate, Progress, Skeleton, Timeline).
- **Depends on** — WO-ENG-08 (the theme.css drain deletes the dead DaisyUI class mappings and proves which class hooks have real consumers; this WO removes the dependency, the residue, and the doc claims). Coordination: ENG-08's consumer-proof gate in `scripts/engine-token-audit.mjs` is the authoritative inventory of surviving class hooks — re-express exactly those.
- **Steps** —
  1. Inventory residual DaisyUI class usage post-ENG-08: sweep the nine modern engine files above plus the built `dist/modern-engine.css` for DaisyUI-generated classes that actually render (e.g. Segmented's `join` grouping and any size-class maps); cross-check against ENG-08's consumer-proven selector list.
  2. Re-express each residual consumer on the rottay-native pattern the rest of the modern engine uses (`rottay-*` classes + `--ds-*` tokens, per the component CSS-variable pattern in `ui-design-system/CLAUDE.md`); behavior and props stay identical — this is a styling re-home, not a redesign.
  3. Remove `@plugin "daisyui"` from `packages/core/src/tokens/css/engines/modern/compiled.css` (Tailwind STAYS — it still generates the utility classes the modern engines scan for); confirm `pnpm --filter @rottay/design-system run build:modern-css` emits a working `dist/modern-engine.css`.
  4. Drop `daisyui` from `packages/core/package.json` devDependencies (:285) and regenerate the lockfile (`pnpm install`, then predict CI with a local `--frozen-lockfile` install — the lockfile ships with the WO).
  5. Correct the identity strings: `runtime/engines/registry.ts:60-61` (`displayName` to `'Modern (Rottay Premium)'`; `library` to a native value, sweeping every consumer of the `library` field), the `contracts/engine/index.ts:11` and :72 doc comments, the expectations in `_internal/testing/system/tests/engines.test.ts`, and the `ui-design-system/CLAUDE.md` engine table entry to "modern — Rottay-native premium skin".
  6. Cross-repo doc note: `docs-engineering/engineering/design-system/runtime/engines/` still describes modern as the DaisyUI wrapper — `docs-engineering` is READ-ONLY in this lane; record in the WO handoff that the orchestrator updates that README (and the hub engine table) at certification, in the same session per the monorepo documentation rule.
  7. Repo-wide sweep: `grep -ri daisyui` over `packages/core/src`, `packages/core/package.json`, and `packages/showroom/src` returns 0 hits; historical records (`roadmap/proposals.md`, archived audits) stay untouched — they are records, not claims.
  8. Sighted before/after of the nine re-expressed components plus a flagship set under both tenant palettes: zero visual delta.
- **Files** — `packages/core/package.json` + `pnpm-lock.yaml` (dependency drop + lockfile), `packages/core/src/tokens/css/engines/modern/compiled.css`, the nine modern engine files (`components/primitives/navigation/{Segmented,Stepper,Steps,Link,Breadcrumb}/engines/modern.tsx`, `components/primitives/feedback/{Rate,Progress,Skeleton}/engines/modern.tsx`, `components/primitives/display/Timeline/engines/modern.tsx`), `packages/core/src/runtime/engines/registry.ts`, `packages/core/src/contracts/engine/index.ts`, `packages/core/src/_internal/testing/system/tests/engines.test.ts`, `ui-design-system/CLAUDE.md` (engine table), `packages/showroom/src/**` (only where a page names DaisyUI).
- **Acceptance gate** — `grep -ri daisyui packages/core/src packages/core/package.json packages/showroom/src` returns 0; `pnpm --filter @rottay/design-system run build` green (including `build:modern-css` emitting a working `dist/modern-engine.css` without the plugin) + `pnpm test` green, with the regenerated lockfile passing a local `--frozen-lockfile` install; `node scripts/engine-token-audit.mjs --check` green (no counter rises); sighted before/after in `test-artifacts/tokens/tok-03/` under both tenant palettes showing zero visual delta on the re-expressed components; the docs-engineering follow-up is recorded in the handoff.
- **Do NOT** — Do not touch the classic (antd) or rustic engines. Do not restyle while re-expressing (zero visual delta is the gate). Do not remove Tailwind (it still builds the modern utility CSS). Do not write into `docs-engineering` (read-only here; orchestrator note instead). Do not scrub historical DaisyUI mentions from roadmap/audit records. Do not ship without the regenerated lockfile (frozen-install trap). Never `git restore` directories.
- **Size** — M.
- **Delegation prompt** — In `/Users/daniel/Developer/Rottay/ui-design-system`, retire the dead DaisyUI layer from the modern engine per proposal P-03 (`roadmap/proposals.md`, APPROVED 2026-07-07). Depends on WO-ENG-08 (theme.css drain) being done — its consumer-proof gate in `scripts/engine-token-audit.mjs` is the authoritative list of surviving class hooks. (1) Inventory residual DaisyUI class usage in the nine modern engine files that reference DaisyUI (`primitives/navigation/{Segmented,Stepper,Steps,Link,Breadcrumb}/engines/modern.tsx`, `primitives/feedback/{Rate,Progress,Skeleton}/engines/modern.tsx`, `primitives/display/Timeline/engines/modern.tsx` — Segmented is a real `join`-class consumer) plus the built `dist/modern-engine.css`. (2) Re-express each residual consumer on the rottay-native pattern (`rottay-*` classes + `--ds-*` tokens); identical behavior/props; zero visual delta. (3) Remove `@plugin "daisyui"` from `packages/core/src/tokens/css/engines/modern/compiled.css` — Tailwind stays; confirm `pnpm --filter @rottay/design-system run build:modern-css` still emits a working `dist/modern-engine.css`. (4) Drop `daisyui` from `packages/core/package.json` devDependencies (:285) and regenerate `pnpm-lock.yaml` (`pnpm install`; verify with a local `--frozen-lockfile` install). (5) Correct the identity strings: `runtime/engines/registry.ts:60-61` (`displayName: 'Modern (Rottay Premium)'`, native `library` value — sweep the field's consumers), `contracts/engine/index.ts:11` + :72 doc comments, `_internal/testing/system/tests/engines.test.ts` expectations, and the `ui-design-system/CLAUDE.md` engine table ("modern — Rottay-native premium skin"). (6) Record in the handoff that `docs-engineering/engineering/design-system/runtime/engines/` needs the same correction — docs-engineering is READ-ONLY here; the orchestrator applies it at certification. (7) Final sweep: `grep -ri daisyui packages/core/src packages/core/package.json packages/showroom/src` = 0 hits (leave roadmap/audit historical records untouched). Gate: the grep returns 0; `pnpm --filter @rottay/design-system run build` + `pnpm test` green with the regenerated lockfile; `node scripts/engine-token-audit.mjs --check` green; sighted before/after in `test-artifacts/tokens/tok-03/` under rottay (dark surface, `#0A0A0C`) AND a light-surface tenant (bithire or evnto) with zero visual delta — run the showroom (`pnpm --filter @rottay/showroom run dev`, http://localhost:7001) and LOOK at the PNGs. Fences: classic/rustic untouched; Tailwind stays; no restyling; edit-only, no commits, never git-restore directories, app-bithire and docs-engineering read-only, showroom dev server allowed.

## Dependency summary

```
WO-TOK-01 (generated artifacts + DTCG) — no deps; CROSS-REPO window with app-evnto WO-IDN-06
                                          (same build-vertical-artifacts.mjs; second-lander
                                          re-verifies parity)
WO-TOK-02 (OKLCH color engine)         ── needs WO-ENG-06 + WO-TOK-01; shares the APCA scorer
                                          with WO-GAT-04 (whichever lands second reuses it)
WO-TOK-03 (DaisyUI retirement)         ── needs WO-ENG-08
```

Start order: **TOK-01 first and anytime** — no dependencies, and it is the regeneration
pipeline every later token change ships through; honor the written cross-repo window with
app-evnto WO-IDN-06 (never edit the generator files concurrently; whichever program executes
second re-verifies parity before certifying). **TOK-02 after WO-ENG-06 + WO-TOK-01.**
**TOK-03 after WO-ENG-08.** Every visual WO's acceptance gate additionally REQUIRES the sighted
before/after captures under both tenant palettes (a dark-surface tenant — rottay — and a
light-surface tenant — bithire or evnto; there is no light/dark toggle); the owner approves
signature moments.

- **AMENDMENT 2026-07-09 (orchestrator, under the owner's delegated approval)** — Surveyed against the code. **The premise partly holds; the SCOPE is materially wrong. Do not schedule this WO as written.**
  TRUE: `daisyui@^5.5.19` is a devDependency (`package.json:300`, not `:285`); `compiled.css` declares `@plugin "daisyui"`; `build:modern-css` runs inside `build`; `registry.ts:60-61` carries `displayName: 'Modern (DaisyUI)'` / `library: 'daisyui'`; `contracts/engine/index.ts:11,:72` and `engines.test.ts` carry the same identity.
  FALSE OR MISLEADING, in four ways:
  1. **"Nine modern engine files" is wrong at both ends.** `Segmented` renders NO `join` class today — only stale comments say it does. Meanwhile `Pagination` (a real `.join` consumer), `patterns/visualization/timeline` (`timeline`, `timeline-box`) and `patterns/misc/file-manager` (`link link-hover`) ARE real consumers this WO never lists.
  2. **The mechanism is wrong.** The WO frames the styling as coming from `dist/modern-engine.css`, but that 178KB artifact is exported as `./styles/modern` and imported by NO consumer — the showroom loads `styles.css`, app-bithire loads nothing. The load-bearing DaisyUI styling is the 38 `[data-tenant] .steps/.rating/.timeline/.breadcrumbs/a.link/.skeleton/.progress/.join` selectors in `tokens/css/engines/modern/theme.css`, which is ABSENT from the Files list and must be drained in lockstep or the `themeCss.unreferencedSelectors = 0` EXACT invariant breaks.
  3. **The dependency framing is off.** WO-ENG-08 is `done`, but `theme.css` is 1,692 lines and the DaisyUI class mappings it supposedly deleted are still present and consumer-proven. Those living mappings ARE this WO's real targets.
  4. **Its headline gate contradicts `pnpm test`.** `grep -ri daisyui packages/core/src == 0` demands scrubbing the word from ~291 files including generated tenant artifacts, while `theme.css` must KEEP 16 DaisyUI-5 `--color-*` variables and `modern-bridge.contract.test.ts` asserts the bridge. The two cannot both be satisfied.
  **Amended plan**: before scheduling, add `tokens/css/engines/modern/theme.css`, `Pagination`, `patterns/visualization/timeline` and `patterns/misc/file-manager` to Files; drop `Segmented`; state the artifact-regeneration step; and replace the `grep == 0` gate with "no DaisyUI CLASS consumer and no `@plugin \"daisyui\"` remain; doc-comment mentions and the DaisyUI-5 bridge variables are enumerated and accepted."

### WO-TOK-04 Retire the dead table-row variable aliases
- **Outcome** — The brand compiler emits one name per table-row channel. `pnpm --filter @rottay/design-system run lint` is green.
- **Why** — P-26 (proposals.md, APPROVED by the owner 2026-07-09). `compilers/brand-theme/index.ts:768-771` emits `--ds-table-row-bg-hover` AND `--ds-table-row-hover-bg`, `--ds-table-row-bg-striped` AND `--ds-table-row-striped-bg`. Components consume only the `-bg-hover` / `-bg-striped` spelling (4 and 6 references under `src/components/`); the other two have no consumer in the DS, app-bithire, app-platform, or app-evnto. `scripts/audit-integration.mjs` reports them as two `orphan-premium-var` violations, and core lint has been red since before 2026-07-09 (reproduced against commit `494e7d85` in a throwaway worktree). A standing red gate hides the next real one.
- **Depends on** — none. Lands FIRST among the new tokens WOs: all three edit `compilers/brand-theme/index.ts` and regenerate the tenant artifacts, and running them concurrently would race on the generated files.
- **Steps** —
  1. Delete the two dead emissions at `compilers/brand-theme/index.ts:769,771`.
  2. Regenerate the tenant artifacts (`packages/core/src/tokens/css/artifacts/**`, `packages/core/styles/**`) and pass the regenerate-and-diff parity guard: the ONLY delta may be the removal of those two variables.
  3. Confirm no app consumes them: `grep -rl -- "--ds-table-row-hover-bg\|--ds-table-row-striped-bg"` over the three sibling app repos returns nothing (they are READ-ONLY; only read).
- **Files** — `packages/core/src/compilers/brand-theme/index.ts`, `packages/core/src/tokens/css/artifacts/**`, `packages/core/styles/**`.
- **Acceptance gate** — `pnpm --filter @rottay/design-system run lint` exits 0 with `audit-integration: 0 violations`; `pnpm --filter @rottay/design-system run build` green; artifact diff contains only the two removed variables; `pnpm --filter @rottay/showroom run test:gates` green (zero pixel movement expected — a moved pixel means a component silently consumed one of them).
- **Do NOT** — Do not silence `audit-integration` by allowlisting the vars. Do not rename the surviving spelling. Removing an emitted variable is a public-surface change: record it in the release note.
- **Size** — S.

### WO-TOK-05 A dynamic dark tenant keeps its BrandTheme chrome
- **Outcome** — A DB-driven tenant on a dark theme renders the input, card, and modal chrome its own `BrandTheme` declares, exactly as a first-party artifact tenant does. The six `not-derived` entries in the WO-GAT-03 whitelabel baseline drop to zero and the gate holds them there.
- **Why** — P-24 (proposals.md, APPROVED by the owner 2026-07-09), the biggest whitelabel hole and the reason the hostile-tenant gate was built. The tenant CSS generator emits a dark block scoped `html[data-tenant='x'][data-theme='dark']` (specificity 0,2,1) containing hardcoded literals — e.g. `'--ds-card-bg': '#111827'` at `runtime/tenant/storage/static/generator/index.ts:565`. The compiled BrandTheme chrome rides in the light block (0,1,1), so the dark block wins and the tenant's `chrome.controls.input.*`, `chrome.cardComponent.*`, and `chrome.modal.*` are discarded. Proven at runtime: torture-dark asks for `input.bg: '#1A0014'` and paints `#0F172A`; it asks for `cardComponent.bg: '#120010'` and paints `#111827`. The LIGHT fixture honours the same channels, so the defect is dark-path-only. First-party verticals are immune only because their generated artifacts re-state the chrome — which is exactly the "true by construction" blind spot P-05 was written to expose.
- **Depends on** — WO-TOK-04 (same compiler file, same artifact regeneration).
- **Steps** —
  1. In `generator/index.ts`, make the dark block layer the tenant's COMPILED dark chrome rather than literals. A literal in the dark block is a default masquerading as a tenant value; the defaults belong in the foundation's `[data-theme='dark']` rules, where a tenant rule can override them.
  2. Preserve the derived dark values (`darkSemanticVariables`, `darkBrandingVariables`, `buildDarkRuntimeScale`) for tenants that declare no chrome — a tenant that says nothing must still get a usable dark surface.
  3. Update `generator` unit tests: assert a synthetic dark tenant with `chrome.cardComponent.bg` set gets THAT value, and one without it gets the derived default.
- **Files** — `packages/core/src/runtime/tenant/storage/static/generator/index.ts`, its test suite, `packages/core/src/compilers/brand-theme/index.ts` (only if the dark chrome map must be compiled), `packages/core/src/tokens/css/artifacts/**`, `packages/core/styles/**`.
- **Acceptance gate** — `pnpm --filter @rottay/showroom run test:whitelabel` green with `torture-baseline.json` shrunk to at most one entry (`select/native/background-color`, which is a different defect); regenerate the baseline with `TORTURE_UPDATE_BASELINE=1` and commit the shrunk file; `pnpm --filter @rottay/design-system run build` + `pnpm test` green; first-party artifact diff EMPTY (rottay/bithire/evnto already re-state their chrome, so their output must not move); sighted proof on `/probe/whitelabel-torture?fixture=torture-dark` that the input and card now carry the fixture's magenta-on-black chrome.
- **Do NOT** — Do not fix this by re-stating chrome in every artifact; that is the disease, not the cure. Do not weaken the whitelabel probe to make the baseline shrink.
- **Size** — M.

- **AMENDMENT 2026-07-09 (orchestrator, after measuring)** — This WO's acceptance gate demanded the whitelabel baseline shrink from 7 violations "to at most one". The measurement disproved the assumption behind that number: **five** of the six `not-derived` entries were generator-side and are fixed (input bg/border/color, card bg, modal bg); the sixth, `card/outlined/border-color`, has a DIFFERENT root cause in a DIFFERENT file and outside this WO's Files fence. `runtime/personality/primitives.ts:171` derives `--ds-card-border` from the `showBorder` boolean and `SystemCssVariablesBridge` stamps it INLINE on `<html>`, which beats even the more specific generated dark block that correctly carries the tenant's `#4D0033`. Verified in the browser: the generated CSS is right; the inline stamp wins.
  **Amended gate**: the baseline shrinks to 2 (`card/outlined/border-color` = the bridge defect, now WO-ENG-19; `select/native/background-color` = a different defect). The gate number was set on an assumption; the probe corrected it, which is what the probe is for. Do not read this as the gate being loosened to fit the result -- the root cause is named, attributed, and given its own work order with the shrink-to-1 gate.

### WO-TOK-06 The app canvas becomes a BrandTheme channel
- **Outcome** — A tenant declares its own canvas on both surfaces. A dynamic light tenant renders light components on a light ground; a dynamic dark tenant's `darkBackgroundColor` reaches `--ds-color-bg-primary`.
- **Why** — P-25 (proposals.md, APPROVED by the owner 2026-07-09). `palette.darkBackgroundColor` compiles to `--ds-color-dark-bg` and never reaches `--ds-color-bg-primary`, and `BrandPalette` has no light background field at all. The foundation is dark-first (`:root { --ds-color-bg-primary: #0A0A0C }`) with no `[data-theme='light']` canvas block, so a dynamic light tenant paints light components on a dark ground — visible in `test-artifacts/gates/gat-03/torture-light.png`, and measured: torture-dark asks for `#050307` and the canvas paints `#0a0a0a`. Only generated first-party artifacts set the canvas per tenant. The owner's 2026-07-07 decision — the tenant palette decides the ground, there is no user-facing light/dark toggle — is therefore unimplementable for any tenant that is not first-party. (This is NOT the withdrawn P-07: no toggle is proposed.)
- **Depends on** — WO-TOK-05.
- **Steps** —
  1. Add a light `backgroundColor` to `BrandPalette` (`contracts/themes/index.ts`), mirroring `darkBackgroundColor`.
  2. Bridge both fields onto the canvas token family in the compiler and the generator, so the surface tokens (`--ds-color-bg-primary/secondary/elevated`) derive from the tenant's declared ground rather than from a foundation literal.
  3. Give the foundation a `[data-theme='light']` canvas block, so a tenant that declares nothing still gets a light ground in light mode instead of inheriting the dark-first `:root`.
  4. Extend the WO-GAT-03 probe with a `ground/root/background-color` entry now that the channel exists. Note why it could not be added before: with no channel, the two fixtures differ only because each falls back to a different default, so the probe would have passed for the wrong reason.
- **Files** — `packages/core/src/contracts/themes/index.ts`, `packages/core/src/compilers/brand-theme/index.ts`, `packages/core/src/runtime/tenant/storage/static/generator/index.ts`, `packages/core/src/tokens/css/foundation/themes/default.css`, `packages/showroom/e2e/whitelabel/torture.spec.ts`, `packages/core/src/tokens/ts/brand-themes/torture.ts`, artifacts + `packages/core/styles/**`.
- **Acceptance gate** — `pnpm --filter @rottay/showroom run test:whitelabel` green with the new canvas probe passing under both torture fixtures; `test:gates` green with first-party baselines UNMOVED (rottay/bithire artifacts already declare their canvas, so their pixels must not shift); `pnpm test` + build green; sighted proof that `?fixture=torture-light` renders on a light ground.
- **Do NOT** — Do not introduce a user-facing light/dark toggle (owner decision 2026-07-07; P-07 withdrawn). Do not let the foundation's light canvas outrank a tenant's declared one.
- **Size** — M.
