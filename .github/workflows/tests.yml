# Retained for backward compatibility and badge URLs.
# All work is done by ci.yml.
#
# Badge URL:
# https://github.com/rottay/design-system/actions/workflows/tests.yml/badge.svg
name: Tests

on:
  workflow_dispatch:

jobs:
  tests:
    name: Tests (via CI)
    uses: ./.github/workflows/ci.yml
    permissions:
      contents: read
      pull-requests: write
