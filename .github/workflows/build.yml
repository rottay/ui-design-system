# Retained for backward compatibility and badge URLs.
# All work is done by ci.yml.
#
# Badge URL:
# https://github.com/rottay/design-system/actions/workflows/build.yml/badge.svg
name: Build

on:
  workflow_dispatch:

jobs:
  build:
    name: Build (via CI)
    uses: ./.github/workflows/ci.yml
    permissions:
      contents: read
      pull-requests: write
