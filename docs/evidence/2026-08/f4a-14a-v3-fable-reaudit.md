# F4A-14a v3 — preauditoría final Fable 5 (read-only, falsificable)

VERDICT: ACCEPT

Auditor: Fable 5 (read-only). DT: Codex. Autor del brief: Cloud Opus. Fecha: 2026-08-21.
Brief auditado: /private/tmp/f4a-14a-opus-implementation-brief-v3.md — SHA-256 `06ecaa76c0a54b018fa91ca5d1bc68326e83e0af8af49c7779361a9ec0622ff1` (verificado byte-exacto).
Insumos verificados byte-exactos: mi REJECT v2 `adb815ef7f30dd2d3be5c860271424fd6dbb562ad9556f22a5051949f8e3e53b`; mi preaudit v1 `50b307dedc89c751c9b77096d8a0a0dd5470258af76e985faa4c0203932adbd2`.

## Snapshot

HEAD `9d5582dfdf1d02f1d7e8fd468720b1d829e50454`, worktree limpio (0 porcelain, re-verificado al cierre). Los 3 hashes de pre-estado del write-set re-medidos esta ronda y exactos a §1 del brief (`35a6912f…` / `7c8f562b…` / `1808731f…`) — el árbol no cambió desde mis dos rondas previas, así que toda medición anterior sigue anclada; las críticas se re-midieron igual en fresco.

## Verificación independiente de los nueve frentes

**(1) Aritmética central — VERIFICADA EN FRESCO.** Suma de las `assignments` de las 64 raíces: **268 = rottay 101 + bithire 97 + evnto 70** (coherencia total=r+b+e ✓). `tier.page.ink.assignments = {total:5, rottay:2, bithire:2, evnto:1}` y el drift reconcilia al entero en las cuatro cifras: 263+5=268, 99+2=101, 95+2=97, 69+1=70 — el 263 de la prosa era el snapshot de 63 raíces y F4A-6 agregó la raíz sin asentarlo. Deltas por raíz verificados contra el estado actual (raised.fg −2 rottay; overlay.fg −2 rottay; overlay.bg −2 rottay −1 bithire; total −7): destino **261 = 95 + 96 + 70**, exacto a §2.9/§5/stop-9 del brief. El P0-2 de mi v2 quedó absorbido con la narrativa de dos hechos separados (drift F4A-6 ajeno a K4; delta K4 −7) y el mandato "derivado, no copiado".

**(2) Las 8 reparaciones + correcciones previas — TODAS INCORPORADAS Y VERIFICADAS.** Sección por sección contra el árbol: §2.1/§2.2 con ×2 y modes `["base","dark"]` preservando la posición dark de bithire (P0-1); §3 con 58 raíces / 174 entries — re-contado en fresco: las 8 raíces abarcan exactamente 24 de las 198 entries, restan 174 (P1-1); §4.7 con las dos anclas de vocabulario y sus conteos exactos (P1-2); §4.6 sin el rótulo NOOP (P2-1); §2.4 con ambos predicados y sus resultados 15/17/0 y 15/15/0 (P2-2 — los 2 extra son `textareaPaddingX/Y` autorados en bithire, verificado en ronda 1); §2.9 correcto (P2-3 + P0-2); §2.3 con el destino del segundo canal restringido a `derivation`/`evidence` y el fence anti-`collapses`, y la tabla de headers con los tres números medidos (P2-4); §2.8 como reparación PARCIAL declarada con el residuo del canal para F4A-close, sin clausura falsa, exigida también en el reporte §10 y listada en §11 (P2-5). La celda §0 P2-4 quedó corregida (503/523/577; :185 es licencia).

**(3) Assignments base+dark de bithire en raised/overlay fg — VERIFICADO EN FRESCO.** El bloque dark del artefacto bithire abre en `:1251` y emite LOS CANALES CABEZA NUEVOS: `--ds-material-overlay-foreground` en `:1545` y `--ds-material-raised-foreground` en `:1566`; base en `:717`/`:741`. Rottay y evnto: **0 ocurrencias** de los cuatro canales material/overlay en sus artefactos (contado). La autoría en fuente (dark overlay de bithire, `@domicile derived`) quedó verificada en mis rondas previas sobre este mismo árbol. `authoredLeafPaths` es un export real de `manifest/mirror-parity/index.mjs`.

**(4) Overlay: canal/status/segundo canal/no-materialización — VERIFICADO EN FRESCO.** La asimetría que justifica ×1 en overlay.bg: el head nuevo `--ds-material-overlay-background` se emite sólo en base (`:716`); el dark emite el secundario `--ds-surface-overlay` (`:1664`, contra `:965` en base). Pisos autorados: `default.css:433` (raised) y `:434` (overlay-bg) → sus "existe" se sostienen; `--ds-material-overlay-foreground` con **cero** declaraciones autoradas en todo src/ excluyendo artifacts (grep exit 1) → `solo-artefacto` es OBLIGADO por el freshness gate. El pin de ausencia del Tour test está en `:55-64` con el comentario del incidente verbatim. La advertencia de la declaración circular es documentación; ninguna sección manda materializar y los fences lo prohíben.

**(5) effect.intensity — VERIFICADO EN FRESCO.** Emisiones `1 / 0.58 / 1` (rottay `:510`, bithire `:458`, evnto `:233`); fuente bithire `effectIntensity: 0.58` en `index.ts:4034` con `@domicile seed` + dial; declinaciones explícitas byte-exactas en las líneas citadas (rottay `3767-3769`, evnto `2076-2078`: `@placeholder SURFACES.effectIntensity` + `@domicile unassigned` + governor "gap aceptado…"); el texto-licencia a reemplazar existe en `roster-variantes.md:185`. La nota obligatoria del brief coincide con la verdad medida y prohíbe la regresión `0.58 → 1`.

**(6) Roster — VERIFICADO EN FRESCO.** 198 entries, 66 roots (universe 63+3 como declara §3); headers `.md:503/:523/:577` byte-coherentes con la tabla del brief (canal, exposure, status — incluida la instrucción de mover el status de `:523` a solo-artefacto); `:469`/`:495` correctamente excluidos (otras raíces) y `:717` como prosa histórica; 174 entries restantes exactas. El texto exacto del governor F4B del roster (`"dial: (raiz autora — dial en F4B)"`, 9 entries, 3 bithire) y los conteos de tags de fuente (797/618/95) re-confirmados; la cuarta entry bithire deliberada está declarada en §4.7.

**(7) Write-set — VERIFICADO.** Exactamente 3 paths, limpios en HEAD con hashes exactos; prohibiciones explícitas sobre temas/generados/artifacts/styles/dist con referentes reales; las invariantes 1-2 se prueban por hash en la batería; el write-set no toca fuente compilable. `stash@{0}` ajeno presente y cercado.

**(8) Gates, invariantes y stop conditions — SUFICIENTES.** Ambos gates corridos en fresco esta ronda: verdes (64 roots, 48/10/6; 26/28/10). Post-reparación simulada en rondas previas sobre este mismo árbol: freshness verde con 47/10/7 (la única movida es además forzada por el gate), exposure intacto (ningún control declara los canales nuevos — re-grep en fresco, exit 1; `token-overrides` existe; exposure no se toca y el baseline pineado se sostiene). Stop 1-9 cubren: pre-estado, regeneración (verificado que no puede disparar en falso: el gate valida contra el árbol sin hash ni productor), 26/28/10, 64 y 47/10/7, scope (§2.10), generador del roster (verificado negativo), hashes de tema/artefacto, cuarto path, y la derivabilidad del 261 con prohibición de ajustar a mano. Restore sin `git checkout` (cumple la ley post-incidente).

**(9) Sin productores ocultos ni semántica disfrazada — VERIFICADO.** Productores/consumidores de `roster-variantes`: cero (grep en fresco, exit 1). Lectores del catálogo en scripts: los dos gates más un tercero que ninguna ronda previa había enumerado — `scripts/ci/f0-honesty-gates/index.test.mjs` — inspeccionado esta ronda: corre el freshness gate sobre el árbol real esperando PASS (que se mantiene post-reparación) y drills con violaciones plantadas sobre árboles SINTÉTICOS (`fake.root`); no clava conteos, canales ni assignments del catálogo real → benigno, no se enrojece con el lote. La semántica de asignación ("emisión del canal cabeza por modo") NO está disfrazada: §2.3 la declara como instrucción del DT, se registra en `derivation` de roots[22], se aplica uniformemente — reproduce exactamente ×2/×2/×1 sobre las tres raíces, verificado contra las emisiones — y §11 lista la cuestión general como NO cerrada por este lote. El catálogo es autoridad auto-declarada `"MEDIDO -- inventario de lectura"` enmendable bajo gate; `corrections` es `{statement, items}` como §2.9 afirma; la prosa "63 raíces" de `method.roots`/`reconciliation.statement` verificada presente y correctamente tratada (registrar, no reescribir — reescribirla exigiría re-medir 3275, fuera de K4).

## Hallazgos

P0: ninguno. P1: ninguno. P2: ninguno bloqueante — dos notas menores que no requieren reemisión: (i) el bloque "Insumos de esta reemisión" de §1 conserva la lista v1-era (el header del brief ya trae los tres hashes completos, incluido mi v2 — redundancia inocua); (ii) al reparar el roster de `effect.intensity`, las filas del `.md` en ~181-183 (rottay/evnto `authored/seed`) caen bajo la cláusula general de consistencia `.md`/`.json` — ya cubierta por §3, sólo se señala para el barrido del worker.

## Comandos read-only ejecutados esta ronda

`git rev-parse HEAD`; `git status --porcelain` (×2, apertura y cierre); `shasum -a 256` (brief v3, ambos insumos, los 3 del write-set); `node -e` de suma de las 64 `assignments` + `tier.page.ink` + deltas por raíz + forma de `corrections` + prosa "63"; `node -e` del roster (198/66, F4B exacto, 24 in-scope/174 restantes); `sed` de `.md:185/503/523/577`; greps de emisiones en los 3 artefactos (canales material, surface-overlay, effect-intensity, line-height, icon-size, apertura del bloque dark), pisos en `default.css`, cero declaraciones de overlay-fg, Tour test `:55-64`, declinaciones rottay/evnto, `0.58` en fuente; ejecución de ambos gates (verdes); búsqueda de productores de roster (negativa), lectores del catálogo (tercer lector inspeccionado y adjudicado benigno) y controles declarando los canales nuevos (negativa). Cero escrituras en el repo, cero tests mutantes/build/generadores, cero git mutante. Write-set de esta ronda: este archivo y su `.ready`.

## Cierre

El brief v3 es emitible tal como está: las diez correcciones acumuladas de mis dos REJECT están incorporadas y verificadas de primera mano contra el árbol, la aritmética central deriva limpia (268−7=261=95+96+70), los gates la aceptan, las stop conditions la protegen, y nada queda vendido como cerrado que no lo esté (iconSize parcial, scope, semántica general, F4B, prosa "63" — todos en §11). El repo queda intacto: worktree limpio re-verificado al cierre de la auditoría.
