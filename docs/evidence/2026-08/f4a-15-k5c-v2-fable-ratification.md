# F4A-15 / K5c v2 — ratificación Fable 5 del mapa semántico corregido (read-only)

VERDICT: ACCEPT

Auditor: Fable 5 (read-only). DT: Codex. Fecha: 2026-08-21.
Documento ratificado: `/private/tmp/f4a-15-k5c-sonnet-semantic-map-v2.md` — SHA-256 `fdf3ccc7a1831dce49636afde07d3d5c2f7c73f33bf9a38bc20caff58b3c9c4b` (verificado byte-exacto).
Mi auditoría previa: SHA-256 `6e11dca156e62ce7bf9f791961797c9336151822b25962701b012dae98714a3a` (verificado byte-exacto).
HEAD `9d5582dfdf1d02f1d7e8fd468720b1d829e50454`, worktree limpio, staged 0 — verificados a la apertura y al cierre. **K4/K5 writes cercados, no tocados. Ningún `OPEN_KIMI` reabierto ni adjudicado.**

## Los ocho chequeos, con verificación propia

**1. F-1…F-6 incorporadas sin perder el núcleo — CONFIRMADO.** Las seis correcciones de mi auditoría están cada una en su sección, re-medidas por Sonnet (no copiadas) y coherentes con lo que yo mismo medí sobre este árbol: anatomy reformulado (§1), filas #9/#10 corregidas (§2/§3), §1-de-v1 reescrito como "cero alias + cuatro default-inheritance direccionales" con las cuatro citas exactas (§4), STOP de esquema retirado (§5), censo de 98 anexado con stop nueva (§6), sidecar con mis tres condiciones (§7), read-set ampliado (§8). El núcleo queda intacto y restatado: **37→34, un solo colapso (padding), 16/0/30 = 46 exacto** — los mismos números que reproduje independientemente en mi auditoría.

**2. `anatomy` — CONFIRMADO, y v2 lo deja MEJOR anclado que mi propia auditoría.** Verifiqué esta ronda el bloque de capability (`capabilities/index.ts:413-430`): `brandThemePath: 'chrome.{cardComponent,table,sidebar,layout}.anatomy'` — la MISMA ruta que `BrandTableChrome.anatomy` —, `derivedChannels: []`, `derivedRootAttributes: ['data-anatomy-card','data-anatomy-table',…]`, `compat: 'additive; envelope-gated (allowAnatomyVariants)'` y `defaultBehavior: 'default anatomy; fails closed unless the vertical envelope opts in'` (fail-closed, un dato aún más fuerte que el que v2 cita). Y verifiqué que el skin modern tiene reglas para LOS TRES valores no-default: `ruled` (2126,2131), `zebra` (2140,2148,2154) y **`open` (2167,2173,2178,2183)** — v2 completó lo que mi auditoría citó parcialmente (yo sólo nombré ruled/zebra). Plane=attribute, policy-gated, nunca "desconectado" ✓. **El séptimo `OPEN_KIMI` es legítimo y NO duplica los seis de Opus v2**: los seis versan sobre domicilios/criterios/razones del plano CSS-canal (seed-vs-baseline, root-coincidence, criterio graduado, las 18 de rottay, unión+sentido, razones de baseline); el séptimo pregunta si el plano ATRIBUTO entra al roster/placeholder de temas — una dimensión de transporte que ningún ítem previo contenía, nacida de F-1. v2 lo plantea sin adjudicarlo ✓.

**3. El matiz propio de Sonnet — VERIFICADO EXACTO, y es un aporte real.** `modern/skin/table.css:131` consume `letter-spacing: var(--ds-table-header-letter-spacing);` **a pelo** (y `:132` el text-transform, también a pelo) — leído directamente esta ronda. Mi auditoría corrigió la fila #9 citando sólo `data-table.css` (que SÍ tiene piso eyebrow→`0.08em`); generalizar eso a "no observable" habría sobre-afirmado en la dirección opuesta, exactamente como v2 dice. La respuesta por-consumidor (Obs.=no en PatternDataTable, Obs.=sí en el primitivo Table) es la correcta. **No cambia el censo semántico**: la columna Obs. es anotación por consumidor; el estado AUTHORED/ABSENT por tenant —lo único que el 46 cuenta— no se mueve. 16/0/30=46 queda intacto ✓.

**4. Los cuatro fallbacks #17/#19/#20/#21 — CIERRAN EL STOP, citas sin sobre-afirmación.** Spot-verificados línea por línea esta ronda: #17 `rowBgSelected` → `color-mix(primary 9%, transparent)` (data-table.css:1296-1300 ✓); #19 `rowBorder` → `var(--ds-color-border-subtle)` (:69-71, vía `--ds-modern-table-divider` ✓); #20 `rowHoverShadow` → `inset 0 0 0 1px color-mix(primary 10%, transparent)` (:1345-1349 ✓); #21 `rowFocusShadow` → `inset 0 0 0 2px color-mix(primary 52%, transparent)` (:1325-1329 ✓) — los porcentajes de v2 (9/10/52) son exactos. Y la afirmación complementaria "estos 4 canales no se consumen en `table.css`" verificada: 0 ocurrencias. También verifiqué la reestructura de F-4: `data-table.css:1177-1180` anida `headerShadow → headerBorder → border` tal como v2 lo cita. El retiro completo del `STOP_NEEDS_SCHEMA` es legítimo.

**5. 132 / 34 / 3 / 98 — CONFIRMADO CONTRA MI PROPIO CENSO.** Mi auditoría midió con método independiente (parser de `var(` multilínea en Node) los mismos números que v2 obtuvo con `grep -oE` + `comm`: 132 canales `--ds-table-*` consumidos en modern; 34 = emitidos∩consumidos; los 3 muertos exactos (`filterRowBg`, `filterFocusShadow`, `loadingOverlayBg`) como únicos emitidos-sin-consumo; 98 = 132−34 consumer-only. La aritmética cruza limpia (34+3=37=campos emisibles del tipo). **La stop nueva está y es exacta**: ningún `@placeholder`/tag de tema puede cubrir uno de los 98 como si fuera campo de `BrandTableChrome` — darles domicilio exige ampliar la interfaz, un cambio contractual, no un tag. v2 la declara verbatim en §6 ✓.

**6. Sidecar — NO crea segunda autoridad.** v2 incorpora mis tres condiciones exactas: la ley vive en el TIPO (`themes/index.ts:2088`) y el sidecar es proyección GENERADA (o manual con `lawSource` archivo:línea y disciplina de baseline); domiciliado bajo `variant-parity` (rechazado el root nuevo `manifest/semantic-groups/` de primer nivel); esquema con `plane: "css-channel" | "attribute"` desde el día uno (F-1 lo hizo no-hipotético). Colapso como paso DENTRO de variant-parity con guarda anti-rename; el ratchet léxico queda intacto como unidad propia. Alcance inicial 1 familia/1 grupo. Nada escrito ✓.

**7. Rustic — sólo salvedad, censo intacto.** Verificado esta ronda: rustic invierte la prioridad (genérico primero) y sus `th`/`td` de la tercera talla usan `var(--ds-table-cell-padding, 1rem 1.25rem)` sin canal de densidad (engines/rustic/index.tsx:73,77 ✓, además de 53-67 ya verificadas). v2 lo cita explícitamente como salvedad anti-generalización y declara que NO cambia el 46 (universo de autoría, no de consumo por engine) ✓.

**8. Cero writes — CONFIRMADO.** Repo intacto a apertura y cierre (HEAD idéntico, porcelain 0, staged 0); K4/K5 sin tocar; cero tests mutantes/build/generadores en mi ronda. La nota de higiene de v2 (dos redirecciones `>` a /tmp creadas y borradas durante el censo, rehecho con sustitución de proceso) está declarada honestamente, no tocó el repo, y sigue el precedente aceptado de v1.

## Notas no bloqueantes (para el acta, no exigen reemisión)

1. v2 §6 remite a "stop 8 en §9", pero §9 es la sección de rustic y v2 no restata la lista numerada de stops de v1 — el puntero interno queda colgado. Inofensivo: la stop nueva está declarada verbatim inline en §6, y las stops 1-7 de v1 siguen vigentes por herencia declarada de la reemisión.
2. Errata tipográfica en §10: "No until muevo ninguna hoja" — se entiende "No muevo". Cosmética.

## Qué queda `OPEN_KIMI` (exacto, sin posición atribuida)

Los **seis de Opus v2 §7, intactos**: (1) `seed` vs `baseline` en base/dark; (2) la regla general de root-coincidence governor; (3) el criterio graduado para coincidencias con canales no-raíz; (4) las 18 de rottay; (5) el SENTIDO de los gaps —la mitad "unión" queda `MEASURED` con esta v2 (34 ejes, 46=16/0/30); la mitad "¿evnto deliberadamente mínimo o incompleto?" sigue abierta—; (6) la razón falsable de cada `baseline`. Más el **séptimo, nuevo**: ¿el plano ATRIBUTO (`anatomy`) entra al roster/placeholder de temas con domicilio propio, o es categoría aparte con su propia ley de cobertura? Ninguno adjudicado por Sonnet, por mí, ni por esta ratificación.

## Alcance de esta ratificación

**Esta ratificación NO habilita implementación alguna.** Ratifica el mapa v2 como medición (`MEASURED`) del paso 1 de K5c: la unión semántica y sus censos. Los placeholders siguen en CERO; ningún tag, placeholder, sidecar, gate ni corrección de prosa puede escribirse hasta que (a) Kimi adjudique los `OPEN_KIMI` pertinentes —en particular el 5 (sentido de los 46) para cualquier placeholder, y el 7 para cualquier cobertura del plano atributo— y (b) el DT emita el brief de escritura correspondiente con su propio ciclo de preauditoría. Las stop conditions de v1 más la nueva de §6 (los 98 consumer-only exigen cambio contractual, jamás placeholder) quedan ratificadas como parte del contrato del futuro lote.

## Comandos read-only ejecutados

`git rev-parse HEAD`, `git status --porcelain`, `git diff --cached --name-only` (apertura y cierre); `shasum -a 256` de ambos insumos; lectura completa del mapa v2; verificación contra el árbol de todos los claims nuevos de v2: bloque de capability `Anatomy variants` (capabilities/index.ts:413-430), las 9 reglas `data-anatomy-table` incluidas las 4 de `open`, `table.css:125-135` (letter-spacing y text-transform a pelo, y el comentario de header-block-size deliberadamente no consumido), `data-table.css:1176-1181` (nido headerShadow→headerBorder→border), `:69-71` (rowBorder→border-subtle), `:1296-1300`/`:1345-1349`/`:1325-1329` (los tres shadows/selected con sus porcentajes), grep de los 4 canales en `table.css` (0), `engines/rustic/index.tsx:72-78`. Los censos 132/34/3/98 y los conteos 37→34/46 cruzados contra mis propias mediciones de la ronda anterior (árbol idéntico por HEAD y hashes). **Cero escrituras al repo; write-set exacto: este memo y su `.ready`.**

## Repo intacto

Cierre verificado: `git rev-parse HEAD` = `9d5582dfdf1d02f1d7e8fd468720b1d829e50454`, `git status --porcelain` vacío, staged 0. K4/K5 intactos.
